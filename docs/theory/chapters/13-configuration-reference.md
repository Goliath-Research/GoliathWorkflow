# Configuration Reference (moved)

Exhaustive action/`actionConfig` parameter tables live in the Reference pillar:

**[`docs/reference/configuration-reference.md`](../../reference/configuration-reference.md)**

Theory chapters cross-link parameter *meaning*; the reference doc is the canonical lookup for types, bounds, and operator-set knobs (site/profile — not study manifests).
