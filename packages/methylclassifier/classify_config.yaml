# Configuration for sample classification
# This file specifies the directories and parameters for classifying samples

# Directory containing JSON config files with sample lists
config_dir: "/home/ubuntu/Work/samples/humans/psomagen/AN00026418/centroids/pb-cancer-12"

# Directory containing trained classifier models (.pkl files)
model_dir: "/home/ubuntu/Work/samples/humans/psomagen/AN00025834/data/detection/pb-ch"

# Optional: Specific chromosome to process (leave empty for all)
chromosome: ""

# Optional: Specific context to process (CG, CHG, CHH) (leave empty for all)
context: ""

# Output file for results (CSV format)
output_file: "classification_results.csv"

# Debug mode (true/false)
debug: false
