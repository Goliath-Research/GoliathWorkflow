# methyl_domain

Tagged JSON domain types for MethylPipeline workflow scope variables (`$type` discriminator).

Export schemas:

```bash
source .venv/bin/activate
pip install -e packages/methyldomain
methyl-export-domain-schemas
methyl-export-domain-schemas --check
```
