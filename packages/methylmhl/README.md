# methylmhl

Native methylation haplotype block (MHB) discovery and methylation haplotype
load (MHL) for MethylPipeline. Consumes `{chrom}-CG.mhap.h5` sidecars — not
informME tile histograms.

CLI: `methyl-mhl --project <project.json> --resolved-config <slice.json>`
