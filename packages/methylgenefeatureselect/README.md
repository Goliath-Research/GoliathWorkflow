# methyl-gene-feature-select

Phase 3: select minimal **gene × region** features (promoter, exon, intron, gene_body, terminator)
via ECDF OvR prefix k-search for `feature_family_set: structural_scored` tabular backends.

Outputs `gene-features-classifier.csv` with `(gene_name, feature_type)` rows.
