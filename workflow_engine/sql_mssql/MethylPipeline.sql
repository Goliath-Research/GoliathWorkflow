--
-- Script was generated by Devart dbForge Studio for SQL Server, Version 2026.1.39.0
-- Product home page: https://www.devart.com/dbforge/sql/studio
-- Script date 6/8/2026 9:10:58 AM
-- Server version: 12.0.2000.8
--

--
-- Create schema [wf]
--
PRINT (N'Create schema [wf]')
GO
CREATE SCHEMA wf AUTHORIZATION dbo
GO

SET QUOTED_IDENTIFIER, ANSI_NULLS ON
GO

--
-- Create or alter function [wf].[wf_json_fragment_from_string]
--
GO
PRINT (N'Create or alter function [wf].[wf_json_fragment_from_string]')
GO

CREATE OR ALTER FUNCTION wf.wf_json_fragment_from_string(@s NVARCHAR(MAX))
RETURNS NVARCHAR(MAX)
AS
BEGIN
    RETURN N'"' + REPLACE(REPLACE(REPLACE(@s, N'\', N'\\'), N'"', N'\"'), CHAR(10), N'\n') + N'"';
END;
GO

--
-- Create table [wf].[workflow_def]
--
PRINT (N'Create table [wf].[workflow_def]')
GO
CREATE TABLE wf.workflow_def (
  id bigint IDENTITY,
  name nvarchar(256) NOT NULL,
  description nvarchar(max) NULL,
  created_at_utc datetime2 NOT NULL CONSTRAINT DF_workflow_def_created DEFAULT (sysutcdatetime()),
  PRIMARY KEY CLUSTERED (id)
)
GO

--
-- Create table [wf].[workflow_version]
--
PRINT (N'Create table [wf].[workflow_version]')
GO
CREATE TABLE wf.workflow_version (
  id bigint IDENTITY,
  workflow_def_id bigint NOT NULL,
  version_major int NOT NULL CONSTRAINT DF_wfv_major DEFAULT (1),
  version_minor int NOT NULL CONSTRAINT DF_wfv_minor DEFAULT (0),
  is_active bit NOT NULL CONSTRAINT DF_wfv_active DEFAULT (1),
  root_node_id bigint NULL,
  PRIMARY KEY CLUSTERED (id),
  CONSTRAINT UQ_wfv_def_version UNIQUE (workflow_def_id, version_major, version_minor)
)
GO

--
-- Create foreign key [FK_wfv_def] on table [wf].[workflow_version]
--
PRINT (N'Create foreign key [FK_wfv_def] on table [wf].[workflow_version]')
GO
ALTER TABLE wf.workflow_version
  ADD CONSTRAINT FK_wfv_def FOREIGN KEY (workflow_def_id) REFERENCES wf.workflow_def (id)
GO

--
-- Create table [wf].[workflow_instance]
--
PRINT (N'Create table [wf].[workflow_instance]')
GO
CREATE TABLE wf.workflow_instance (
  id bigint IDENTITY,
  workflow_version_id bigint NOT NULL,
  status varchar(32) NOT NULL CONSTRAINT DF_wi_status DEFAULT (N'CREATED'),
  context_json json NULL,
  started_at_utc datetime2 NULL,
  completed_at_utc datetime2 NULL,
  CONSTRAINT PK__workflow__3213E83FCD137946 PRIMARY KEY CLUSTERED (id),
  CONSTRAINT CK_wi_status CHECK ([status]=N'CANCELLED' OR [status]=N'FAILED' OR [status]=N'COMPLETED' OR [status]=N'RUNNING' OR [status]=N'CREATED')
)
GO

--
-- Create index [IX_wi_version_status] on table [wf].[workflow_instance]
--
PRINT (N'Create index [IX_wi_version_status] on table [wf].[workflow_instance]')
GO
CREATE INDEX IX_wi_version_status
  ON wf.workflow_instance (workflow_version_id, status)
GO

--
-- Create foreign key [FK_wi_version] on table [wf].[workflow_instance]
--
PRINT (N'Create foreign key [FK_wi_version] on table [wf].[workflow_instance]')
GO
ALTER TABLE wf.workflow_instance
  ADD CONSTRAINT FK_wi_version FOREIGN KEY (workflow_version_id) REFERENCES wf.workflow_version (id)
GO

--
-- Create table [wf].[scope_variable]
--
PRINT (N'Create table [wf].[scope_variable]')
GO
CREATE TABLE wf.scope_variable (
  workflow_instance_id bigint NOT NULL,
  scope_node_execution_id bigint NOT NULL,
  var_name nvarchar(128) NOT NULL,
  value_json nvarchar(max) NOT NULL,
  updated_at_utc datetime2 NOT NULL CONSTRAINT DF_sv_updated DEFAULT (sysutcdatetime()),
  CONSTRAINT PK_scope_variable PRIMARY KEY CLUSTERED (workflow_instance_id, scope_node_execution_id, var_name)
)
GO

--
-- Create index [IX_sv_instance_scope] on table [wf].[scope_variable]
--
PRINT (N'Create index [IX_sv_instance_scope] on table [wf].[scope_variable]')
GO
CREATE INDEX IX_sv_instance_scope
  ON wf.scope_variable (workflow_instance_id, scope_node_execution_id)
GO

--
-- Create foreign key [FK_sv_instance] on table [wf].[scope_variable]
--
PRINT (N'Create foreign key [FK_sv_instance] on table [wf].[scope_variable]')
GO
ALTER TABLE wf.scope_variable
  ADD CONSTRAINT FK_sv_instance FOREIGN KEY (workflow_instance_id) REFERENCES wf.workflow_instance (id) ON DELETE CASCADE
GO

--
-- Create table [wf].[instance_cursor]
--
PRINT (N'Create table [wf].[instance_cursor]')
GO
CREATE TABLE wf.instance_cursor (
  workflow_instance_id bigint NOT NULL,
  last_polled_at_utc datetime2 NULL,
  notes nvarchar(512) NULL,
  PRIMARY KEY CLUSTERED (workflow_instance_id)
)
GO

--
-- Create foreign key [FK_ic_instance] on table [wf].[instance_cursor]
--
PRINT (N'Create foreign key [FK_ic_instance] on table [wf].[instance_cursor]')
GO
ALTER TABLE wf.instance_cursor
  ADD CONSTRAINT FK_ic_instance FOREIGN KEY (workflow_instance_id) REFERENCES wf.workflow_instance (id) ON DELETE CASCADE
GO

--
-- Create table [wf].[workflow_action]
--
PRINT (N'Create table [wf].[workflow_action]')
GO
CREATE TABLE wf.workflow_action (
  id bigint IDENTITY,
  action_name nvarchar(256) NOT NULL,
  capability nvarchar(128) NULL,
  payload_schema_ref nvarchar(512) NULL,
  PRIMARY KEY CLUSTERED (id),
  CONSTRAINT UQ_workflow_action_name UNIQUE (action_name)
)
GO

--
-- Create table [wf].[workflow_node]
--
PRINT (N'Create table [wf].[workflow_node]')
GO
CREATE TABLE wf.workflow_node (
  id bigint IDENTITY,
  workflow_version_id bigint NOT NULL,
  node_type varchar(32) NOT NULL,
  node_key nvarchar(128) NOT NULL,
  workflow_action_id bigint NULL,
  repeat_count int NULL,
  condition_ref_node_key nvarchar(128) NULL,
  switch_ref_node_key nvarchar(128) NULL,
  condition_var nvarchar(128) NULL,
  switch_var nvarchar(128) NULL,
  PRIMARY KEY CLUSTERED (id),
  CONSTRAINT UQ_wn_version_key UNIQUE (workflow_version_id, node_key),
  CONSTRAINT CK_wn_node_type CHECK ([node_type]=N'WHILE' OR [node_type]=N'REPEAT' OR [node_type]=N'SWITCH' OR [node_type]=N'IF' OR [node_type]=N'PARALLEL' OR [node_type]=N'SEQUENCE' OR [node_type]=N'ACTION')
)
GO

--
-- Create index [IX_workflow_node_version] on table [wf].[workflow_node]
--
PRINT (N'Create index [IX_workflow_node_version] on table [wf].[workflow_node]')
GO
CREATE INDEX IX_workflow_node_version
  ON wf.workflow_node (workflow_version_id)
GO

--
-- Create foreign key [FK_wn_action] on table [wf].[workflow_node]
--
PRINT (N'Create foreign key [FK_wn_action] on table [wf].[workflow_node]')
GO
ALTER TABLE wf.workflow_node
  ADD CONSTRAINT FK_wn_action FOREIGN KEY (workflow_action_id) REFERENCES wf.workflow_action (id)
GO

--
-- Create foreign key [FK_wn_version] on table [wf].[workflow_node]
--
PRINT (N'Create foreign key [FK_wn_version] on table [wf].[workflow_node]')
GO
ALTER TABLE wf.workflow_node
  ADD CONSTRAINT FK_wn_version FOREIGN KEY (workflow_version_id) REFERENCES wf.workflow_version (id) ON DELETE CASCADE
GO

--
-- Create foreign key [FK_wfv_root] on table [wf].[workflow_version]
--
PRINT (N'Create foreign key [FK_wfv_root] on table [wf].[workflow_version]')
GO
ALTER TABLE wf.workflow_version WITH NOCHECK
  ADD CONSTRAINT FK_wfv_root FOREIGN KEY (root_node_id) REFERENCES wf.workflow_node (id)
GO

--
-- Create table [wf].[workflow_input_template]
--
PRINT (N'Create table [wf].[workflow_input_template]')
GO
CREATE TABLE wf.workflow_input_template (
  workflow_node_id bigint NOT NULL,
  template_json json NOT NULL CONSTRAINT DF_wit_template DEFAULT (N'{}'),
  CONSTRAINT PK__workflow__F9F67F8AB25EB81B PRIMARY KEY CLUSTERED (workflow_node_id)
)
GO

--
-- Create foreign key [FK_wit_node] on table [wf].[workflow_input_template]
--
PRINT (N'Create foreign key [FK_wit_node] on table [wf].[workflow_input_template]')
GO
ALTER TABLE wf.workflow_input_template
  ADD CONSTRAINT FK_wit_node FOREIGN KEY (workflow_node_id) REFERENCES wf.workflow_node (id) ON DELETE CASCADE
GO

--
-- Create table [wf].[workflow_input_binding]
--
PRINT (N'Create table [wf].[workflow_input_binding]')
GO
CREATE TABLE wf.workflow_input_binding (
  id bigint IDENTITY,
  workflow_node_id bigint NOT NULL,
  target_json_path nvarchar(1024) NOT NULL,
  source_expr nvarchar(1024) NOT NULL,
  is_required bit NOT NULL CONSTRAINT DF_wib_required DEFAULT (0),
  PRIMARY KEY CLUSTERED (id)
)
GO

--
-- Create index [IX_wib_node] on table [wf].[workflow_input_binding]
--
PRINT (N'Create index [IX_wib_node] on table [wf].[workflow_input_binding]')
GO
CREATE INDEX IX_wib_node
  ON wf.workflow_input_binding (workflow_node_id)
GO

--
-- Create foreign key [FK_wib_node] on table [wf].[workflow_input_binding]
--
PRINT (N'Create foreign key [FK_wib_node] on table [wf].[workflow_input_binding]')
GO
ALTER TABLE wf.workflow_input_binding
  ADD CONSTRAINT FK_wib_node FOREIGN KEY (workflow_node_id) REFERENCES wf.workflow_node (id) ON DELETE CASCADE
GO

--
-- Create table [wf].[workflow_edge]
--
PRINT (N'Create table [wf].[workflow_edge]')
GO
CREATE TABLE wf.workflow_edge (
  id bigint IDENTITY,
  parent_node_id bigint NOT NULL,
  child_node_id bigint NOT NULL,
  child_order int NOT NULL CONSTRAINT DF_we_child_order DEFAULT (0),
  branch_kind varchar(32) NULL,
  condition_expr nvarchar(max) NULL,
  switch_case_value int NULL,
  is_default bit NOT NULL CONSTRAINT DF_we_default DEFAULT (0),
  PRIMARY KEY CLUSTERED (id),
  CONSTRAINT CK_we_branch_kind CHECK ([branch_kind] IS NULL OR ([branch_kind]=N'BODY' OR [branch_kind]=N'DEFAULT' OR [branch_kind]=N'CASE' OR [branch_kind]=N'ELSE' OR [branch_kind]=N'THEN' OR [branch_kind]=N'PARALLEL' OR [branch_kind]=N'SEQUENCE')),
  CONSTRAINT CK_we_no_self_loop CHECK ([parent_node_id]<>[child_node_id])
)
GO

--
-- Create index [IX_we_child] on table [wf].[workflow_edge]
--
PRINT (N'Create index [IX_we_child] on table [wf].[workflow_edge]')
GO
CREATE INDEX IX_we_child
  ON wf.workflow_edge (child_node_id)
GO

--
-- Create index [IX_we_parent] on table [wf].[workflow_edge]
--
PRINT (N'Create index [IX_we_parent] on table [wf].[workflow_edge]')
GO
CREATE INDEX IX_we_parent
  ON wf.workflow_edge (parent_node_id)
GO

--
-- Create index [UQ_we_parent_child] on table [wf].[workflow_edge]
--
PRINT (N'Create index [UQ_we_parent_child] on table [wf].[workflow_edge]')
GO
CREATE UNIQUE INDEX UQ_we_parent_child
  ON wf.workflow_edge (parent_node_id, child_node_id)
GO

--
-- Create index [UQ_we_parent_child_order] on table [wf].[workflow_edge]
--
PRINT (N'Create index [UQ_we_parent_child_order] on table [wf].[workflow_edge]')
GO
CREATE UNIQUE INDEX UQ_we_parent_child_order
  ON wf.workflow_edge (parent_node_id, child_order)
GO

--
-- Create foreign key [FK_we_child] on table [wf].[workflow_edge]
--
PRINT (N'Create foreign key [FK_we_child] on table [wf].[workflow_edge]')
GO
ALTER TABLE wf.workflow_edge
  ADD CONSTRAINT FK_we_child FOREIGN KEY (child_node_id) REFERENCES wf.workflow_node (id)
GO

--
-- Create foreign key [FK_we_parent] on table [wf].[workflow_edge]
--
PRINT (N'Create foreign key [FK_we_parent] on table [wf].[workflow_edge]')
GO
ALTER TABLE wf.workflow_edge
  ADD CONSTRAINT FK_we_parent FOREIGN KEY (parent_node_id) REFERENCES wf.workflow_node (id)
GO

--
-- Create table [wf].[variable_output_binding]
--
PRINT (N'Create table [wf].[variable_output_binding]')
GO
CREATE TABLE wf.variable_output_binding (
  id bigint IDENTITY,
  workflow_node_id bigint NOT NULL,
  var_name nvarchar(128) NOT NULL,
  source_kind varchar(32) NOT NULL,
  source_json_path nvarchar(1024) NULL,
  PRIMARY KEY CLUSTERED (id),
  CONSTRAINT UQ_vob_node_var UNIQUE (workflow_node_id, var_name),
  CONSTRAINT CK_vob_source CHECK ([source_kind]=N'output_path' OR [source_kind]=N'result_code')
)
GO

--
-- Create index [IX_vob_node] on table [wf].[variable_output_binding]
--
PRINT (N'Create index [IX_vob_node] on table [wf].[variable_output_binding]')
GO
CREATE INDEX IX_vob_node
  ON wf.variable_output_binding (workflow_node_id)
GO

--
-- Create foreign key [FK_vob_node] on table [wf].[variable_output_binding]
--
PRINT (N'Create foreign key [FK_vob_node] on table [wf].[variable_output_binding]')
GO
ALTER TABLE wf.variable_output_binding
  ADD CONSTRAINT FK_vob_node FOREIGN KEY (workflow_node_id) REFERENCES wf.workflow_node (id) ON DELETE CASCADE
GO

--
-- Create table [wf].[node_scope_default]
--
PRINT (N'Create table [wf].[node_scope_default]')
GO
CREATE TABLE wf.node_scope_default (
  id bigint IDENTITY,
  workflow_node_id bigint NOT NULL,
  var_name nvarchar(128) NOT NULL,
  default_expr nvarchar(1024) NOT NULL,
  PRIMARY KEY CLUSTERED (id),
  CONSTRAINT UQ_nsd_node_var UNIQUE (workflow_node_id, var_name)
)
GO

--
-- Create index [IX_nsd_node] on table [wf].[node_scope_default]
--
PRINT (N'Create index [IX_nsd_node] on table [wf].[node_scope_default]')
GO
CREATE INDEX IX_nsd_node
  ON wf.node_scope_default (workflow_node_id)
GO

--
-- Create foreign key [FK_nsd_node] on table [wf].[node_scope_default]
--
PRINT (N'Create foreign key [FK_nsd_node] on table [wf].[node_scope_default]')
GO
ALTER TABLE wf.node_scope_default
  ADD CONSTRAINT FK_nsd_node FOREIGN KEY (workflow_node_id) REFERENCES wf.workflow_node (id) ON DELETE CASCADE
GO

--
-- Create table [wf].[node_execution]
--
PRINT (N'Create table [wf].[node_execution]')
GO
CREATE TABLE wf.node_execution (
  id bigint IDENTITY,
  workflow_instance_id bigint NOT NULL,
  workflow_node_id bigint NOT NULL,
  status varchar(32) NOT NULL CONSTRAINT DF_ne_status DEFAULT (N'PENDING'),
  attempt_no int NOT NULL CONSTRAINT DF_ne_attempt DEFAULT (1),
  parent_node_execution_id bigint NULL,
  iteration_no int NOT NULL CONSTRAINT DF_ne_iter DEFAULT (0),
  input_json json NULL,
  output_json json NULL,
  result_code int NULL,
  engine_error_code int NULL,
  engine_error_message nvarchar(1024) NULL,
  started_at_utc datetime2 NULL,
  ended_at_utc datetime2 NULL,
  available_at_utc datetime2 NULL,
  PRIMARY KEY CLUSTERED (id),
  CONSTRAINT CK_ne_status CHECK ([status]=N'CANCELLED' OR [status]=N'SKIPPED' OR [status]=N'FAILED' OR [status]=N'SUCCEEDED' OR [status]=N'RUNNING' OR [status]=N'READY' OR [status]=N'PENDING')
)
GO

--
-- Create index [IX_ne_instance_node] on table [wf].[node_execution]
--
PRINT (N'Create index [IX_ne_instance_node] on table [wf].[node_execution]')
GO
CREATE INDEX IX_ne_instance_node
  ON wf.node_execution (workflow_instance_id, workflow_node_id, status)
GO

--
-- Create index [IX_ne_instance_status_avail] on table [wf].[node_execution]
--
PRINT (N'Create index [IX_ne_instance_status_avail] on table [wf].[node_execution]')
GO
CREATE INDEX IX_ne_instance_status_avail
  ON wf.node_execution (workflow_instance_id, status, available_at_utc)
  INCLUDE (workflow_node_id)
GO

--
-- Create index [IX_ne_instance_status_ended] on table [wf].[node_execution]
--
PRINT (N'Create index [IX_ne_instance_status_ended] on table [wf].[node_execution]')
GO
CREATE INDEX IX_ne_instance_status_ended
  ON wf.node_execution (workflow_instance_id, status, ended_at_utc DESC)
  INCLUDE (workflow_node_id, result_code, output_json)
GO

--
-- Create index [IX_ne_ready_worker_poll] on table [wf].[node_execution]
--
PRINT (N'Create index [IX_ne_ready_worker_poll] on table [wf].[node_execution]')
GO
CREATE INDEX IX_ne_ready_worker_poll
  ON wf.node_execution (status, available_at_utc)
  INCLUDE (workflow_instance_id, workflow_node_id)
  WHERE ([status] IN ('READY', 'RUNNING'))
GO

--
-- Create foreign key [FK_ne_instance] on table [wf].[node_execution]
--
PRINT (N'Create foreign key [FK_ne_instance] on table [wf].[node_execution]')
GO
ALTER TABLE wf.node_execution
  ADD CONSTRAINT FK_ne_instance FOREIGN KEY (workflow_instance_id) REFERENCES wf.workflow_instance (id) ON DELETE CASCADE
GO

--
-- Create foreign key [FK_ne_node] on table [wf].[node_execution]
--
PRINT (N'Create foreign key [FK_ne_node] on table [wf].[node_execution]')
GO
ALTER TABLE wf.node_execution
  ADD CONSTRAINT FK_ne_node FOREIGN KEY (workflow_node_id) REFERENCES wf.workflow_node (id)
GO

--
-- Create foreign key [FK_ne_parent_exec] on table [wf].[node_execution]
--
PRINT (N'Create foreign key [FK_ne_parent_exec] on table [wf].[node_execution]')
GO
ALTER TABLE wf.node_execution
  ADD CONSTRAINT FK_ne_parent_exec FOREIGN KEY (parent_node_execution_id) REFERENCES wf.node_execution (id)
GO

--
-- Create foreign key [FK_node_execution_instance_cursor_workflow_instance_id] on table [wf].[node_execution]
--
PRINT (N'Create foreign key [FK_node_execution_instance_cursor_workflow_instance_id] on table [wf].[node_execution]')
GO
ALTER TABLE wf.node_execution
  ADD CONSTRAINT FK_node_execution_instance_cursor_workflow_instance_id FOREIGN KEY (workflow_instance_id) REFERENCES wf.instance_cursor (workflow_instance_id)
GO

--
-- Create or alter function [wf].[wf_try_task_result_code]
--
GO
PRINT (N'Create or alter function [wf].[wf_try_task_result_code]')
GO

CREATE OR ALTER FUNCTION wf.wf_try_task_result_code(@workflow_instance_id BIGINT, @node_key NVARCHAR(128))
RETURNS INT
AS
BEGIN
    DECLARE @r INT;
    SELECT TOP (1) @r = ne.result_code
    FROM wf.node_execution AS ne
    INNER JOIN wf.workflow_node AS wn ON wn.id = ne.workflow_node_id
    WHERE ne.workflow_instance_id = @workflow_instance_id
      AND wn.node_key = @node_key
      AND ne.status = N'SUCCEEDED'
    ORDER BY ne.ended_at_utc DESC, ne.id DESC;
    RETURN @r;
END;
GO

--
-- Create or alter procedure [wf].[wf_while_continue]
--
GO
PRINT (N'Create or alter procedure [wf].[wf_while_continue]')
GO

CREATE OR ALTER PROCEDURE wf.wf_while_continue
    @while_execution_id BIGINT
AS
BEGIN
    SET NOCOUNT ON;

    DECLARE @workflow_instance_id BIGINT;
    DECLARE @inst BIGINT;
    DECLARE @ctl BIGINT;
    SELECT @inst = workflow_instance_id, @ctl = workflow_node_id FROM wf.node_execution WHERE id = @while_execution_id;

    DECLARE @wcref NVARCHAR(128);
    SELECT @wcref = condition_ref_node_key FROM wf.workflow_node WHERE id = @ctl;

    DECLARE @wcond INT = wf.wf_try_task_result_code(@workflow_instance_id, @wcref);

    IF @wcond IS NULL OR @wcond = 0
    BEGIN
        UPDATE wf.node_execution SET status = N'SUCCEEDED', ended_at_utc = SYSUTCDATETIME() WHERE id = @while_execution_id;
        EXEC wf.wf_engine_on_composite_complete @node_execution_id = @while_execution_id;
        RETURN;
    END

    DECLARE @iter INT = ISNULL((SELECT MAX(iteration_no) FROM wf.node_execution WHERE parent_node_execution_id = @while_execution_id AND workflow_node_id = (
        SELECT TOP (1) child_node_id FROM wf.workflow_edge WHERE parent_node_id = @ctl AND branch_kind = N'BODY' ORDER BY child_order ASC
    )), 0);

    DECLARE @wbody BIGINT;
    SELECT TOP (1) @wbody = child_node_id FROM wf.workflow_edge WHERE parent_node_id = @ctl AND branch_kind = N'BODY' ORDER BY child_order ASC;

    DECLARE @iter1 INT = @iter + 1;
    EXEC wf.wf_engine_activate
        @workflow_instance_id = @inst,
        @workflow_node_id = @wbody,
        @parent_node_execution_id = @while_execution_id,
        @iteration_no = @iter1,
        @sequence_index = NULL,
        @parallel_index = NULL;
END;
GO

--
-- Create or alter function [wf].[wf_try_task_output_json]
--
GO
PRINT (N'Create or alter function [wf].[wf_try_task_output_json]')
GO

CREATE OR ALTER FUNCTION wf.wf_try_task_output_json(@workflow_instance_id BIGINT, @node_key NVARCHAR(128), @jsonPath NVARCHAR(4000))
RETURNS NVARCHAR(MAX)
AS
BEGIN
    DECLARE @out NVARCHAR(MAX);
    SELECT TOP (1) @out = CAST(ne.output_json AS NVARCHAR(MAX))
    FROM wf.node_execution AS ne
    INNER JOIN wf.workflow_node AS wn ON wn.id = ne.workflow_node_id
    WHERE ne.workflow_instance_id = @workflow_instance_id
      AND wn.node_key = @node_key
      AND ne.status = N'SUCCEEDED'
    ORDER BY ne.ended_at_utc DESC, ne.id DESC;

    IF @out IS NULL RETURN NULL;

    IF @jsonPath IS NULL OR LTRIM(RTRIM(@jsonPath)) = N'' RETURN @out;

    IF LEFT(@jsonPath, 1) <> N'$' SET @jsonPath = N'$.' + @jsonPath;

    DECLARE @jq NVARCHAR(MAX) = JSON_QUERY(@out, @jsonPath);
    IF @jq IS NOT NULL RETURN @jq;

    DECLARE @jv NVARCHAR(MAX) = JSON_VALUE(@out, @jsonPath);
    IF @jv IS NULL RETURN NULL;

    DECLARE @bi BIGINT = TRY_CONVERT(BIGINT, @jv);
    IF @bi IS NOT NULL AND CAST(@bi AS NVARCHAR(50)) = LTRIM(RTRIM(@jv))
        RETURN CAST(@bi AS NVARCHAR(50));

    RETURN wf.wf_json_fragment_from_string(@jv);
END;
GO

--
-- Create or alter procedure [wf].[wf_sequence_continue]
--
GO
PRINT (N'Create or alter procedure [wf].[wf_sequence_continue]')
GO

CREATE OR ALTER PROCEDURE wf.wf_sequence_continue
    @sequence_execution_id BIGINT
AS
BEGIN
    SET NOCOUNT ON;

    DECLARE @inst BIGINT;
    DECLARE @seq_node BIGINT;
    SELECT @inst = workflow_instance_id, @seq_node = workflow_node_id FROM wf.node_execution WHERE id = @sequence_execution_id;

    DECLARE @last_child_ne BIGINT;
    SELECT TOP (1) @last_child_ne = ne.id
    FROM wf.node_execution AS ne
    WHERE ne.parent_node_execution_id = @sequence_execution_id AND ne.status IN (N'SUCCEEDED', N'FAILED', N'SKIPPED')
    ORDER BY ne.ended_at_utc DESC, ne.id DESC;

    DECLARE @last_status VARCHAR(32);
    DECLARE @last_child_wn BIGINT;
    SELECT @last_child_wn = workflow_node_id, @last_status = status FROM wf.node_execution WHERE id = @last_child_ne;

    IF @last_status = N'FAILED'
    BEGIN
        UPDATE wf.node_execution SET status = N'FAILED', ended_at_utc = SYSUTCDATETIME() WHERE id = @sequence_execution_id;
        UPDATE wf.workflow_instance SET status = N'FAILED', completed_at_utc = SYSUTCDATETIME() WHERE id = @inst;
        RETURN;
    END

    DECLARE @next_order INT;
    SELECT @next_order = e.child_order + 1
    FROM wf.workflow_edge AS e
    WHERE e.parent_node_id = @seq_node AND e.child_node_id = @last_child_wn;

    DECLARE @next_child BIGINT;
    SELECT TOP (1) @next_child = child_node_id
    FROM wf.workflow_edge
    WHERE parent_node_id = @seq_node AND child_order = @next_order;

    IF @next_child IS NULL
    BEGIN
        UPDATE wf.node_execution SET status = N'SUCCEEDED', ended_at_utc = SYSUTCDATETIME() WHERE id = @sequence_execution_id;
        EXEC wf.wf_engine_on_composite_complete @node_execution_id = @sequence_execution_id;
        RETURN;
    END;

    DECLARE @iter INT = (SELECT iteration_no FROM wf.node_execution WHERE id = @sequence_execution_id);
    EXEC wf.wf_engine_activate
        @workflow_instance_id = @inst,
        @workflow_node_id = @next_child,
        @parent_node_execution_id = @sequence_execution_id,
        @iteration_no = @iter,
        @sequence_index = @next_order,
        @parallel_index = NULL;
END;
GO

--
-- Create or alter procedure [wf].[wf_parallel_continue]
--
GO
PRINT (N'Create or alter procedure [wf].[wf_parallel_continue]')
GO

CREATE OR ALTER PROCEDURE wf.wf_parallel_continue
    @parallel_execution_id BIGINT
AS
BEGIN
    SET NOCOUNT ON;

    DECLARE @inst BIGINT;
    DECLARE @pnode BIGINT;
    SELECT @inst = workflow_instance_id, @pnode = workflow_node_id FROM wf.node_execution WHERE id = @parallel_execution_id;

    DECLARE @total INT = (SELECT COUNT(*) FROM wf.workflow_edge WHERE parent_node_id = @pnode);

    DECLARE @finished INT = (
        SELECT COUNT(*)
        FROM wf.node_execution AS ne
        WHERE ne.parent_node_execution_id = @parallel_execution_id
          AND ne.status IN (N'SUCCEEDED', N'FAILED', N'SKIPPED', N'CANCELLED')
    );

    IF @finished < @total RETURN;

    IF EXISTS (
        SELECT 1 FROM wf.node_execution
        WHERE parent_node_execution_id = @parallel_execution_id AND status = N'FAILED'
    )
    BEGIN
        UPDATE wf.node_execution SET status = N'FAILED', ended_at_utc = SYSUTCDATETIME() WHERE id = @parallel_execution_id;
        UPDATE wf.workflow_instance SET status = N'FAILED', completed_at_utc = SYSUTCDATETIME() WHERE id = @inst;
        RETURN;
    END

    UPDATE wf.node_execution SET status = N'SUCCEEDED', ended_at_utc = SYSUTCDATETIME() WHERE id = @parallel_execution_id;
    EXEC wf.wf_engine_on_composite_complete @node_execution_id = @parallel_execution_id;
END;
GO

--
-- Create or alter procedure [wf].[wf_engine_on_composite_complete]
--
GO
PRINT (N'Create or alter procedure [wf].[wf_engine_on_composite_complete]')
GO

CREATE OR ALTER PROCEDURE wf.wf_engine_on_composite_complete
    @node_execution_id BIGINT
AS
BEGIN
    SET NOCOUNT ON;

    DECLARE @parent BIGINT;
    DECLARE @inst BIGINT;
    SELECT @parent = parent_node_execution_id, @inst = workflow_instance_id FROM wf.node_execution WHERE id = @node_execution_id;

    IF @parent IS NULL
    BEGIN
        UPDATE wf.workflow_instance
        SET status = N'COMPLETED', completed_at_utc = SYSUTCDATETIME()
        WHERE id = @inst AND status = N'RUNNING';
        RETURN;
    END

    EXEC wf.wf_engine_continue_parent @parent_node_execution_id = @parent;
END;
GO

--
-- Create or alter procedure [wf].[wf_engine_continue_parent]
--
GO
PRINT (N'Create or alter procedure [wf].[wf_engine_continue_parent]')
GO

CREATE OR ALTER PROCEDURE wf.wf_engine_continue_parent
    @parent_node_execution_id BIGINT
AS
BEGIN
    SET NOCOUNT ON;

    DECLARE @pnode BIGINT;
    DECLARE @ptype VARCHAR(32);
    SELECT @pnode = ne.workflow_node_id, @ptype = wn.node_type
    FROM wf.node_execution AS ne
    INNER JOIN wf.workflow_node AS wn ON wn.id = ne.workflow_node_id
    WHERE ne.id = @parent_node_execution_id;

    IF @ptype = N'SEQUENCE'
        EXEC wf.wf_sequence_continue @sequence_execution_id = @parent_node_execution_id;

    ELSE IF @ptype = N'PARALLEL'
        EXEC wf.wf_parallel_continue @parallel_execution_id = @parent_node_execution_id;

    ELSE IF @ptype IN (N'IF', N'SWITCH')
    BEGIN
        UPDATE wf.node_execution SET status = N'SUCCEEDED', ended_at_utc = SYSUTCDATETIME() WHERE id = @parent_node_execution_id;
        EXEC wf.wf_engine_on_composite_complete @node_execution_id = @parent_node_execution_id;
    END

    ELSE IF @ptype = N'REPEAT'
        EXEC wf.wf_repeat_continue @repeat_execution_id = @parent_node_execution_id;

    ELSE IF @ptype = N'WHILE'
        EXEC wf.wf_while_continue @while_execution_id = @parent_node_execution_id;

    /* FOREACH arrives with wf_sql_foreach_support.sql; dispatch through the router so
       re-running this base script never drops FOREACH continuation. */
    ELSE IF @ptype = N'FOREACH'
        EXEC wf.wf_foreach_route_continue @foreach_execution_id = @parent_node_execution_id;
END;
GO

--
-- Create table [wf].[loop_state]
--
PRINT (N'Create table [wf].[loop_state]')
GO
CREATE TABLE wf.loop_state (
  id bigint IDENTITY,
  workflow_instance_id bigint NOT NULL,
  control_node_id bigint NOT NULL,
  scope_node_execution_id bigint NOT NULL,
  current_iteration int NOT NULL CONSTRAINT DF_ls_cur DEFAULT (0),
  repeat_target_count int NULL,
  PRIMARY KEY CLUSTERED (id),
  CONSTRAINT UQ_ls_scope UNIQUE (scope_node_execution_id)
)
GO

--
-- Create index [IX_ls_instance] on table [wf].[loop_state]
--
PRINT (N'Create index [IX_ls_instance] on table [wf].[loop_state]')
GO
CREATE INDEX IX_ls_instance
  ON wf.loop_state (workflow_instance_id, control_node_id)
GO

--
-- Create foreign key [FK_ls_control] on table [wf].[loop_state]
--
PRINT (N'Create foreign key [FK_ls_control] on table [wf].[loop_state]')
GO
ALTER TABLE wf.loop_state
  ADD CONSTRAINT FK_ls_control FOREIGN KEY (control_node_id) REFERENCES wf.workflow_node (id)
GO

--
-- Create foreign key [FK_ls_instance] on table [wf].[loop_state]
--
PRINT (N'Create foreign key [FK_ls_instance] on table [wf].[loop_state]')
GO
ALTER TABLE wf.loop_state
  ADD CONSTRAINT FK_ls_instance FOREIGN KEY (workflow_instance_id) REFERENCES wf.workflow_instance (id)
GO

--
-- Create foreign key [FK_ls_scope] on table [wf].[loop_state]
--
PRINT (N'Create foreign key [FK_ls_scope] on table [wf].[loop_state]')
GO
ALTER TABLE wf.loop_state
  ADD CONSTRAINT FK_ls_scope FOREIGN KEY (scope_node_execution_id) REFERENCES wf.node_execution (id)
GO

--
-- Create or alter procedure [wf].[wf_repeat_continue]
--
GO
PRINT (N'Create or alter procedure [wf].[wf_repeat_continue]')
GO

CREATE OR ALTER PROCEDURE wf.wf_repeat_continue
    @repeat_execution_id BIGINT
AS
BEGIN
    SET NOCOUNT ON;

    DECLARE @inst BIGINT;
    DECLARE @ctl BIGINT;
    DECLARE @ls BIGINT;
    DECLARE @cur INT;
    DECLARE @max INT;

    SELECT @inst = workflow_instance_id, @ctl = workflow_node_id FROM wf.node_execution WHERE id = @repeat_execution_id;
    SELECT TOP (1)
        @ls = id,
        @cur = current_iteration,
        @max = repeat_target_count
    FROM wf.loop_state
    WHERE scope_node_execution_id = @repeat_execution_id;

    IF @ls IS NULL
    BEGIN
        UPDATE wf.node_execution SET status = N'FAILED', engine_error_code = 10007, ended_at_utc = SYSUTCDATETIME() WHERE id = @repeat_execution_id;
        UPDATE wf.workflow_instance SET status = N'FAILED', completed_at_utc = SYSUTCDATETIME() WHERE id = @inst;
        RETURN;
    END

    SET @cur += 1;
    UPDATE wf.loop_state SET current_iteration = @cur WHERE id = @ls;

    IF @cur >= @max
    BEGIN
        UPDATE wf.node_execution SET status = N'SUCCEEDED', ended_at_utc = SYSUTCDATETIME() WHERE id = @repeat_execution_id;
        EXEC wf.wf_engine_on_composite_complete @node_execution_id = @repeat_execution_id;
        RETURN;
    END

    DECLARE @body BIGINT;
    SELECT TOP (1) @body = child_node_id FROM wf.workflow_edge WHERE parent_node_id = @ctl AND branch_kind = N'BODY' ORDER BY child_order ASC;

    DECLARE @cur1 INT = @cur + 1;
    EXEC wf.wf_engine_activate
        @workflow_instance_id = @inst,
        @workflow_node_id = @body,
        @parent_node_execution_id = @repeat_execution_id,
        @iteration_no = @cur1,
        @sequence_index = NULL,
        @parallel_index = NULL;
END;
GO

--
-- Create table [wf].[execution_context]
--
PRINT (N'Create table [wf].[execution_context]')
GO
CREATE TABLE wf.execution_context (
  id bigint IDENTITY,
  node_execution_id bigint NOT NULL,
  context_key nvarchar(256) NOT NULL,
  context_value_json json NULL,
  CONSTRAINT PK__executio__3213E83FAE67548B PRIMARY KEY CLUSTERED (id),
  CONSTRAINT UQ_ec_ne_key UNIQUE (node_execution_id, context_key)
)
GO

--
-- Create index [IX_ec_ne] on table [wf].[execution_context]
--
PRINT (N'Create index [IX_ec_ne] on table [wf].[execution_context]')
GO
CREATE INDEX IX_ec_ne
  ON wf.execution_context (node_execution_id)
GO

--
-- Create foreign key [FK_ec_ne] on table [wf].[execution_context]
--
PRINT (N'Create foreign key [FK_ec_ne] on table [wf].[execution_context]')
GO
ALTER TABLE wf.execution_context
  ADD CONSTRAINT FK_ec_ne FOREIGN KEY (node_execution_id) REFERENCES wf.node_execution (id) ON DELETE CASCADE
GO

--
-- Create or alter procedure [wf].[wf_seed_execution_context]
--
GO
PRINT (N'Create or alter procedure [wf].[wf_seed_execution_context]')
GO

CREATE OR ALTER PROCEDURE wf.wf_seed_execution_context
    @node_execution_id BIGINT,
    @workflow_instance_id BIGINT,
    @workflow_node_id BIGINT,
    @parent_node_execution_id BIGINT NULL,
    @iteration_no INT,
    @sequence_index INT NULL,
    @parallel_index INT NULL
AS
BEGIN
    SET NOCOUNT ON;

    DELETE FROM wf.execution_context WHERE node_execution_id = @node_execution_id;

    INSERT INTO wf.execution_context (node_execution_id, context_key, context_value_json)
    VALUES (@node_execution_id, N'ctx.iterationNo', CAST(@iteration_no AS NVARCHAR(32)));

    IF @sequence_index IS NOT NULL
        INSERT INTO wf.execution_context (node_execution_id, context_key, context_value_json)
        VALUES (@node_execution_id, N'ctx.sequenceIndex', CAST(@sequence_index AS NVARCHAR(32)));

    IF @parallel_index IS NOT NULL
        INSERT INTO wf.execution_context (node_execution_id, context_key, context_value_json)
        VALUES (@node_execution_id, N'ctx.parallelIndex', CAST(@parallel_index AS NVARCHAR(32)));

    IF @parent_node_execution_id IS NOT NULL
    BEGIN
        DECLARE @prc INT;
        SELECT @prc = result_code FROM wf.node_execution WHERE id = @parent_node_execution_id;
        INSERT INTO wf.execution_context (node_execution_id, context_key, context_value_json)
        VALUES (@node_execution_id, N'ctx.parent.resultCode', CAST(@prc AS NVARCHAR(32)));
    END
END;
GO

--
-- Create or alter procedure [wf].[wf_resolve_token]
--
GO
PRINT (N'Create or alter procedure [wf].[wf_resolve_token]')
GO

CREATE OR ALTER PROCEDURE wf.wf_resolve_token
    @token NVARCHAR(1024),
    @node_execution_id BIGINT,
    @workflow_instance_id BIGINT,
    @out_fragment NVARCHAR(MAX) OUTPUT,
    @failed BIT OUTPUT,
    @fail_code INT OUTPUT,
    @fail_msg NVARCHAR(1024) OUTPUT
AS
BEGIN
    SET NOCOUNT ON;
    SET @failed = 0;
    SET @fail_code = NULL;
    SET @fail_msg = NULL;

    IF CHARINDEX(N' ', @token) > 0 OR CHARINDEX(N'+', @token) > 0 OR CHARINDEX(N'(', @token) > 0
    BEGIN
        SET @failed = 1;
        SET @fail_code = 10002;
        SET @fail_msg = N'Unsupported placeholder expression.';
        RETURN;
    END

    IF LEFT(@token, 9) = N'ctx.task.'
    BEGIN
        DECLARE @rest NVARCHAR(1024) = SUBSTRING(@token, 10, 4000);
        DECLARE @dot INT = CHARINDEX(N'.', @rest);
        DECLARE @nk NVARCHAR(128);
        DECLARE @tail NVARCHAR(1024);

        IF @dot = 0
        BEGIN
            SET @failed = 1;
            SET @fail_code = 10002;
            SET @fail_msg = N'Invalid ctx.task reference.';
            RETURN;
        END

        SET @nk = LEFT(@rest, @dot - 1);
        SET @tail = SUBSTRING(@rest, @dot + 1, 4000);

        IF @tail = N'resultCode'
        BEGIN
            DECLARE @rc INT = wf.wf_try_task_result_code(@workflow_instance_id, @nk);
            SET @out_fragment = CAST(@rc AS NVARCHAR(32));
            RETURN;
        END

        IF LEFT(@tail, 7) = N'output.'
        BEGIN
            DECLARE @jp NVARCHAR(4000) = SUBSTRING(@tail, 8, 4000);
            DECLARE @jq NVARCHAR(MAX) = wf.wf_try_task_output_json(@workflow_instance_id, @nk, @jp);
            SET @out_fragment = @jq;
            RETURN;
        END

        SET @failed = 1;
        SET @fail_code = 10002;
        SET @fail_msg = N'Unsupported ctx.task tail.';
        RETURN;
    END

    IF LEFT(@token, 4) = N'ctx.'
    BEGIN
        DECLARE @v NVARCHAR(MAX);
        SELECT @v = CAST(context_value_json AS NVARCHAR(MAX))
        FROM wf.execution_context
        WHERE node_execution_id = @node_execution_id AND context_key = @token;

        IF @v IS NULL
        BEGIN
            SET @failed = 1;
            SET @fail_code = 10001;
            SET @fail_msg = N'Missing context value for ' + @token;
            RETURN;
        END

        SET @out_fragment = @v;
        RETURN;
    END

    SET @failed = 1;
    SET @fail_code = 10002;
    SET @fail_msg = N'Unsupported token.';
END;
GO

--
-- Create or alter procedure [wf].[wf_resolve_placeholders]
--
GO
PRINT (N'Create or alter procedure [wf].[wf_resolve_placeholders]')
GO

CREATE OR ALTER PROCEDURE wf.wf_resolve_placeholders
    @text NVARCHAR(MAX),
    @node_execution_id BIGINT,
    @workflow_instance_id BIGINT,
    @resolved NVARCHAR(MAX) OUTPUT,
    @failed BIT OUTPUT,
    @fail_code INT OUTPUT,
    @fail_msg NVARCHAR(1024) OUTPUT
AS
BEGIN
    SET NOCOUNT ON;
    SET @resolved = @text;
    SET @failed = 0;

    DECLARE @start INT;
    DECLARE @end INT;
    DECLARE @token NVARCHAR(1024);
    DECLARE @frag NVARCHAR(MAX);
    DECLARE @tf BIT;
    DECLARE @fc INT;
    DECLARE @fm NVARCHAR(1024);

    WHILE CHARINDEX(N'${', @resolved) > 0
    BEGIN
        SET @start = CHARINDEX(N'${', @resolved);
        SET @end = CHARINDEX(N'}', @resolved, @start + 2);
        IF @end = 0 BREAK;

        SET @token = SUBSTRING(@resolved, @start + 2, @end - (@start + 2));

        EXEC wf.wf_resolve_token
            @token = @token,
            @node_execution_id = @node_execution_id,
            @workflow_instance_id = @workflow_instance_id,
            @out_fragment = @frag OUTPUT,
            @failed = @tf OUTPUT,
            @fail_code = @fc OUTPUT,
            @fail_msg = @fm OUTPUT;

        IF @tf = 1
        BEGIN
            SET @failed = 1;
            SET @fail_code = @fc;
            SET @fail_msg = @fm;
            RETURN;
        END

        SET @resolved = STUFF(@resolved, @start, @end - @start + 1, ISNULL(@frag, N'null'));
    END
END;
GO

--
-- Create or alter procedure [wf].[wf_build_input_json_for_action]
--
GO
PRINT (N'Create or alter procedure [wf].[wf_build_input_json_for_action]')
GO

CREATE OR ALTER PROCEDURE wf.wf_build_input_json_for_action
    @node_execution_id BIGINT,
    @workflow_instance_id BIGINT,
    @workflow_node_id BIGINT,
    @final_json NVARCHAR(MAX) OUTPUT,
    @failed BIT OUTPUT,
    @fail_code INT OUTPUT,
    @fail_msg NVARCHAR(1024) OUTPUT
AS
BEGIN
    SET NOCOUNT ON;
    SET @failed = 0;

    DECLARE @template NVARCHAR(MAX) = N'{}';
    IF EXISTS (SELECT 1 FROM wf.workflow_input_template WHERE workflow_node_id = @workflow_node_id)
        SELECT @template = CAST(template_json AS NVARCHAR(MAX)) FROM wf.workflow_input_template WHERE workflow_node_id = @workflow_node_id;

    DECLARE @cur NVARCHAR(MAX);
    DECLARE @tf BIT;
    DECLARE @fc INT;
    DECLARE @fm NVARCHAR(1024);

    EXEC wf.wf_resolve_placeholders
        @text = @template,
        @node_execution_id = @node_execution_id,
        @workflow_instance_id = @workflow_instance_id,
        @resolved = @cur OUTPUT,
        @failed = @tf OUTPUT,
        @fail_code = @fc OUTPUT,
        @fail_msg = @fm OUTPUT;

    IF @tf = 1
    BEGIN
        SET @failed = 1;
        SET @fail_code = @fc;
        SET @fail_msg = @fm;
        RETURN;
    END

    IF ISJSON(@cur) = 0
    BEGIN
        SET @failed = 1;
        SET @fail_code = 10008;
        SET @fail_msg = N'Template JSON is invalid after placeholder resolution.';
        RETURN;
    END

    DECLARE @bid BIGINT;
    DECLARE @path NVARCHAR(1024);
    DECLARE @expr NVARCHAR(1024);
    DECLARE @req BIT;

    DECLARE c CURSOR LOCAL FAST_FORWARD FOR
        SELECT id, target_json_path, source_expr, is_required
        FROM wf.workflow_input_binding
        WHERE workflow_node_id = @workflow_node_id
        ORDER BY id;

    OPEN c;
    FETCH NEXT FROM c INTO @bid, @path, @expr, @req;

    WHILE @@FETCH_STATUS = 0
    BEGIN
        DECLARE @frag NVARCHAR(MAX);

        EXEC wf.wf_resolve_placeholders
            @text = @expr,
            @node_execution_id = @node_execution_id,
            @workflow_instance_id = @workflow_instance_id,
            @resolved = @frag OUTPUT,
            @failed = @tf OUTPUT,
            @fail_code = @fc OUTPUT,
            @fail_msg = @fm OUTPUT;

        IF @tf = 1 AND @req = 1
        BEGIN
            SET @failed = 1;
            SET @fail_code = @fc;
            SET @fail_msg = @fm;
            CLOSE c;
            DEALLOCATE c;
            RETURN;
        END

        IF @tf = 1 AND (@req = 0 OR @req IS NULL)
            SET @frag = N'null';

        IF LEFT(@path, 1) <> N'$' SET @path = N'$.' + @path;

        IF ISNULL(@frag, N'') = N'' SET @frag = N'null';

        IF ISJSON(@frag) = 1
            SET @cur = JSON_MODIFY(@cur, @path, JSON_QUERY(@frag));
        ELSE
            SET @cur = JSON_MODIFY(@cur, @path, JSON_QUERY(wf.wf_json_fragment_from_string(@frag)));

        FETCH NEXT FROM c INTO @bid, @path, @expr, @req;
    END

    CLOSE c;
    DEALLOCATE c;

    SET @final_json = @cur;
END;
GO

--
-- Create or alter procedure [wf].[wf_engine_activate]
--
GO
PRINT (N'Create or alter procedure [wf].[wf_engine_activate]')
GO

CREATE OR ALTER PROCEDURE wf.wf_engine_activate
    @workflow_instance_id BIGINT,
    @workflow_node_id BIGINT,
    @parent_node_execution_id BIGINT NULL,
    @iteration_no INT = 0,
    @sequence_index INT NULL,
    @parallel_index INT NULL
AS
BEGIN
    SET NOCOUNT ON;
    SET XACT_ABORT ON;

    DECLARE @node_type VARCHAR(32);
    DECLARE @version_id BIGINT;
    DECLARE @node_key NVARCHAR(128);

    SELECT @node_type = node_type, @version_id = workflow_version_id, @node_key = node_key
    FROM wf.workflow_node
    WHERE id = @workflow_node_id;

    IF @node_type IS NULL RETURN;

    IF @node_type = N'ACTION'
    BEGIN
        DECLARE @ne_id BIGINT;
        INSERT INTO wf.node_execution (
            workflow_instance_id, workflow_node_id, status, attempt_no,
            parent_node_execution_id, iteration_no, available_at_utc
        )
        VALUES (
            @workflow_instance_id, @workflow_node_id, N'READY', 1,
            @parent_node_execution_id, @iteration_no, SYSUTCDATETIME()
        );
        SET @ne_id = SCOPE_IDENTITY();

        EXEC wf.wf_seed_execution_context
            @node_execution_id = @ne_id,
            @workflow_instance_id = @workflow_instance_id,
            @workflow_node_id = @workflow_node_id,
            @parent_node_execution_id = @parent_node_execution_id,
            @iteration_no = @iteration_no,
            @sequence_index = @sequence_index,
            @parallel_index = @parallel_index;

        DECLARE @fj NVARCHAR(MAX);
        DECLARE @failed BIT;
        DECLARE @fc INT;
        DECLARE @fm NVARCHAR(1024);

        EXEC wf.wf_build_input_json_for_action
            @node_execution_id = @ne_id,
            @workflow_instance_id = @workflow_instance_id,
            @workflow_node_id = @workflow_node_id,
            @final_json = @fj OUTPUT,
            @failed = @failed OUTPUT,
            @fail_code = @fc OUTPUT,
            @fail_msg = @fm OUTPUT;

        IF @failed = 1
        BEGIN
            UPDATE wf.node_execution
            SET status = N'FAILED',
                engine_error_code = @fc,
                engine_error_message = @fm,
                ended_at_utc = SYSUTCDATETIME()
            WHERE id = @ne_id;

            UPDATE wf.workflow_instance
            SET status = N'FAILED', completed_at_utc = SYSUTCDATETIME()
            WHERE id = @workflow_instance_id;

            RETURN;
        END

        UPDATE wf.node_execution SET input_json = CAST(@fj AS json) WHERE id = @ne_id;
        RETURN;
    END

    /* Composite: create RUNNING execution row */
    DECLARE @pex BIGINT;
    INSERT INTO wf.node_execution (
        workflow_instance_id, workflow_node_id, status, attempt_no,
        parent_node_execution_id, iteration_no, started_at_utc, available_at_utc
    )
    VALUES (
        @workflow_instance_id, @workflow_node_id, N'RUNNING', 1,
        @parent_node_execution_id, @iteration_no, SYSUTCDATETIME(), SYSUTCDATETIME()
    );
    SET @pex = SCOPE_IDENTITY();

    IF @node_type = N'SEQUENCE'
    BEGIN
        DECLARE @child1 BIGINT;
        SELECT TOP (1) @child1 = child_node_id
        FROM wf.workflow_edge
        WHERE parent_node_id = @workflow_node_id
        ORDER BY child_order ASC;

        IF @child1 IS NULL
        BEGIN
            UPDATE wf.node_execution SET status = N'SUCCEEDED', ended_at_utc = SYSUTCDATETIME() WHERE id = @pex;
            EXEC wf.wf_engine_on_composite_complete @node_execution_id = @pex;
            RETURN;
        END

        DECLARE @ord INT = 0;
        SELECT TOP (1) @ord = child_order FROM wf.workflow_edge WHERE parent_node_id = @workflow_node_id ORDER BY child_order ASC;

        EXEC wf.wf_engine_activate
            @workflow_instance_id = @workflow_instance_id,
            @workflow_node_id = @child1,
            @parent_node_execution_id = @pex,
            @iteration_no = @iteration_no,
            @sequence_index = @ord,
            @parallel_index = NULL;
        RETURN;
    END

    IF @node_type = N'PARALLEL'
    BEGIN
        DECLARE @cid BIGINT;
        DECLARE @pidx INT = 0;
        DECLARE pc CURSOR LOCAL FAST_FORWARD FOR
            SELECT child_node_id
            FROM wf.workflow_edge
            WHERE parent_node_id = @workflow_node_id
            ORDER BY child_order ASC;

        OPEN pc;
        FETCH NEXT FROM pc INTO @cid;

        WHILE @@FETCH_STATUS = 0
        BEGIN
            EXEC wf.wf_engine_activate
                @workflow_instance_id = @workflow_instance_id,
                @workflow_node_id = @cid,
                @parent_node_execution_id = @pex,
                @iteration_no = @iteration_no,
                @sequence_index = NULL,
                @parallel_index = @pidx;

            SET @pidx += 1;
            FETCH NEXT FROM pc INTO @cid;
        END

        CLOSE pc;
        DEALLOCATE pc;
        RETURN;
    END

    IF @node_type = N'IF'
    BEGIN
        DECLARE @cref NVARCHAR(128);
        SELECT @cref = condition_ref_node_key FROM wf.workflow_node WHERE id = @workflow_node_id;

        DECLARE @cond INT = wf.wf_try_task_result_code(@workflow_instance_id, @cref);
        DECLARE @pick VARCHAR(32) = CASE WHEN @cond IS NOT NULL AND @cond <> 0 THEN N'THEN' ELSE N'ELSE' END;

        DECLARE @ifchild BIGINT;
        SELECT TOP (1) @ifchild = child_node_id
        FROM wf.workflow_edge
        WHERE parent_node_id = @workflow_node_id AND branch_kind = @pick
        ORDER BY child_order ASC;

        IF @ifchild IS NULL
        BEGIN
            UPDATE wf.node_execution SET status = N'FAILED', engine_error_code = 10003, engine_error_message = N'Missing IF branch.', ended_at_utc = SYSUTCDATETIME() WHERE id = @pex;
            UPDATE wf.workflow_instance SET status = N'FAILED', completed_at_utc = SYSUTCDATETIME() WHERE id = @workflow_instance_id;
            RETURN;
        END

        EXEC wf.wf_engine_activate
            @workflow_instance_id = @workflow_instance_id,
            @workflow_node_id = @ifchild,
            @parent_node_execution_id = @pex,
            @iteration_no = @iteration_no,
            @sequence_index = NULL,
            @parallel_index = NULL;
        RETURN;
    END

    IF @node_type = N'SWITCH'
    BEGIN
        DECLARE @sref NVARCHAR(128);
        SELECT @sref = switch_ref_node_key FROM wf.workflow_node WHERE id = @workflow_node_id;

        DECLARE @sv INT = wf.wf_try_task_result_code(@workflow_instance_id, @sref);

        DECLARE @swchild BIGINT;
        SELECT TOP (1) @swchild = child_node_id
        FROM wf.workflow_edge
        WHERE parent_node_id = @workflow_node_id AND branch_kind = N'CASE' AND switch_case_value = @sv
        ORDER BY child_order ASC;

        IF @swchild IS NULL
            SELECT TOP (1) @swchild = child_node_id
            FROM wf.workflow_edge
            WHERE parent_node_id = @workflow_node_id AND branch_kind = N'DEFAULT'
            ORDER BY child_order ASC;

        IF @swchild IS NULL
        BEGIN
            UPDATE wf.node_execution SET status = N'FAILED', engine_error_code = 10004, engine_error_message = N'Missing SWITCH case.', ended_at_utc = SYSUTCDATETIME() WHERE id = @pex;
            UPDATE wf.workflow_instance SET status = N'FAILED', completed_at_utc = SYSUTCDATETIME() WHERE id = @workflow_instance_id;
            RETURN;
        END

        EXEC wf.wf_engine_activate
            @workflow_instance_id = @workflow_instance_id,
            @workflow_node_id = @swchild,
            @parent_node_execution_id = @pex,
            @iteration_no = @iteration_no,
            @sequence_index = NULL,
            @parallel_index = NULL;
        RETURN;
    END

    IF @node_type = N'REPEAT'
    BEGIN
        DECLARE @rcount INT;
        SELECT @rcount = repeat_count FROM wf.workflow_node WHERE id = @workflow_node_id;

        IF @rcount IS NULL OR @rcount < 1 SET @rcount = 1;

        INSERT INTO wf.loop_state (workflow_instance_id, control_node_id, scope_node_execution_id, current_iteration, repeat_target_count)
        VALUES (@workflow_instance_id, @workflow_node_id, @pex, 0, @rcount);

        DECLARE @body BIGINT;
        SELECT TOP (1) @body = child_node_id FROM wf.workflow_edge WHERE parent_node_id = @workflow_node_id AND branch_kind = N'BODY' ORDER BY child_order ASC;

        IF @body IS NULL
        BEGIN
            UPDATE wf.node_execution SET status = N'FAILED', engine_error_code = 10005, ended_at_utc = SYSUTCDATETIME() WHERE id = @pex;
            UPDATE wf.workflow_instance SET status = N'FAILED', completed_at_utc = SYSUTCDATETIME() WHERE id = @workflow_instance_id;
            RETURN;
        END

        EXEC wf.wf_engine_activate
            @workflow_instance_id = @workflow_instance_id,
            @workflow_node_id = @body,
            @parent_node_execution_id = @pex,
            @iteration_no = 1,
            @sequence_index = NULL,
            @parallel_index = NULL;
        RETURN;
    END

    IF @node_type = N'WHILE'
    BEGIN
        DECLARE @wbody BIGINT;
        SELECT TOP (1) @wbody = child_node_id FROM wf.workflow_edge WHERE parent_node_id = @workflow_node_id AND branch_kind = N'BODY' ORDER BY child_order ASC;

        IF @wbody IS NULL
        BEGIN
            UPDATE wf.node_execution SET status = N'FAILED', engine_error_code = 10006, ended_at_utc = SYSUTCDATETIME() WHERE id = @pex;
            UPDATE wf.workflow_instance SET status = N'FAILED', completed_at_utc = SYSUTCDATETIME() WHERE id = @workflow_instance_id;
            RETURN;
        END

        DECLARE @wcref NVARCHAR(128);
        SELECT @wcref = condition_ref_node_key FROM wf.workflow_node WHERE id = @workflow_node_id;

        DECLARE @wcond INT = wf.wf_try_task_result_code(@workflow_instance_id, @wcref);

        IF @wcond IS NULL OR @wcond = 0
        BEGIN
            UPDATE wf.node_execution SET status = N'SUCCEEDED', ended_at_utc = SYSUTCDATETIME() WHERE id = @pex;
            EXEC wf.wf_engine_on_composite_complete @node_execution_id = @pex;
            RETURN;
        END

        EXEC wf.wf_engine_activate
            @workflow_instance_id = @workflow_instance_id,
            @workflow_node_id = @wbody,
            @parent_node_execution_id = @pex,
            @iteration_no = 1,
            @sequence_index = NULL,
            @parallel_index = NULL;
        RETURN;
    END
END;
GO

--
-- Create or alter procedure [wf].[sp_start_workflow_instance]
--
GO
PRINT (N'Create or alter procedure [wf].[sp_start_workflow_instance]')
GO

CREATE OR ALTER PROCEDURE wf.sp_start_workflow_instance
    @workflow_instance_id BIGINT
AS
BEGIN
    SET NOCOUNT ON;
    SET XACT_ABORT ON;

    DECLARE @vid BIGINT;
    DECLARE @root BIGINT;

    SELECT @vid = workflow_version_id FROM wf.workflow_instance WHERE id = @workflow_instance_id;
    SELECT @root = root_node_id FROM wf.workflow_version WHERE id = @vid;

    IF @root IS NULL
        THROW 50001, N'Workflow version has no root_node_id.', 1;

    UPDATE wf.workflow_instance
    SET 
      status = N'RUNNING', 
      started_at_utc = SYSUTCDATETIME()
    WHERE id = @workflow_instance_id;

    EXEC wf.wf_engine_activate
        @workflow_instance_id = @workflow_instance_id,
        @workflow_node_id = @root,
        @parent_node_execution_id = NULL,
        @iteration_no = 0,
        @sequence_index = NULL,
        @parallel_index = NULL;
END;
GO

--
-- Create table [wf].[cluster]
--
PRINT (N'Create table [wf].[cluster]')
GO
CREATE TABLE wf.cluster (
  id bigint IDENTITY,
  cluster_key nvarchar(128) NOT NULL,
  name nvarchar(256) NOT NULL,
  provider nvarchar(64) NULL,
  shared_storage_uri nvarchar(1024) NULL,
  worker_mount_path nvarchar(256) NOT NULL CONSTRAINT DF_cluster_mount DEFAULT (N'/work'),
  status varchar(32) NOT NULL CONSTRAINT DF_cluster_status DEFAULT (N'ACTIVE'),
  created_at_utc datetime2 NOT NULL CONSTRAINT DF_cluster_created DEFAULT (sysutcdatetime()),
  updated_at_utc datetime2 NULL,
  PRIMARY KEY CLUSTERED (id),
  CONSTRAINT UQ_cluster_key UNIQUE (cluster_key),
  CONSTRAINT CK_cluster_status CHECK ([status]=N'DISABLED' OR [status]=N'ACTIVE')
)
GO

--
-- Create table [wf].[worker]
--
PRINT (N'Create table [wf].[worker]')
GO
CREATE TABLE wf.worker (
  id bigint IDENTITY,
  cluster_id bigint NOT NULL,
  external_worker_key nvarchar(128) NULL,
  display_name nvarchar(256) NULL,
  hostname nvarchar(256) NULL,
  capabilities json NULL,
  status varchar(32) NOT NULL CONSTRAINT DF_worker_status DEFAULT (N'REGISTERED'),
  last_seen_at_utc datetime2 NULL,
  created_at_utc datetime2 NOT NULL CONSTRAINT DF_worker_created DEFAULT (sysutcdatetime()),
  updated_at_utc datetime2 NULL,
  PRIMARY KEY CLUSTERED (id),
  CONSTRAINT UQ_worker_external_key UNIQUE (external_worker_key),
  CONSTRAINT CK_worker_status CHECK ([status]=N'REVOKED' OR [status]=N'SUSPENDED' OR [status]=N'REGISTERED')
)
GO

--
-- Create index [IX_worker_cluster_status] on table [wf].[worker]
--
PRINT (N'Create index [IX_worker_cluster_status] on table [wf].[worker]')
GO
CREATE INDEX IX_worker_cluster_status
  ON wf.worker (cluster_id, status)
GO

--
-- Create foreign key [FK_worker_cluster] on table [wf].[worker]
--
PRINT (N'Create foreign key [FK_worker_cluster] on table [wf].[worker]')
GO
ALTER TABLE wf.worker
  ADD CONSTRAINT FK_worker_cluster FOREIGN KEY (cluster_id) REFERENCES wf.cluster (id)
GO

--
-- Create table [wf].[worker_token]
--
PRINT (N'Create table [wf].[worker_token]')
GO
CREATE TABLE wf.worker_token (
  id bigint IDENTITY,
  worker_id bigint NOT NULL,
  token_hash varbinary(32) NOT NULL,
  token_prefix nvarchar(16) NULL,
  issued_at_utc datetime2 NOT NULL CONSTRAINT DF_wt_issued DEFAULT (sysutcdatetime()),
  expires_at_utc datetime2 NULL,
  revoked_at_utc datetime2 NULL,
  status varchar(32) NOT NULL CONSTRAINT DF_wt_status DEFAULT (N'ACTIVE'),
  PRIMARY KEY CLUSTERED (id),
  CONSTRAINT UQ_wt_worker_hash UNIQUE (worker_id, token_hash),
  CONSTRAINT CK_wt_status CHECK ([status]=N'REVOKED' OR [status]=N'EXPIRED' OR [status]=N'ACTIVE')
)
GO

--
-- Create foreign key [FK_wt_worker] on table [wf].[worker_token]
--
PRINT (N'Create foreign key [FK_wt_worker] on table [wf].[worker_token]')
GO
ALTER TABLE wf.worker_token
  ADD CONSTRAINT FK_wt_worker FOREIGN KEY (worker_id) REFERENCES wf.worker (id) ON DELETE CASCADE
GO

--
-- Create or alter procedure [wf].[wf_worker_authenticate]
--
GO
PRINT (N'Create or alter procedure [wf].[wf_worker_authenticate]')
GO

CREATE OR ALTER PROCEDURE wf.wf_worker_authenticate
    @worker_id BIGINT,
    @worker_token NVARCHAR(4000)
AS
BEGIN
    SET NOCOUNT ON;

    IF @worker_id IS NULL
        THROW 50002, N'worker_id is required.', 1;

    DECLARE @tok NVARCHAR(4000) = NULLIF(LTRIM(RTRIM(@worker_token)), N'');
    IF @tok IS NULL
        THROW 50002, N'worker_token is required.', 1;

    DECLARE @hash VARBINARY(32) = HASHBYTES(N'SHA2_256', @tok);

    IF NOT EXISTS (
        SELECT 1
        FROM wf.worker_token AS wt
        INNER JOIN wf.worker AS w ON w.id = wt.worker_id
        INNER JOIN wf.cluster AS c ON c.id = w.cluster_id
        WHERE wt.worker_id = @worker_id
          AND wt.token_hash = @hash
          AND wt.status = N'ACTIVE'
          AND (wt.expires_at_utc IS NULL OR wt.expires_at_utc > SYSUTCDATETIME())
          AND w.status = N'REGISTERED'
          AND c.status = N'ACTIVE'
    )
        THROW 50003, N'Invalid or unauthorized worker credentials.', 1;

    UPDATE wf.worker
    SET last_seen_at_utc = SYSUTCDATETIME()
    WHERE id = @worker_id;
END;
GO

--
-- Create table [wf].[task_lease]
--
PRINT (N'Create table [wf].[task_lease]')
GO
CREATE TABLE wf.task_lease (
  node_execution_id bigint NOT NULL,
  worker_id bigint NOT NULL,
  lease_expires_at_utc datetime2 NOT NULL,
  heartbeat_at_utc datetime2 NULL,
  PRIMARY KEY CLUSTERED (node_execution_id)
)
GO

--
-- Create index [IX_tl_worker_expiry] on table [wf].[task_lease]
--
PRINT (N'Create index [IX_tl_worker_expiry] on table [wf].[task_lease]')
GO
CREATE INDEX IX_tl_worker_expiry
  ON wf.task_lease (worker_id, lease_expires_at_utc)
GO

--
-- Create foreign key [FK_tl_ne] on table [wf].[task_lease]
--
PRINT (N'Create foreign key [FK_tl_ne] on table [wf].[task_lease]')
GO
ALTER TABLE wf.task_lease
  ADD CONSTRAINT FK_tl_ne FOREIGN KEY (node_execution_id) REFERENCES wf.node_execution (id) ON DELETE CASCADE
GO

--
-- Create foreign key [FK_tl_worker] on table [wf].[task_lease]
--
PRINT (N'Create foreign key [FK_tl_worker] on table [wf].[task_lease]')
GO
ALTER TABLE wf.task_lease
  ADD CONSTRAINT FK_tl_worker FOREIGN KEY (worker_id) REFERENCES wf.worker (id)
GO

--
-- Create or alter procedure [wf].[wf_engine_on_action_complete]
--
GO
PRINT (N'Create or alter procedure [wf].[wf_engine_on_action_complete]')
GO

CREATE OR ALTER PROCEDURE wf.wf_engine_on_action_complete
    @action_execution_id BIGINT,
    @result_code INT,
    @output_json NVARCHAR(MAX) NULL
AS
BEGIN
    SET NOCOUNT ON;

    DECLARE @inst BIGINT;
    DECLARE @wn BIGINT;
    DECLARE @parent BIGINT;
    /* Normalize payload as NVARCHAR only. Do not use a typed `json` variable here: assigning `json`
       to an `nvarchar(max)` column (older schemas) raises error 257; `nvarchar` assigns to both
       `nvarchar(max)` and native `json` columns via implicit conversion where supported. */
    DECLARE @oj NVARCHAR(MAX) = CASE
        WHEN @output_json IS NULL THEN NULL
        WHEN LTRIM(RTRIM(@output_json)) = N'' THEN NULL
        ELSE @output_json
    END;

    SELECT @inst = workflow_instance_id, @wn = workflow_node_id, @parent = parent_node_execution_id
    FROM wf.node_execution WHERE id = @action_execution_id;

    IF @result_code < 0
    BEGIN
        UPDATE wf.node_execution
        SET status = N'FAILED',
            result_code = @result_code,
            output_json = @oj,
            ended_at_utc = SYSUTCDATETIME(),
            engine_error_code = @result_code
        WHERE id = @action_execution_id;

        DELETE FROM wf.task_lease WHERE node_execution_id = @action_execution_id;

        -- Leave instance RUNNING so sibling FOREACH tasks remain claimable.
        RETURN;
    END

    UPDATE wf.node_execution
    SET status = N'SUCCEEDED',
        result_code = @result_code,
        output_json = @oj,
        ended_at_utc = SYSUTCDATETIME()
    WHERE id = @action_execution_id;

    DELETE FROM wf.task_lease WHERE node_execution_id = @action_execution_id;

    IF @parent IS NULL
    BEGIN
        UPDATE wf.workflow_instance SET status = N'COMPLETED', completed_at_utc = SYSUTCDATETIME() WHERE id = @inst;
        RETURN;
    END

    DECLARE @ptype VARCHAR(32);
    SELECT @ptype = wn.node_type
    FROM wf.node_execution AS ne
    INNER JOIN wf.workflow_node AS wn ON wn.id = ne.workflow_node_id
    WHERE ne.id = @parent;

    IF @ptype = N'SEQUENCE'
        EXEC wf.wf_sequence_continue @sequence_execution_id = @parent;

    ELSE IF @ptype = N'PARALLEL'
        EXEC wf.wf_parallel_continue @parallel_execution_id = @parent;

    ELSE IF @ptype IN (N'IF', N'SWITCH')
    BEGIN
        UPDATE wf.node_execution SET status = N'SUCCEEDED', ended_at_utc = SYSUTCDATETIME() WHERE id = @parent;
        EXEC wf.wf_engine_on_composite_complete @node_execution_id = @parent;
    END

    ELSE IF @ptype = N'REPEAT'
        EXEC wf.wf_repeat_continue @repeat_execution_id = @parent;

    ELSE IF @ptype = N'WHILE'
        EXEC wf.wf_while_continue @while_execution_id = @parent;

    /* FOREACH arrives with wf_sql_foreach_support.sql; dispatch through the router so
       re-running this base script never drops FOREACH continuation. */
    ELSE IF @ptype = N'FOREACH'
        EXEC wf.wf_foreach_route_continue @foreach_execution_id = @parent;
END;
GO

--
-- Create or alter procedure [wf].[sp_worker_submit_result]
--
GO
PRINT (N'Create or alter procedure [wf].[sp_worker_submit_result]')
GO

CREATE OR ALTER PROCEDURE wf.sp_worker_submit_result
    @node_execution_id BIGINT,
    @worker_id BIGINT,
    @worker_token NVARCHAR(4000),
    @result_code INT,
    @output_json NVARCHAR(MAX) NULL,
    @accepted BIT OUTPUT,
    @instance_status VARCHAR(32) OUTPUT,
    @next_ready_count INT OUTPUT
AS
BEGIN
    SET NOCOUNT ON;
    SET XACT_ABORT ON;

    SET @accepted = 0;
    SET @next_ready_count = 0;
    SET @instance_status = NULL;

    EXEC wf.wf_worker_authenticate @worker_id = @worker_id, @worker_token = @worker_token;

    BEGIN TRANSACTION;

    DECLARE @lease_worker BIGINT;
    SELECT @lease_worker = worker_id FROM wf.task_lease WITH (UPDLOCK, HOLDLOCK) WHERE node_execution_id = @node_execution_id;

    IF @lease_worker IS NULL OR @lease_worker <> @worker_id
    BEGIN
        ROLLBACK TRANSACTION;
        RETURN;
    END

    DECLARE @cur_status VARCHAR(32);
    SELECT @cur_status = status FROM wf.node_execution WITH (UPDLOCK, HOLDLOCK) WHERE id = @node_execution_id;

    IF @cur_status <> N'RUNNING'
    BEGIN
        ROLLBACK TRANSACTION;
        RETURN;
    END

    EXEC wf.wf_engine_on_action_complete
        @action_execution_id = @node_execution_id,
        @result_code = @result_code,
        @output_json = @output_json;

    SET @accepted = 1;

    SELECT @instance_status = status FROM wf.workflow_instance WHERE id = (SELECT workflow_instance_id FROM wf.node_execution WHERE id = @node_execution_id);

    SELECT @next_ready_count = COUNT(*)
    FROM wf.node_execution
    WHERE workflow_instance_id = (SELECT workflow_instance_id FROM wf.node_execution WHERE id = @node_execution_id)
      AND status = N'READY';

    COMMIT TRANSACTION;
END;
GO

--
-- Create or alter procedure [wf].[sp_worker_request_task]
--
GO
PRINT (N'Create or alter procedure [wf].[sp_worker_request_task]')
GO

CREATE OR ALTER FUNCTION wf.wf_worker_is_omnibus(@capabilities NVARCHAR(MAX))
RETURNS BIT
AS
BEGIN
    -- Only explicit ["*"] is omnibus. NULL / '' / [] mean "no capabilities".
    IF @capabilities IS NULL OR LTRIM(RTRIM(@capabilities)) = N'' OR @capabilities = N'[]'
        RETURN 0;
    IF EXISTS (
        SELECT 1
        FROM OPENJSON(@capabilities) WITH (value NVARCHAR(128) '$') AS caps
        WHERE caps.value = N'*'
    )
        RETURN 1;
    RETURN 0;
END;
GO

CREATE OR ALTER FUNCTION wf.wf_worker_capability_allowed(
    @worker_capabilities NVARCHAR(MAX),
    @task_capability NVARCHAR(128)
)
RETURNS BIT
AS
BEGIN
    IF @task_capability IS NULL
        RETURN 1;
    IF wf.wf_worker_is_omnibus(@worker_capabilities) = 1
        RETURN 1;
    IF EXISTS (
        SELECT 1
        FROM OPENJSON(@worker_capabilities) WITH (value NVARCHAR(128) '$') AS caps
        WHERE caps.value = @task_capability
    )
        RETURN 1;
    RETURN 0;
END;
GO

CREATE OR ALTER PROCEDURE wf.sp_worker_request_task
    @worker_id BIGINT,
    @worker_token NVARCHAR(4000),
    @capability NVARCHAR(128) NULL,
    @max_lease_seconds INT = 300
AS
BEGIN
    SET NOCOUNT ON;
    SET XACT_ABORT ON;

    EXEC wf.wf_worker_authenticate @worker_id = @worker_id, @worker_token = @worker_token;

    DECLARE @now DATETIME2(7) = SYSUTCDATETIME();
    DECLARE @lease_end DATETIME2(7) = DATEADD(SECOND, @max_lease_seconds, @now);
    DECLARE @worker_capabilities NVARCHAR(MAX);
    DECLARE @is_omnibus BIT;
    DECLARE @reclaim_cutoff DATETIME2(7) = DATEADD(SECOND, -60, @now);

    -- Auto-unstick crashed workers: expired/missing leases → READY (quiet: no result set).
    IF EXISTS (
        SELECT 1
        FROM wf.node_execution AS ne
        LEFT JOIN wf.task_lease AS l ON l.node_execution_id = ne.id
        WHERE ne.status = N'RUNNING'
          AND (
                l.node_execution_id IS NULL
             OR l.lease_expires_at_utc <= @reclaim_cutoff
          )
    )
    BEGIN
        EXEC wf.sp_reclaim_expired_leases @grace_seconds = 60, @quiet = 1;
    END

    SELECT @worker_capabilities = w.capabilities
    FROM wf.worker AS w
    WHERE w.id = @worker_id;

    SET @is_omnibus = wf.wf_worker_is_omnibus(@worker_capabilities);

    IF @capability IS NOT NULL
       AND @is_omnibus = 0
       AND wf.wf_worker_capability_allowed(@worker_capabilities, @capability) = 0
    BEGIN
        RETURN;
    END

    BEGIN TRANSACTION;

    DECLARE @picked_ids TABLE (id BIGINT);

    ;WITH cte AS (
        SELECT TOP (1) ne.id
        FROM wf.node_execution AS ne WITH (ROWLOCK, READPAST, UPDLOCK)
        INNER JOIN wf.workflow_node AS wn ON wn.id = ne.workflow_node_id
        INNER JOIN wf.workflow_action AS wa ON wa.id = wn.workflow_action_id
        INNER JOIN wf.workflow_instance AS wi ON wi.id = ne.workflow_instance_id
        WHERE ne.status = N'READY'
          AND wn.node_type = N'ACTION'
          AND wi.status = N'RUNNING'
          AND (ne.available_at_utc IS NULL OR ne.available_at_utc <= @now)
          AND wf.wf_worker_capability_allowed(@worker_capabilities, wa.capability) = 1
          AND (
              @capability IS NULL
              OR wa.capability = @capability
              OR wa.capability IS NULL
          )
        ORDER BY ne.available_at_utc ASC, ne.id ASC
    )
    UPDATE ne
    SET status = N'RUNNING',
        started_at_utc = @now
    OUTPUT inserted.id INTO @picked_ids(id)
    FROM wf.node_execution AS ne
    INNER JOIN cte ON cte.id = ne.id;

    DECLARE @picked BIGINT;
    SELECT @picked = id FROM @picked_ids;

    IF @picked IS NULL
    BEGIN
        ROLLBACK TRANSACTION;
        RETURN;
    END

    ;MERGE wf.task_lease AS t
    USING (SELECT @picked AS node_execution_id) AS s ON (t.node_execution_id = s.node_execution_id)
    WHEN MATCHED THEN
        UPDATE SET worker_id = @worker_id, lease_expires_at_utc = @lease_end, heartbeat_at_utc = @now
    WHEN NOT MATCHED THEN
        INSERT (node_execution_id, worker_id, lease_expires_at_utc, heartbeat_at_utc)
        VALUES (@picked, @worker_id, @lease_end, @now);

    COMMIT TRANSACTION;

    SELECT
        ne.id AS node_execution_id,
        ne.workflow_instance_id,
        wn.node_key,
        wa.action_name,
        wa.capability,
        ne.attempt_no,
        ne.input_json,
        ne.iteration_no
    FROM wf.node_execution AS ne
    INNER JOIN wf.workflow_node AS wn ON wn.id = ne.workflow_node_id
    INNER JOIN wf.workflow_action AS wa ON wa.id = wn.workflow_action_id
    WHERE ne.id = @picked;
END;
GO

--
-- Create or alter procedure [wf].[sp_worker_heartbeat]
--
GO
PRINT (N'Create or alter procedure [wf].[sp_worker_heartbeat]')
GO

CREATE OR ALTER PROCEDURE wf.sp_worker_heartbeat
    @node_execution_id BIGINT,
    @worker_id BIGINT,
    @worker_token NVARCHAR(4000),
    @extend_seconds INT = 300
AS
BEGIN
    SET NOCOUNT ON;

    EXEC wf.wf_worker_authenticate @worker_id = @worker_id, @worker_token = @worker_token;

    DECLARE @now DATETIME2(7) = SYSUTCDATETIME();

    UPDATE tl
    SET lease_expires_at_utc = DATEADD(SECOND, @extend_seconds, @now),
        heartbeat_at_utc = @now
    FROM wf.task_lease AS tl
    WHERE tl.node_execution_id = @node_execution_id AND tl.worker_id = @worker_id;

    SELECT @@ROWCOUNT AS rows_updated;
END;
GO

--
-- Create or alter procedure [wf].[sp_worker_fail_task]
--
GO
PRINT (N'Create or alter procedure [wf].[sp_worker_fail_task]')
GO

CREATE OR ALTER PROCEDURE wf.sp_worker_fail_task
    @node_execution_id BIGINT,
    @worker_id BIGINT,
    @worker_token NVARCHAR(4000),
    @error_code INT,
    @error_message NVARCHAR(1024) NULL
AS
BEGIN
    SET NOCOUNT ON;

    EXEC wf.wf_worker_authenticate @worker_id = @worker_id, @worker_token = @worker_token;

    IF NOT EXISTS (
        SELECT 1 FROM wf.task_lease WHERE node_execution_id = @node_execution_id AND worker_id = @worker_id
    )
        RETURN;

    UPDATE wf.node_execution
    SET status = N'FAILED',
        engine_error_code = @error_code,
        engine_error_message = @error_message,
        ended_at_utc = SYSUTCDATETIME()
    WHERE id = @node_execution_id AND status = N'RUNNING';

    DELETE FROM wf.task_lease WHERE node_execution_id = @node_execution_id;

    -- Do not mark workflow_instance FAILED here: one sample action failure must not
    -- strand sibling READY tasks in a FOREACH (SamplePrep Align fan-out).
END;
GO

--
-- Create or alter procedure [wf].[sp_reclaim_expired_leases]
-- See also: wf_reclaim_expired_leases.sql (portal wrapper).
--
GO
PRINT (N'Create or alter procedure [wf].[sp_reclaim_expired_leases]')
GO

CREATE OR ALTER PROCEDURE wf.sp_reclaim_expired_leases
    @workflow_instance_id BIGINT = NULL,
    @grace_seconds INT = 60,
    @quiet BIT = 0
AS
BEGIN
    SET NOCOUNT ON;
    SET XACT_ABORT ON;

    IF @grace_seconds < 0 SET @grace_seconds = 0;

    DECLARE @cutoff DATETIME2(7) = DATEADD(SECOND, -@grace_seconds, SYSUTCDATETIME());
    DECLARE @reclaimed TABLE (
        node_execution_id BIGINT NOT NULL,
        workflow_instance_id BIGINT NOT NULL,
        previous_worker_id BIGINT NULL,
        lease_expired_at_utc DATETIME2(7) NULL
    );

    BEGIN TRANSACTION;

    ;WITH expired AS (
        SELECT ne.id AS node_execution_id,
               ne.workflow_instance_id,
               l.worker_id AS previous_worker_id,
               l.lease_expires_at_utc
        FROM wf.node_execution AS ne
        LEFT JOIN wf.task_lease AS l ON l.node_execution_id = ne.id
        WHERE ne.status = N'RUNNING'
          AND (
                l.node_execution_id IS NULL
             OR l.lease_expires_at_utc <= @cutoff
          )
          AND (@workflow_instance_id IS NULL OR ne.workflow_instance_id = @workflow_instance_id)
    )
    UPDATE ne
    SET status = N'READY',
        result_code = NULL,
        engine_error_code = NULL,
        engine_error_message = N'reclaimed: lease expired or missing',
        output_json = NULL,
        started_at_utc = NULL,
        ended_at_utc = NULL,
        attempt_no = ISNULL(ne.attempt_no, 0) + 1,
        available_at_utc = SYSUTCDATETIME()
    OUTPUT inserted.id, inserted.workflow_instance_id, e.previous_worker_id, e.lease_expires_at_utc
    INTO @reclaimed (node_execution_id, workflow_instance_id, previous_worker_id, lease_expired_at_utc)
    FROM wf.node_execution AS ne
    INNER JOIN expired AS e ON e.node_execution_id = ne.id
    WHERE ne.status = N'RUNNING';

    DELETE l
    FROM wf.task_lease AS l
    INNER JOIN @reclaimed AS r ON r.node_execution_id = l.node_execution_id;

    COMMIT TRANSACTION;

    IF @quiet = 0
    BEGIN
        SELECT node_execution_id,
               workflow_instance_id,
               previous_worker_id,
               lease_expired_at_utc,
               SYSUTCDATETIME() AS reclaimed_at_utc
        FROM @reclaimed
        ORDER BY node_execution_id;
    END
END;
GO

--
-- Create schema [Meta]
--
PRINT (N'Create schema [Meta]')
GO
CREATE SCHEMA Meta AUTHORIZATION dbo
GO

--
-- Create or alter function [Meta].[fnGetViewName]
--
GO
PRINT (N'Create or alter function [Meta].[fnGetViewName]')
GO
CREATE OR ALTER FUNCTION Meta.fnGetViewName (@TableName VARCHAR(64))
RETURNS VARCHAR(64)
BEGIN
  RETURN 'V' + @TableName;
END;

GO

--
-- Create or alter procedure [Meta].[spHistoryInsert]
--
GO
PRINT (N'Create or alter procedure [Meta].[spHistoryInsert]')
GO
CREATE OR ALTER PROCEDURE Meta.spHistoryInsert(@ObjID INT, @EventID TINYINT, @HistoryID INT OUTPUT)
AS 
BEGIN
	INSERT INTO dbo.History (ObjID, EventID)
    VALUES (@ObjID, @EventID);

  SET @HistoryID = SCOPE_IDENTITY();
END
GO

--
-- Create table [Meta].[LinkTypes]
--
PRINT (N'Create table [Meta].[LinkTypes]')
GO
CREATE TABLE Meta.LinkTypes (
  ID tinyint IDENTITY,
  Name varchar(32) NOT NULL,
  CONSTRAINT PK_LinkKind_ID PRIMARY KEY CLUSTERED (ID)
)
GO

--
-- Create table [Meta].[Event]
--
PRINT (N'Create table [Meta].[Event]')
GO
CREATE TABLE Meta.Event (
  ID tinyint IDENTITY,
  Name varchar(32) NOT NULL,
  PRIMARY KEY CLUSTERED (ID)
)
GO

--
-- Create table [Meta].[Classes]
--
PRINT (N'Create table [Meta].[Classes]')
GO
CREATE TABLE Meta.Classes (
  ID int IDENTITY,
  Name varchar(128) NOT NULL,
  TableName varchar(64) NULL,
  ParentID int NULL,
  HasHistory bit NULL,
  CONSTRAINT PK_Classes PRIMARY KEY CLUSTERED (ID)
)
GO

--
-- Create or alter function [Meta].[GetClassHasHistory]
--
GO
PRINT (N'Create or alter function [Meta].[GetClassHasHistory]')
GO
CREATE OR ALTER FUNCTION Meta.GetClassHasHistory(@ClassID INT)
RETURNS BIT
AS
BEGIN
  RETURN (SELECT HasHistory FROM Meta.Classes WHERE ID = @ClassID);
END;
GO

--
-- Create or alter function [Meta].[fnGetFirstChildClassID]
--
GO
PRINT (N'Create or alter function [Meta].[fnGetFirstChildClassID]')
GO
CREATE OR ALTER FUNCTION Meta.fnGetFirstChildClassID (@ClassID int)
RETURNS INT
AS
BEGIN
  DECLARE @ChildClassID INT;

  SELECT
    @ChildClassID = MIN(c.ID)
  FROM Meta.Classes c
  WHERE c.ParentID = @ClassID;

  RETURN @ChildClassID;
END
GO

--
-- Create or alter procedure [Meta].[spClassDelete]
--
GO
PRINT (N'Create or alter procedure [Meta].[spClassDelete]')
GO
--
-- The goal of this procedure is to emulate the
-- behavior of CASCADE DELETE
--
CREATE OR ALTER PROCEDURE Meta.spClassDelete(@ClassID INT)
AS
BEGIN
  DECLARE @ChildClassID INT;

  IF @ClassID IS NULL
    RETURN;

  SET @ChildClassID = Meta.fnGetFirstChildClassID(@ClassID);

  WHILE @ChildClassID IS NOT NULL
  BEGIN
    EXEC Meta.spClassDelete @ClassID = @ChildClassID;

    SET @ChildClassID = Meta.fnGetFirstChildClassID(@ClassID);
  END;
END
GO

--
-- Create or alter function [Meta].[fnGetClassFieldsWithTypes]
--
GO
PRINT (N'Create or alter function [Meta].[fnGetClassFieldsWithTypes]')
GO
CREATE OR ALTER FUNCTION Meta.fnGetClassFieldsWithTypes(@ClassID INT)
RETURNS VARCHAR(8000)
AS
BEGIN
  RETURN
  (
    SELECT STRING_AGG(ColumnDeclaration + ',', CHAR(13) + CHAR(10)) WITHIN GROUP (ORDER BY column_id) AS JoinedColumns
    FROM (
      SELECT 
        QUOTENAME(c.name) + ' ' + 
        t.name + 
        CASE 
           WHEN t.name IN ('char', 'varchar', 'nchar', 'nvarchar') 
                THEN '(' + 
                  CASE WHEN c.max_length = -1 
                    THEN 'MAX' 
                    ELSE CAST(
                      CASE WHEN t.name LIKE 'n%' 
                        THEN c.max_length / 2 
                        ELSE c.max_length 
                      END AS VARCHAR(5)
                    ) 
                  END + ')' 
           ELSE '' 
        END +
        CASE 
           WHEN t.name IN ('decimal', 'numeric') 
                THEN '(' + CAST(c.precision AS VARCHAR(5)) + ',' + CAST(c.scale AS VARCHAR(5)) + ')' 
                ELSE '' 
        END +
        CASE WHEN c.is_nullable = 1 THEN ' NULL' ELSE ' NOT NULL' END AS ColumnDeclaration,
        c.column_id
      FROM sys.columns c
      JOIN sys.types t ON c.user_type_id = t.user_type_id
      JOIN sys.tables tbl ON c.object_id = tbl.object_id
      JOIN Meta.Classes m ON tbl.Name = m.TableName
      WHERE m.ID = @ClassID
    ) src
  )
END;
GO

--
-- Create or alter procedure [Meta].[spClassCreateHistorySql]
--
GO
PRINT (N'Create or alter procedure [Meta].[spClassCreateHistorySql]')
GO
CREATE OR ALTER PROCEDURE Meta.spClassCreateHistorySql (@ClassID INT, @Sql NVARCHAR(4000) OUTPUT)
AS
BEGIN
  SET NOCOUNT ON;
  DECLARE @TableName VARCHAR(64);
  DECLARE @ViewName VARCHAR(64);
  DECLARE @Fields NVARCHAR(4000);

  SET @TableName = (SELECT 'H' + TableName FROM Meta.Classes WHERE ID = @ClassID);
  IF @TableName IS NULL
    THROW 50001, 'Invalid ClassID', 1;

  SET @Fields = [Meta].[fnGetClassFieldsWithTypes](@ClassID);
  SET @Sql = 
    'CREATE TABLE [dbo].[' + @TableName + '] (' +
    'HID INT NOT NULL, ' +
    'EventTime DATETIME NOT NULL DEFAULT GETDATE(), ' +
    ISNULL(@Fields, '') + 
    'CONSTRAINT PK_' + @TableName + '_HID PRIMARY KEY CLUSTERED (HID), ' +
    'CONSTRAINT FK_' + @TableName + '_History_ID FOREIGN KEY (HID) ' +
    'REFERENCES dbo.History(ID) ON DELETE CASCADE)';
END;
GO

--
-- Create or alter function [Meta].[fnGetClassFields]
--
GO
PRINT (N'Create or alter function [Meta].[fnGetClassFields]')
GO
CREATE OR ALTER FUNCTION Meta.fnGetClassFields(@ClassID INT)
RETURNS VARCHAR(8000)
AS
BEGIN
  RETURN (
    SELECT STRING_AGG(c.[name], ', ')
    FROM [sys].[columns] AS c
    INNER JOIN [sys].[tables] AS t ON c.[object_id] = t.[object_id]
    INNER JOIN [Meta].[Classes] AS cls ON t.[name] = cls.[TableName]
    WHERE cls.ID = @ClassID
  );
END;
GO

--
-- Create or alter procedure [Meta].[spClassCreateViewSql]
--
GO
PRINT (N'Create or alter procedure [Meta].[spClassCreateViewSql]')
GO
CREATE OR ALTER PROCEDURE Meta.spClassCreateViewSql(@ClassID INT, @Sql NVARCHAR(4000) OUTPUT)
AS
BEGIN
  DECLARE @ParentClassID    INT;
  DECLARE @ParentClassName  VARCHAR(64);
  DECLARE @ParentTableName  VARCHAR(64);
  DECLARE @ParentViewName   VARCHAR(64);
  DECLARE @TableName        VARCHAR(64);
  DECLARE @ViewName         VARCHAR(64);
  DECLARE @ViewFields       VARCHAR(8000);

  SET @ParentClassID   = (SELECT ParentID FROM Meta.Classes WHERE ID = @ClassID);
  SELECT 
    @ParentClassName = Name,
    @ParentTableName = TableName
  FROM Meta.Classes 
  WHERE ID = @ParentClassID;

  SET @ParentViewName  = Meta.fnGetViewName(@ParentTableName);
  SET @TableName = (SELECT TableName FROM Meta.Classes WHERE ID = @ClassID);

  SET @ViewName  = Meta.fnGetViewName(@TableName);
  SET @ViewFields = Meta.fnGetClassFields(@ClassID);

  -- Add to each field the alias of the table
  -- Delete the start of the fields (ID, )
  -- ID,FirstName,MiddleName,LastName
  SET @ViewFields  = REPLACE(@ViewFields, ', ', ', t.');
  -- ID,t.FirstName,t.MiddleName,t.LastName
  SET @ViewFields  = RIGHT(@ViewFields, LEN(@ViewFields) - 3);
  -- t.FirstName,t.MiddleName,t.LastName

  SET @Sql = 
    'CREATE OR ALTER VIEW ' + @ViewName + 
    ' AS ' +  
    'SELECT pv.*, ' + @ViewFields + 
    ' FROM ' + @ParentViewName + ' pv INNER JOIN ' + @TableName + ' t ON t.ID = pv.ID';
END;
GO

--
-- Create or alter procedure [Meta].[spUpdateObject]
--
GO
PRINT (N'Create or alter procedure [Meta].[spUpdateObject]')
GO
CREATE OR ALTER PROCEDURE Meta.spUpdateObject
    @ClassID        int,                    -- Usually the concrete/leaf class ID
    @ID             int,                    -- Existing object ID to update
    @ValuesJson     nvarchar(max),          -- JSON with fields to update (can span multiple levels)
    @ErrorMessage   nvarchar(1000) = NULL OUTPUT
AS
BEGIN
    SET NOCOUNT ON;
    SET XACT_ABORT ON;

    DECLARE @TranStarted bit = 0;
    IF @@TRANCOUNT = 0
    BEGIN
        BEGIN TRANSACTION;
        SET @TranStarted = 1;
    END

    BEGIN TRY
        -- 1. Basic validation
        IF NOT EXISTS (SELECT 1 FROM dbo.Objs WHERE ID = @ID)
        BEGIN
            RAISERROR('Object with ID %d does not exist.', 16, 1, @ID);
        END

        IF NOT EXISTS (SELECT 1 FROM Meta.Classes WHERE ID = @ClassID)
        BEGIN
            RAISERROR('Class ID %d does not exist.', 16, 1, @ClassID);
        END

        -- 2. Get full inheritance chain (leaf → root)
        DECLARE @Chain TABLE (
            Ordinal     int IDENTITY(1,1),
            ClassID     int,
            ClassName   nvarchar(128),
            TableName   nvarchar(128)
        );

        WITH hierarchy AS
        (
            SELECT ID, Name, COALESCE(TableName, Name) AS TableName, ParentID, 0 AS Lvl
            FROM Meta.Classes 
            WHERE ID = @ClassID
            
            UNION ALL
            
            SELECT c.ID, c.Name, COALESCE(c.TableName, c.Name), c.ParentID, h.Lvl + 1
            FROM Meta.Classes c
            INNER JOIN hierarchy h ON c.ID = h.ParentID
        )
        INSERT INTO @Chain (ClassID, ClassName, TableName)
        SELECT ID, Name, TableName
        FROM hierarchy
        WHERE TableName <> 'Objs'   -- exclude Objs table
        ORDER BY Lvl DESC;   -- deepest (leaf) first → root last

        -- 3. Update each level that has own columns — from leaf to root
        DECLARE 
            @CurClassID   int,
            @CurTable     sysname,
            @sql          nvarchar(max),
            @setClause    nvarchar(max),
            @hasColumns   bit;

        DECLARE cur CURSOR LOCAL FAST_FORWARD FOR
            SELECT ClassID, TableName 
            FROM @Chain
            ORDER BY Ordinal;   -- leaf → root

        OPEN cur;
        FETCH NEXT FROM cur INTO @CurClassID, @CurTable;

        WHILE @@FETCH_STATUS = 0
        BEGIN
            IF @CurTable IS NULL OR @CurTable = ''
            BEGIN
                FETCH NEXT FROM cur INTO @CurClassID, @CurTable;
                CONTINUE;
            END

            -- Build SET clause only for columns that exist in JSON
            SET @setClause = N'';
            SET @hasColumns = 0;

            SELECT 
                @setClause += 
                    CASE 
                        WHEN EXISTS (
                            SELECT 1 
                            FROM OPENJSON(@ValuesJson)
                            WHERE [key] COLLATE SQL_Latin1_General_CP1_CI_AS = c.name
                        )
                        THEN QUOTENAME(c.name) + ' = JSON_VALUE(@ValuesJson, ''$.' + c.name + '''), '
                    END,
                @hasColumns = 1
            FROM sys.columns c
            WHERE c.object_id = OBJECT_ID(QUOTENAME('dbo') + '.' + QUOTENAME(@CurTable))
              AND c.name <> 'ID'
              AND c.is_computed = 0;

            IF @hasColumns = 0 OR @setClause = ''
            BEGIN
                FETCH NEXT FROM cur INTO @CurClassID, @CurTable;
                CONTINUE;
            END

            -- Remove trailing comma
            SET @setClause = LEFT(@setClause, LEN(@setClause) - 1);

            SET @sql = N'
UPDATE dbo.' + QUOTENAME(@CurTable) + N'
SET ' + @setClause + N'
WHERE ID = @ID;';

            -- Debug option (uncomment when developing)
            -- PRINT @sql;

            EXEC sp_executesql 
                @sql,
                N'@ID int, @ValuesJson nvarchar(max)',
                @ID = @ID,
                @ValuesJson = @ValuesJson;

            FETCH NEXT FROM cur INTO @CurClassID, @CurTable;
        END

        CLOSE cur;
        DEALLOCATE cur;

        -- 4. Optional - History entry (if the concrete class tracks history)
        DECLARE @HasHistory bit;
        SELECT @HasHistory = HasHistory 
        FROM Meta.Classes 
        WHERE ID = @ClassID;

        IF @HasHistory = 1
        BEGIN
            INSERT dbo.History (ObjID, EventID, EventTime)
            VALUES (@ID, 2, DEFAULT);  -- 2 = Updated (you may use different EventID)
        END

        IF @TranStarted = 1
            COMMIT TRANSACTION;
    END TRY
    BEGIN CATCH
        IF @TranStarted = 1 AND XACT_STATE() = -1
            ROLLBACK TRANSACTION;

        SET @ErrorMessage = 
            N'Error in Meta.spUpdateObject (ID=' + CAST(@ID AS nvarchar(20)) + 
            N', ClassID=' + CAST(@ClassID AS nvarchar(20)) + N'): ' +
            ERROR_MESSAGE();

        THROW;
    END CATCH
END
GO

--
-- Create or alter procedure [Meta].[spObjectInsert]
--
GO
PRINT (N'Create or alter procedure [Meta].[spObjectInsert]')
GO
CREATE OR ALTER PROCEDURE Meta.spObjectInsert(@ClassID INT, @ID INT OUTPUT)
AS 
BEGIN
  IF NOT EXISTS (SELECT * FROM Meta.Classes WHERE ID = @ClassID)
  BEGIN
    SET @ID = NULL;
    RETURN;
  END;

  INSERT INTO dbo.TObject(ClassID)
    VALUES(@ClassID);

  SET @ID = SCOPE_IDENTITY();
END;
GO

--
-- Create or alter procedure [Meta].[spInsertObject]
--
GO
PRINT (N'Create or alter procedure [Meta].[spInsertObject]')
GO

CREATE OR ALTER PROCEDURE Meta.spInsertObject
    @ClassID        int,
    @ValuesJson     nvarchar(max),
    @NewID          int           OUTPUT,
    @ErrorMessage   nvarchar(1000) = NULL OUTPUT
AS
BEGIN
    SET NOCOUNT ON;
    SET XACT_ABORT ON;

    DECLARE @TranStarted bit = 0;
    IF @@TRANCOUNT = 0
    BEGIN
        BEGIN TRANSACTION;
        SET @TranStarted = 1;
    END

    BEGIN TRY
        -- Validate class
        IF NOT EXISTS (SELECT 1 FROM Meta.Classes WHERE ID = @ClassID)
            RAISERROR('Class ID %d not found.', 16, 1, @ClassID);

        -- Build chain leaf → root, exclude Objs
        DECLARE @Chain TABLE (
            Ordinal     int IDENTITY,
            ClassID     int,
            TableName   nvarchar(128)
        );

        ;WITH hierarchy AS (
            SELECT ID, COALESCE(TableName, Name) AS TableName, ParentID, 0 AS Lvl
            FROM Meta.Classes WHERE ID = @ClassID
            UNION ALL
            SELECT c.ID, COALESCE(c.TableName, c.Name), c.ParentID, h.Lvl + 1
            FROM Meta.Classes c INNER JOIN hierarchy h ON c.ID = h.ParentID
        )
        INSERT @Chain (ClassID, TableName)
        SELECT ID, TableName
        FROM hierarchy
        WHERE TableName <> 'Objs'
        ORDER BY Lvl DESC;

        -- Create Objs row
        INSERT dbo.Objs (ClassID) VALUES (@ClassID);
        SET @NewID = SCOPE_IDENTITY();

        IF @NewID IS NULL
            RAISERROR('Failed to insert into Objs.', 16, 1);

        -- Process tables
        DECLARE 
            @CurTable   nvarchar(128),
            @sql        nvarchar(max),
            @cols       nvarchar(max),
            @values     nvarchar(max),
            @setClause  nvarchar(max);

        DECLARE cur CURSOR LOCAL FAST_FORWARD FOR 
            SELECT TableName FROM @Chain;

        OPEN cur;
        FETCH NEXT FROM cur INTO @CurTable;

        WHILE @@FETCH_STATUS = 0
        BEGIN
            IF @CurTable IS NULL OR @CurTable = ''
            BEGIN
                FETCH NEXT FROM cur INTO @CurTable;
                CONTINUE;
            END

            SET @cols = N'';
            SET @values = N'';
            SET @setClause = N'';

            SELECT 
                @cols      += CASE WHEN @cols = '' THEN '' ELSE ', ' END + QUOTENAME(name),
                @values    += CASE WHEN @values = '' THEN '' ELSE ', ' END + 'JSON_VALUE(@ValuesJson, ''$.' + name + ''')',
                @setClause += CASE WHEN @setClause = '' THEN '' ELSE ', ' END + QUOTENAME(name) + ' = JSON_VALUE(@ValuesJson, ''$.' + name + ''')'
            FROM sys.columns
            WHERE object_id = OBJECT_ID('dbo.' + @CurTable)
              AND name <> 'ID'
              AND is_computed = 0;

            IF @cols = ''
            BEGIN
                FETCH NEXT FROM cur INTO @CurTable;
                CONTINUE;
            END

            -- Safe & debug-friendly
            SET @sql = CONCAT(
                N'MERGE dbo.', QUOTENAME(@CurTable), N' AS tgt ',
                N'USING (VALUES (@NewID)) AS src(ID) ',
                N'ON tgt.ID = src.ID ',
                N'WHEN MATCHED THEN ',
                N'    UPDATE SET ', @setClause, N' ',
                N'WHEN NOT MATCHED BY TARGET THEN ',
                N'    INSERT (ID',
                CASE WHEN @cols <> '' THEN CONCAT(N', ', @cols) ELSE N'' END,
                N') ',
                N'VALUES (@NewID',
                CASE WHEN @values <> '' THEN CONCAT(N', ', @values) ELSE N'' END,
                N');'
            );
            
            EXEC sp_executesql 
                @sql,
                N'@NewID int, @ValuesJson nvarchar(max)',
                @NewID      = @NewID,
                @ValuesJson = @ValuesJson;

            FETCH NEXT FROM cur INTO @CurTable;
        END

        CLOSE cur;
        DEALLOCATE cur;

        -- History
        IF EXISTS (SELECT 1 FROM Meta.Classes WHERE ID = @ClassID AND HasHistory = 1)
            INSERT dbo.History (ObjID, EventID, EventTime)
            VALUES (@NewID, 1, DEFAULT);

        IF @TranStarted = 1 
          COMMIT TRANSACTION;
    END TRY
    BEGIN CATCH
        IF @TranStarted = 1 AND XACT_STATE() = -1 
          ROLLBACK TRANSACTION;
        SET @ErrorMessage = ERROR_MESSAGE();
        THROW;
    END CATCH
END
GO

--
-- Create or alter procedure [Meta].[spClassCreateTableSql]
--
GO
PRINT (N'Create or alter procedure [Meta].[spClassCreateTableSql]')
GO
CREATE OR ALTER PROCEDURE Meta.spClassCreateTableSql(@ClassID INT, @Sql NVARCHAR(4000) OUTPUT)
AS
BEGIN
  DECLARE @TableName VARCHAR(64);
  DECLARE @Fields NVARCHAR(4000);
  SET @TableName = (SELECT TableName FROM Meta.Classes WHERE ID = @ClassID);
  SET @Fields = 
  (
    SELECT 
        STRING_AGG
        (
            f.Name + ' ' + t.Name + CASE WHEN f.IsRequired = 1 THEN ' NOT NULL' ELSE '' END, 
            ', '
        )
    FROM 
        Meta.Fields f
            INNER JOIN Meta.Types t ON f.TypeID = t.ID
    WHERE f.ClassID = @ClassID
  );
  SET @Sql = 
    'CREATE TABLE [dbo].[' + @TableName + '] (' +
    'ID BIGINT NOT NULL, ' + 
    ISNULL(@Fields, '') + ', ' +
    'CONSTRAINT PK_' + @TableName + '_ID PRIMARY KEY CLUSTERED (ID), ' +
    'CONSTRAINT FK_' + @TableName + '_TObject_ID FOREIGN KEY (ID) ' +
    'REFERENCES Meta.TObject (ID) ON DELETE CASCADE)';
END;
GO

--
-- Create or alter procedure [Meta].[spClassAdded]
--
GO
PRINT (N'Create or alter procedure [Meta].[spClassAdded]')
GO
-- The goal of this procedure is to automate the
-- creation of the recursive view by creating a
-- new view joining the parent view with the
-- inherited table (that is, all the columns from
-- the parent and the additional columns in the
-- child)
-- If the class includes the definition of the
-- properties, it is also possible to create
-- the columns and their constraints.
CREATE OR ALTER PROCEDURE Meta.spClassAdded (@ClassID INT)
AS
BEGIN
  DECLARE @TableName VARCHAR(64);
  DECLARE @Fields VARCHAR(8000);
  DECLARE @ViewName VARCHAR(64);
  DECLARE @SqlCode NVARCHAR(4000);
  DECLARE @HasHistory BIT;
  DECLARE @HistoryTable VARCHAR(64);

  SELECT
    @TableName = TableName
   ,@HasHistory = HasHistory
  FROM Meta.Classes
  WHERE ID = @ClassID;

  -- If the table is not yet created, create it!
  IF NOT EXISTS (SELECT * FROM sys.tables WHERE name = @TableName)
  BEGIN
    EXEC Meta.spClassCreateTableSql @ClassID = @ClassID, @Sql = @SqlCode OUTPUT;

    EXEC sp_executesql @SqlCode;
  END;

  -- Now we can create the view

  EXEC Meta.spClassCreateViewSql @ClassID = @ClassID, @Sql = @SqlCode OUTPUT;
  EXEC sp_executesql @SqlCode;

  -- If this class will keep a history of its objects, create the history table.
  -- Its structure will be identical as the view, but adding an ID and timestamp.
  IF @HasHistory = 1
  BEGIN
    EXEC Meta.spClassCreateHistorySql @ClassID = @ClassID, @Sql = @SqlCode OUTPUT;

    EXEC sp_executesql @SqlCode;
  END
END
GO

--
-- Create table [Meta].[Operations]
--
PRINT (N'Create table [Meta].[Operations]')
GO
CREATE TABLE Meta.Operations (
  ID int IDENTITY,
  Name varchar(100) NOT NULL,
  ClassID int NOT NULL,
  PRIMARY KEY CLUSTERED (ID),
  CONSTRAINT UQ_Operations_Class_Name UNIQUE (ClassID, Name)
)
GO

--
-- Create foreign key [FK_Operations_ObjectTypes] on table [Meta].[Operations]
--
PRINT (N'Create foreign key [FK_Operations_ObjectTypes] on table [Meta].[Operations]')
GO
ALTER TABLE Meta.Operations WITH NOCHECK
  ADD CONSTRAINT FK_Operations_ObjectTypes FOREIGN KEY (ClassID) REFERENCES Meta.Classes (ID) ON DELETE CASCADE
GO

--
-- Create table [Meta].[Objs]
--
PRINT (N'Create table [Meta].[Objs]')
GO
CREATE TABLE Meta.Objs (
  ID int IDENTITY,
  ClassID int NOT NULL,
  PRIMARY KEY CLUSTERED (ID)
)
GO

--
-- Create foreign key [FK_Objs_Classes_ID] on table [Meta].[Objs]
--
PRINT (N'Create foreign key [FK_Objs_Classes_ID] on table [Meta].[Objs]')
GO
ALTER TABLE Meta.Objs
  ADD CONSTRAINT FK_Objs_Classes_ID FOREIGN KEY (ClassID) REFERENCES Meta.Classes (ID) ON DELETE CASCADE ON UPDATE CASCADE
GO

--
-- Create table [Meta].[Links]
--
PRINT (N'Create table [Meta].[Links]')
GO
CREATE TABLE Meta.Links (
  ID int IDENTITY,
  Name varchar(64) NOT NULL,
  ClassID1 int NOT NULL,
  ClassID2 int NOT NULL,
  LinkTypeID tinyint NOT NULL,
  MinIdx2 int NULL,
  MaxIdx2 int NULL,
  Indexed bit NULL DEFAULT (0),
  Owns bit NULL,
  CONSTRAINT PK_Link_ID PRIMARY KEY CLUSTERED (ID),
  CONSTRAINT CK_Link_Cardinality CHECK ([MinIdx2] IS NULL OR [MaxIdx2] IS NULL OR [MinIdx2]<=[MaxIdx2])
)
GO

--
-- Create foreign key [FK_Links_Classes1_ID] on table [Meta].[Links]
--
PRINT (N'Create foreign key [FK_Links_Classes1_ID] on table [Meta].[Links]')
GO
ALTER TABLE Meta.Links WITH NOCHECK
  ADD CONSTRAINT FK_Links_Classes1_ID FOREIGN KEY (ClassID1) REFERENCES Meta.Classes (ID)
GO

--
-- Create foreign key [FK_Links_Classes2_ID] on table [Meta].[Links]
--
PRINT (N'Create foreign key [FK_Links_Classes2_ID] on table [Meta].[Links]')
GO
ALTER TABLE Meta.Links WITH NOCHECK
  ADD CONSTRAINT FK_Links_Classes2_ID FOREIGN KEY (ClassID2) REFERENCES Meta.Classes (ID)
GO

--
-- Create foreign key [FK_Links_LinkTypes] on table [Meta].[Links]
--
PRINT (N'Create foreign key [FK_Links_LinkTypes] on table [Meta].[Links]')
GO
ALTER TABLE Meta.Links WITH NOCHECK
  ADD CONSTRAINT FK_Links_LinkTypes FOREIGN KEY (LinkTypeID) REFERENCES Meta.LinkTypes (ID) ON DELETE CASCADE
GO

--
-- Create table [Meta].[Collections]
--
PRINT (N'Create table [Meta].[Collections]')
GO
CREATE TABLE Meta.Collections (
  ID int IDENTITY,
  Name varchar(200) NOT NULL,
  Type char(1) NOT NULL,
  ItemClassID int NULL,
  ValueType char(1) NOT NULL,
  PRIMARY KEY CLUSTERED (ID),
  CHECK ([ValueType]='O' OR [ValueType]='N' OR [ValueType]='S'),
  CHECK ([Type]='T' OR [Type]='L' OR [Type]='S')
)
GO

--
-- Create foreign key [FK_Collection_ItemClass] on table [Meta].[Collections]
--
PRINT (N'Create foreign key [FK_Collection_ItemClass] on table [Meta].[Collections]')
GO
ALTER TABLE Meta.Collections
  ADD CONSTRAINT FK_Collection_ItemClass FOREIGN KEY (ItemClassID) REFERENCES Meta.Classes (ID)
GO

--
-- Create table [Meta].[CollectionItem]
--
PRINT (N'Create table [Meta].[CollectionItem]')
GO
CREATE TABLE Meta.CollectionItem (
  ID int IDENTITY,
  CollectionID int NOT NULL,
  ParentID int NULL,
  Idx int NULL,
  ValueString varchar(64) NULL,
  ValueNumber decimal(38, 10) NULL,
  ValueObjectID int NULL,
  PRIMARY KEY CLUSTERED (ID),
  CONSTRAINT CHK_Values_OnlyOne CHECK ([ValueString] IS NOT NULL AND [ValueNumber] IS NULL AND [ValueObjectID] IS NULL OR [ValueString] IS NULL AND [ValueNumber] IS NOT NULL AND [ValueObjectID] IS NULL OR [ValueString] IS NULL AND [ValueNumber] IS NULL AND [ValueObjectID] IS NOT NULL)
)
GO

--
-- Create foreign key [FK_CollectionItem_Collection] on table [Meta].[CollectionItem]
--
PRINT (N'Create foreign key [FK_CollectionItem_Collection] on table [Meta].[CollectionItem]')
GO
ALTER TABLE Meta.CollectionItem
  ADD CONSTRAINT FK_CollectionItem_Collection FOREIGN KEY (CollectionID) REFERENCES Meta.Collections (ID)
GO

--
-- Create foreign key [FK_CollectionItem_Objs_ID] on table [Meta].[CollectionItem]
--
PRINT (N'Create foreign key [FK_CollectionItem_Objs_ID] on table [Meta].[CollectionItem]')
GO
ALTER TABLE Meta.CollectionItem
  ADD CONSTRAINT FK_CollectionItem_Objs_ID FOREIGN KEY (ValueObjectID) REFERENCES Meta.Objs (ID) ON DELETE CASCADE ON UPDATE CASCADE
GO

--
-- Create foreign key [FK_CollectionItem_Parent] on table [Meta].[CollectionItem]
--
PRINT (N'Create foreign key [FK_CollectionItem_Parent] on table [Meta].[CollectionItem]')
GO
ALTER TABLE Meta.CollectionItem
  ADD CONSTRAINT FK_CollectionItem_Parent FOREIGN KEY (ParentID) REFERENCES Meta.CollectionItem (ID)
GO

--
-- Create table [Meta].[CollectionItemValue]
--
PRINT (N'Create table [Meta].[CollectionItemValue]')
GO
CREATE TABLE Meta.CollectionItemValue (
  CollectionItemID int NOT NULL,
  Name varchar(64) NOT NULL,
  ValueType char(1) NOT NULL,
  ValueString varchar(400) NULL,
  ValueNumber decimal(38, 10) NULL,
  ValueDate datetime2 NULL,
  ValueBit bit NULL,
  CONSTRAINT PK_CollectionItemValue PRIMARY KEY CLUSTERED (CollectionItemID, Name),
  CONSTRAINT CHK_CollectionItemValue_OnlyOneValue CHECK ([ValueString] IS NOT NULL AND [ValueNumber] IS NULL AND [ValueDate] IS NULL AND [ValueBit] IS NULL OR [ValueString] IS NULL AND [ValueNumber] IS NOT NULL AND [ValueDate] IS NULL AND [ValueBit] IS NULL OR [ValueString] IS NULL AND [ValueNumber] IS NULL AND [ValueDate] IS NOT NULL AND [ValueBit] IS NULL OR [ValueString] IS NULL AND [ValueNumber] IS NULL AND [ValueDate] IS NULL AND [ValueBit] IS NOT NULL),
  CONSTRAINT CHK_CollectionItemValue_ValueType CHECK ([ValueType]='B' OR [ValueType]='D' OR [ValueType]='N' OR [ValueType]='S')
)
GO

--
-- Create index [IX_CollectionItemValue_Name] on table [Meta].[CollectionItemValue]
--
PRINT (N'Create index [IX_CollectionItemValue_Name] on table [Meta].[CollectionItemValue]')
GO
CREATE INDEX IX_CollectionItemValue_Name
  ON Meta.CollectionItemValue (Name)
GO

--
-- Create foreign key [FK_CollectionItemValue_CollectionItem] on table [Meta].[CollectionItemValue]
--
PRINT (N'Create foreign key [FK_CollectionItemValue_CollectionItem] on table [Meta].[CollectionItemValue]')
GO
ALTER TABLE Meta.CollectionItemValue
  ADD CONSTRAINT FK_CollectionItemValue_CollectionItem FOREIGN KEY (CollectionItemID) REFERENCES Meta.CollectionItem (ID) ON DELETE CASCADE
GO

--
-- Create schema [RBAC]
--
PRINT (N'Create schema [RBAC]')
GO
CREATE SCHEMA RBAC AUTHORIZATION dbo
GO

--
-- Create table [RBAC].[Users]
--
PRINT (N'Create table [RBAC].[Users]')
GO
CREATE TABLE RBAC.Users (
  ID int IDENTITY,
  FullName varchar(200) NOT NULL,
  email varchar(50) NOT NULL,
  Active int NOT NULL DEFAULT (1),
  username AS (left([email],charindex('@',[email])-(1))),
  PRIMARY KEY CLUSTERED (ID)
)
GO

--
-- Create index [UQ_RBAC_Users_Email] on table [RBAC].[Users]
--
PRINT (N'Create index [UQ_RBAC_Users_Email] on table [RBAC].[Users]')
GO
CREATE UNIQUE INDEX UQ_RBAC_Users_Email
  ON RBAC.Users (email)
GO

--
-- Create or alter procedure [RBAC].[spGetUserIdByEmail]
--
GO
PRINT (N'Create or alter procedure [RBAC].[spGetUserIdByEmail]')
GO
/* 
	Author	Inty Saez 
	Date 	04/13/2026
	Subject	Get UserId by User's email  
*/

CREATE OR ALTER PROCEDURE RBAC.spGetUserIdByEmail
    @Email  NVARCHAR(100),
    @UserId INT = NULL OUTPUT
AS
BEGIN
    SET NOCOUNT ON;

    SELECT @UserId = u.ID
    FROM   RBAC.Users u
    WHERE  u.email = @Email
      AND  u.Active = 1;
    -- Returns NULL in @UserId if no active user found.
END;
GO

--
-- Create table [RBAC].[Roles]
--
PRINT (N'Create table [RBAC].[Roles]')
GO
CREATE TABLE RBAC.Roles (
  ID int IDENTITY,
  Name varchar(200) NOT NULL,
  BypassScope bit NOT NULL CONSTRAINT DF_Roles_BypassScope DEFAULT (0),
  PRIMARY KEY CLUSTERED (ID),
  UNIQUE (Name)
)
GO

--
-- Create table [RBAC].[BypassScopeApprovals]
--
PRINT (N'Create table [RBAC].[BypassScopeApprovals]')
GO
CREATE TABLE RBAC.BypassScopeApprovals (
  ApprovalID bigint IDENTITY,
  UserID int NOT NULL,
  RoleID int NOT NULL,
  RequestedByUserID int NOT NULL,
  ApprovedByUserID int NULL,
  Status varchar(16) NOT NULL CONSTRAINT DF_BypassScopeApprovals_Status DEFAULT ('PENDING'),
  Reason varchar(500) NOT NULL,
  TicketRef varchar(128) NULL,
  CreatedAtUtc datetime2(3) NOT NULL CONSTRAINT DF_BypassScopeApprovals_CreatedAtUtc DEFAULT (sysutcdatetime()),
  ApprovedAtUtc datetime2(3) NULL,
  ExpiresAtUtc datetime2(3) NULL,
  CONSTRAINT PK_RBAC_BypassScopeApprovals PRIMARY KEY CLUSTERED (ApprovalID),
  CONSTRAINT CK_BypassScopeApprovals_Status CHECK ([Status]='REVOKED' OR [Status]='REJECTED' OR [Status]='APPROVED' OR [Status]='PENDING')
)
GO

--
-- Create index [IX_BypassScopeApprovals_UserRoleStatus] on table [RBAC].[BypassScopeApprovals]
--
PRINT (N'Create index [IX_BypassScopeApprovals_UserRoleStatus] on table [RBAC].[BypassScopeApprovals]')
GO
CREATE INDEX IX_BypassScopeApprovals_UserRoleStatus
  ON RBAC.BypassScopeApprovals (UserID, RoleID, Status, ExpiresAtUtc)
GO

--
-- Create foreign key [FK_BypassScopeApprovals_ApprovedByUserID] on table [RBAC].[BypassScopeApprovals]
--
PRINT (N'Create foreign key [FK_BypassScopeApprovals_ApprovedByUserID] on table [RBAC].[BypassScopeApprovals]')
GO
ALTER TABLE RBAC.BypassScopeApprovals
  ADD CONSTRAINT FK_BypassScopeApprovals_ApprovedByUserID FOREIGN KEY (ApprovedByUserID) REFERENCES RBAC.Users (ID)
GO

--
-- Create foreign key [FK_BypassScopeApprovals_RequestedByUserID] on table [RBAC].[BypassScopeApprovals]
--
PRINT (N'Create foreign key [FK_BypassScopeApprovals_RequestedByUserID] on table [RBAC].[BypassScopeApprovals]')
GO
ALTER TABLE RBAC.BypassScopeApprovals
  ADD CONSTRAINT FK_BypassScopeApprovals_RequestedByUserID FOREIGN KEY (RequestedByUserID) REFERENCES RBAC.Users (ID)
GO

--
-- Create foreign key [FK_BypassScopeApprovals_RoleID] on table [RBAC].[BypassScopeApprovals]
--
PRINT (N'Create foreign key [FK_BypassScopeApprovals_RoleID] on table [RBAC].[BypassScopeApprovals]')
GO
ALTER TABLE RBAC.BypassScopeApprovals
  ADD CONSTRAINT FK_BypassScopeApprovals_RoleID FOREIGN KEY (RoleID) REFERENCES RBAC.Roles (ID)
GO

--
-- Create foreign key [FK_BypassScopeApprovals_UserID] on table [RBAC].[BypassScopeApprovals]
--
PRINT (N'Create foreign key [FK_BypassScopeApprovals_UserID] on table [RBAC].[BypassScopeApprovals]')
GO
ALTER TABLE RBAC.BypassScopeApprovals
  ADD CONSTRAINT FK_BypassScopeApprovals_UserID FOREIGN KEY (UserID) REFERENCES RBAC.Users (ID)
GO

--
-- Create table [RBAC].[Permissions]
--
PRINT (N'Create table [RBAC].[Permissions]')
GO
CREATE TABLE RBAC.Permissions (
  ID int IDENTITY,
  Name varchar(200) NOT NULL,
  ObjID int NOT NULL,
  OperationID int NOT NULL,
  PRIMARY KEY CLUSTERED (ID),
  CONSTRAINT UQ_Permissions_Object_Operation UNIQUE (ObjID, OperationID)
)
GO

--
-- Create index [IX_Permissions_Object] on table [RBAC].[Permissions]
--
PRINT (N'Create index [IX_Permissions_Object] on table [RBAC].[Permissions]')
GO
CREATE INDEX IX_Permissions_Object
  ON RBAC.Permissions (ObjID)
GO

--
-- Create foreign key [FK_Permissions_Objects] on table [RBAC].[Permissions]
--
PRINT (N'Create foreign key [FK_Permissions_Objects] on table [RBAC].[Permissions]')
GO
ALTER TABLE RBAC.Permissions WITH NOCHECK
  ADD CONSTRAINT FK_Permissions_Objects FOREIGN KEY (ObjID) REFERENCES Meta.Objs (ID) ON DELETE CASCADE
GO

--
-- Create foreign key [FK_Permissions_Operations] on table [RBAC].[Permissions]
--
PRINT (N'Create foreign key [FK_Permissions_Operations] on table [RBAC].[Permissions]')
GO
ALTER TABLE RBAC.Permissions
  ADD CONSTRAINT FK_Permissions_Operations FOREIGN KEY (OperationID) REFERENCES Meta.Operations (ID)
GO

--
-- Create table [RBAC].[Role_Permissions]
--
PRINT (N'Create table [RBAC].[Role_Permissions]')
GO
CREATE TABLE RBAC.Role_Permissions (
  RoleID int NOT NULL,
  PermissionID int NOT NULL,
  CONSTRAINT PK_Role_Permissions PRIMARY KEY CLUSTERED (RoleID, PermissionID)
)
GO

--
-- Create index [IX_RolePerm_Perm] on table [RBAC].[Role_Permissions]
--
PRINT (N'Create index [IX_RolePerm_Perm] on table [RBAC].[Role_Permissions]')
GO
CREATE INDEX IX_RolePerm_Perm
  ON RBAC.Role_Permissions (PermissionID)
GO

--
-- Create foreign key [FK_Role_Permissions_Permissions] on table [RBAC].[Role_Permissions]
--
PRINT (N'Create foreign key [FK_Role_Permissions_Permissions] on table [RBAC].[Role_Permissions]')
GO
ALTER TABLE RBAC.Role_Permissions
  ADD CONSTRAINT FK_Role_Permissions_Permissions FOREIGN KEY (PermissionID) REFERENCES RBAC.Permissions (ID) ON DELETE CASCADE ON UPDATE CASCADE
GO

--
-- Create foreign key [FK_Role_Permissions_Roles] on table [RBAC].[Role_Permissions]
--
PRINT (N'Create foreign key [FK_Role_Permissions_Roles] on table [RBAC].[Role_Permissions]')
GO
ALTER TABLE RBAC.Role_Permissions
  ADD CONSTRAINT FK_Role_Permissions_Roles FOREIGN KEY (RoleID) REFERENCES RBAC.Roles (ID) ON DELETE CASCADE ON UPDATE CASCADE
GO

--
-- Create table [RBAC].[IdentityProviders]
--
PRINT (N'Create table [RBAC].[IdentityProviders]')
GO
CREATE TABLE RBAC.IdentityProviders (
  ProviderID int IDENTITY,
  Name varchar(128) NOT NULL,
  ProviderType varchar(32) NOT NULL,
  Issuer nvarchar(512) NOT NULL,
  AllowedDomainPattern varchar(255) NULL,
  Active bit NOT NULL CONSTRAINT DF_RBAC_IdentityProviders_Active DEFAULT (1),
  CreatedAtUtc datetime2(3) NOT NULL CONSTRAINT DF_RBAC_IdentityProviders_CreatedAtUtc DEFAULT (sysutcdatetime()),
  CONSTRAINT PK_RBAC_IdentityProviders PRIMARY KEY CLUSTERED (ProviderID),
  CONSTRAINT UQ_RBAC_IdentityProviders_Issuer UNIQUE (Issuer),
  CONSTRAINT CK_RBAC_IdentityProviders_ProviderType CHECK ([ProviderType]='OIDC_CUSTOM' OR [ProviderType]='ENTRA' OR [ProviderType]='GOOGLE')
)
GO

--
-- Create table [RBAC].[ExternalIdentities]
--
PRINT (N'Create table [RBAC].[ExternalIdentities]')
GO
CREATE TABLE RBAC.ExternalIdentities (
  ID int IDENTITY,
  UserID int NOT NULL,
  ProviderID int NOT NULL,
  Subject nvarchar(512) NOT NULL,
  EmailSnapshot varchar(255) NULL,
  Active bit NOT NULL CONSTRAINT DF_RBAC_ExternalIdentities_Active DEFAULT (1),
  CreatedAtUtc datetime2(3) NOT NULL CONSTRAINT DF_RBAC_ExternalIdentities_CreatedAtUtc DEFAULT (sysutcdatetime()),
  LastLoginAtUtc datetime2(3) NULL,
  CONSTRAINT PK_RBAC_ExternalIdentities PRIMARY KEY CLUSTERED (ID),
  CONSTRAINT UQ_RBAC_ExternalIdentities_Provider_Subject UNIQUE (ProviderID, Subject)
)
GO

--
-- Create index [IX_RBAC_ExternalIdentities_UserID] on table [RBAC].[ExternalIdentities]
--
PRINT (N'Create index [IX_RBAC_ExternalIdentities_UserID] on table [RBAC].[ExternalIdentities]')
GO
CREATE INDEX IX_RBAC_ExternalIdentities_UserID
  ON RBAC.ExternalIdentities (UserID, Active)
GO

--
-- Create foreign key [FK_RBAC_ExternalIdentities_ProviderID] on table [RBAC].[ExternalIdentities]
--
PRINT (N'Create foreign key [FK_RBAC_ExternalIdentities_ProviderID] on table [RBAC].[ExternalIdentities]')
GO
ALTER TABLE RBAC.ExternalIdentities
  ADD CONSTRAINT FK_RBAC_ExternalIdentities_ProviderID FOREIGN KEY (ProviderID) REFERENCES RBAC.IdentityProviders (ProviderID)
GO

--
-- Create foreign key [FK_RBAC_ExternalIdentities_UserID] on table [RBAC].[ExternalIdentities]
--
PRINT (N'Create foreign key [FK_RBAC_ExternalIdentities_UserID] on table [RBAC].[ExternalIdentities]')
GO
ALTER TABLE RBAC.ExternalIdentities
  ADD CONSTRAINT FK_RBAC_ExternalIdentities_UserID FOREIGN KEY (UserID) REFERENCES RBAC.Users (ID)
GO

--
-- Create table [RBAC].[Groups]
--
PRINT (N'Create table [RBAC].[Groups]')
GO
CREATE TABLE RBAC.Groups (
  ID int IDENTITY,
  Name varchar(200) NOT NULL,
  PRIMARY KEY CLUSTERED (ID),
  UNIQUE (Name)
)
GO

--
-- Create table [RBAC].[Group_Users]
--
PRINT (N'Create table [RBAC].[Group_Users]')
GO
CREATE TABLE RBAC.Group_Users (
  GroupID int NOT NULL,
  UserID int NOT NULL,
  AddedAt datetime2(3) NOT NULL DEFAULT (sysutcdatetime()),
  CONSTRAINT PK_GroupMembers PRIMARY KEY CLUSTERED (GroupID, UserID)
)
GO

--
-- Create index [IX_GroupMembers_User] on table [RBAC].[Group_Users]
--
PRINT (N'Create index [IX_GroupMembers_User] on table [RBAC].[Group_Users]')
GO
CREATE INDEX IX_GroupMembers_User
  ON RBAC.Group_Users (UserID)
GO

--
-- Create foreign key [FK_GroupMembers_Groups] on table [RBAC].[Group_Users]
--
PRINT (N'Create foreign key [FK_GroupMembers_Groups] on table [RBAC].[Group_Users]')
GO
ALTER TABLE RBAC.Group_Users
  ADD CONSTRAINT FK_GroupMembers_Groups FOREIGN KEY (GroupID) REFERENCES RBAC.Groups (ID) ON DELETE CASCADE ON UPDATE CASCADE
GO

--
-- Create foreign key [FK_GroupMembers_Users] on table [RBAC].[Group_Users]
--
PRINT (N'Create foreign key [FK_GroupMembers_Users] on table [RBAC].[Group_Users]')
GO
ALTER TABLE RBAC.Group_Users
  ADD CONSTRAINT FK_GroupMembers_Users FOREIGN KEY (UserID) REFERENCES RBAC.Users (ID) ON DELETE CASCADE ON UPDATE CASCADE
GO

--
-- Create table [RBAC].[Group_Roles]
--
PRINT (N'Create table [RBAC].[Group_Roles]')
GO
CREATE TABLE RBAC.Group_Roles (
  GroupID int NOT NULL,
  RoleID int NOT NULL,
  CONSTRAINT PK_Group_Roles PRIMARY KEY CLUSTERED (GroupID, RoleID)
)
GO

--
-- Create index [IX_Group_Roles_Role] on table [RBAC].[Group_Roles]
--
PRINT (N'Create index [IX_Group_Roles_Role] on table [RBAC].[Group_Roles]')
GO
CREATE INDEX IX_Group_Roles_Role
  ON RBAC.Group_Roles (RoleID)
GO

--
-- Create foreign key [FK_Group_Roles_Groups] on table [RBAC].[Group_Roles]
--
PRINT (N'Create foreign key [FK_Group_Roles_Groups] on table [RBAC].[Group_Roles]')
GO
ALTER TABLE RBAC.Group_Roles
  ADD CONSTRAINT FK_Group_Roles_Groups FOREIGN KEY (GroupID) REFERENCES RBAC.Groups (ID) ON DELETE CASCADE ON UPDATE CASCADE
GO

--
-- Create foreign key [FK_Group_Roles_Roles] on table [RBAC].[Group_Roles]
--
PRINT (N'Create foreign key [FK_Group_Roles_Roles] on table [RBAC].[Group_Roles]')
GO
ALTER TABLE RBAC.Group_Roles
  ADD CONSTRAINT FK_Group_Roles_Roles FOREIGN KEY (RoleID) REFERENCES RBAC.Roles (ID) ON DELETE CASCADE ON UPDATE CASCADE
GO

--
-- Create or alter procedure [RBAC].[spGetUserRoles]
--
GO
PRINT (N'Create or alter procedure [RBAC].[spGetUserRoles]')
GO

/*
	Author	Inty Saez 
	Date 	04/13/2026
	Subject Get User's Role 
*/


CREATE OR ALTER PROCEDURE RBAC.spGetUserRoles
    @UserId INT
AS
BEGIN
    SET NOCOUNT ON;

    SELECT DISTINCT
        @UserId     AS UserId,
        r.Name      AS Role,
        r.ID        AS RoleId
    FROM   RBAC.Group_Users  gu
    JOIN   RBAC.Group_Roles  gr  ON gr.GroupID = gu.GroupID
    JOIN   RBAC.Roles        r   ON r.ID       = gr.RoleID
    WHERE  gu.UserID = @UserId
    ORDER BY r.Name;
END;
GO

--
-- Create schema [portal]
--
PRINT (N'Create schema [portal]')
GO
CREATE SCHEMA portal AUTHORIZATION dbo
GO

--
-- Create or alter procedure [portal].[spGetCollectionItems]
--
GO
PRINT (N'Create or alter procedure [portal].[spGetCollectionItems]')
GO
-- ============================================================
-- Author	Inty Saez 
-- portal.spGetCollectionItems
-- Generic SP to retrieve any Collection with its attributes pivoted.
-- Designed for Master/Details consumption from Delphi DataModules.
--
-- IMPORTANT: This procedure lives in the [portal] schema (not e_portal).
--
-- Usage from DataModule:
--   qry.SQL.Text := 'EXEC portal.spGetCollectionItems :CollectionName';
--   qry.ParamByName('CollectionName').AsString := 'Gleason Score';
--   qry.Open;
-- ============================================================
CREATE OR ALTER PROCEDURE portal.spGetCollectionItems
    @CollectionName  varchar(200)
AS
BEGIN
    SET NOCOUNT ON;

    DECLARE @CollID int;

    SELECT @CollID = ID 
    FROM [Meta].[Collections] 
    WHERE [Name] = @CollectionName;

    IF @CollID IS NULL
    BEGIN
        RAISERROR('Collection "%s" not found.', 16, 1, @CollectionName);
        RETURN;
    END;

    -- Pre-compute distinct attributes with their dominant ValueType
    -- (we take the most common type in case of mixed usage)
    IF OBJECT_ID('tempdb..#CollectionAttrs') IS NOT NULL DROP TABLE #CollectionAttrs;

    SELECT 
        civ.Name,
        MAX(civ.ValueType) AS ValueType   -- safe: all rows for same Name should have same type
    INTO #CollectionAttrs
    FROM [Meta].[CollectionItemValue] civ
    INNER JOIN [Meta].[CollectionItem] ci ON ci.ID = civ.CollectionItemID
    WHERE ci.CollectionID = @CollID
    GROUP BY civ.Name;

    DECLARE @ExtendedAttrs nvarchar(max) = N'';

    -- Build the pivot expression list
    SELECT @ExtendedAttrs = STRING_AGG(
        N'MAX(CASE WHEN civ.Name = ' + QUOTENAME(Name, '''') + 
        N' THEN ' +
        CASE 
            WHEN ValueType = 'N' THEN 'civ.ValueNumber'
            WHEN ValueType = 'D' THEN 'civ.ValueDate'
            WHEN ValueType = 'B' THEN 'civ.ValueBit'
            ELSE 'civ.ValueString'
        END +
        N' END) AS ' + QUOTENAME(Name),
        ', '
    )
    FROM #CollectionAttrs;

    IF @ExtendedAttrs IS NULL OR @ExtendedAttrs = ''
    BEGIN
        -- No attributes defined yet → return base columns only
        SELECT 
            ci.ID               AS CollectionItemID,
            ci.ParentID,
            ci.Idx,
            ci.ValueString      AS PrimaryValueString,
            ci.ValueNumber      AS PrimaryValueNumber,
            ci.ValueObjectID
        FROM [Meta].[CollectionItem] ci
        WHERE ci.CollectionID = @CollID
        ORDER BY ci.Idx;
        RETURN;
    END;

    -- Final dynamic query
    DECLARE @SQL nvarchar(max) = N'
        SELECT 
            ci.ID               AS CollectionItemID,
            ci.ParentID,
            ci.Idx,
            ci.ValueString      AS PrimaryValueString,
            ci.ValueNumber      AS PrimaryValueNumber,
            ci.ValueObjectID,
            ' + @ExtendedAttrs + N'
        FROM [Meta].[CollectionItem] ci
        LEFT JOIN [Meta].[CollectionItemValue] civ ON civ.CollectionItemID = ci.ID
        WHERE ci.CollectionID = @CollID
        GROUP BY 
            ci.ID, ci.ParentID, ci.Idx, ci.ValueString, ci.ValueNumber, ci.ValueObjectID
        ORDER BY ci.Idx;
    ';

    EXEC sp_executesql @SQL, N'@CollID int', @CollID;

    DROP TABLE #CollectionAttrs;
END
GO

--
-- Create table [portal].[Objs]
--
PRINT (N'Create table [portal].[Objs]')
GO
CREATE TABLE portal.Objs (
  ObjID int IDENTITY,
  MetaObjID int NOT NULL,
  CONSTRAINT PK_Objs PRIMARY KEY CLUSTERED (ObjID, MetaObjID)
)
GO

--
-- Create foreign key [FK_Objs_Objs_ID] on table [portal].[Objs]
--
PRINT (N'Create foreign key [FK_Objs_Objs_ID] on table [portal].[Objs]')
GO
ALTER TABLE portal.Objs
  ADD CONSTRAINT FK_Objs_Objs_ID FOREIGN KEY (MetaObjID) REFERENCES Meta.Objs (ID) ON DELETE CASCADE ON UPDATE CASCADE
GO

--
-- Create table [portal].[NodeInfos]
--
PRINT (N'Create table [portal].[NodeInfos]')
GO
CREATE TABLE portal.NodeInfos (
  Id int IDENTITY,
  Name varchar(128) NOT NULL,
  Singleton bit NOT NULL DEFAULT (1),
  TypeId tinyint NULL CONSTRAINT DC_NodeInfos_TypeId DEFAULT (1),
  FrameKey nvarchar(100) NULL,
  CONSTRAINT PK_NodeInfos_Id PRIMARY KEY CLUSTERED (Id)
)
GO

--
-- Create table [portal].[NavTree]
--
PRINT (N'Create table [portal].[NavTree]')
GO
CREATE TABLE portal.NavTree (
  ID int IDENTITY,
  ParentID int NULL,
  Caption varchar(50) NOT NULL,
  Seq smallint NOT NULL,
  InfoID int NULL,
  NodePath varchar(2048) NULL,
  SortPath varchar(2048) NULL,
  CONSTRAINT PK_NavTree PRIMARY KEY CLUSTERED (ID)
)
GO

--
-- Create index [IX_NavTree_SortPath] on table [portal].[NavTree]
--
PRINT (N'Create index [IX_NavTree_SortPath] on table [portal].[NavTree]')
GO
CREATE INDEX IX_NavTree_SortPath
  ON portal.NavTree (SortPath)
  WHERE ([SortPath] IS NOT NULL)
GO

--
-- Create index [UX_NavTree_NodePath] on table [portal].[NavTree]
--
PRINT (N'Create index [UX_NavTree_NodePath] on table [portal].[NavTree]')
GO
CREATE UNIQUE INDEX UX_NavTree_NodePath
  ON portal.NavTree (NodePath)
  WHERE ([NodePath] IS NOT NULL)
GO

--
-- Create foreign key [FK_NavTree_NodeInfos_Id] on table [portal].[NavTree]
--
PRINT (N'Create foreign key [FK_NavTree_NodeInfos_Id] on table [portal].[NavTree]')
GO
ALTER TABLE portal.NavTree WITH NOCHECK
  ADD CONSTRAINT FK_NavTree_NodeInfos_Id FOREIGN KEY (InfoID) REFERENCES portal.NodeInfos (Id)
GO

--
-- Create or alter procedure [portal].[spNavTreeMove]
--
GO
PRINT (N'Create or alter procedure [portal].[spNavTreeMove]')
GO

/*
    Author  Inty Saez
    Date    05/07/2026
    Subject Move NavTree Node (rebuild NodePath/SortPath for subtree)
*/
CREATE OR ALTER PROCEDURE portal.spNavTreeMove
    @NodeID INT,
    @NewParentID INT = -1,
    @NewSeq SMALLINT = NULL
AS
BEGIN
    SET NOCOUNT ON;
    SET XACT_ABORT ON;

    IF @NodeID IS NULL OR @NodeID <= 0
        THROW 50010, 'ValidationError: NodeID is required.', 1;

    IF @NewParentID IS NULL OR @NewParentID = 0 OR @NewParentID < -1
        THROW 50023, 'ValidationError: NewParentID must be -1 (root) or a valid node ID.', 1;

    IF @NewParentID = @NodeID
        THROW 50011, 'BusinessRule: A node cannot be its own parent.', 1;

    DECLARE @CurrentParentID INT;
    DECLARE @CurrentSeq SMALLINT;
    DECLARE @OldNodePath VARCHAR(2048);
    DECLARE @OldSortPath VARCHAR(2048);
    DECLARE @ScopeRootPath VARCHAR(2048);
    DECLARE @RootSlashPos INT;

    DECLARE @ParentNodePath VARCHAR(2048) = NULL;
    DECLARE @ParentSortPath VARCHAR(2048) = NULL;
    DECLARE @EffectiveNewSeq SMALLINT;
    DECLARE @NewNodePath VARCHAR(2048);
    DECLARE @NewSortPath VARCHAR(2048);
    DECLARE @SortSegment VARCHAR(5);

    BEGIN TRY
        BEGIN TRANSACTION;

        SELECT
            @CurrentParentID = n.ParentID,
            @CurrentSeq = n.Seq,
            @OldNodePath = n.NodePath,
            @OldSortPath = n.SortPath
        FROM [portal].[NavTree] n WITH (UPDLOCK, HOLDLOCK)
        WHERE n.ID = @NodeID;

        IF @OldNodePath IS NULL
            THROW 50012, 'BusinessRule: Node does not exist or does not have a NodePath.', 1;

        IF @OldSortPath IS NULL OR LTRIM(RTRIM(@OldSortPath)) = ''
            THROW 50013, 'BusinessRule: Node does not have a SortPath.', 1;

        -- Extract root path segment from current node path (e.g., '/1' from '/1/22/23').
        SET @RootSlashPos = CHARINDEX('/', @OldNodePath, 2);
        SET @ScopeRootPath = CASE
                                WHEN @RootSlashPos > 0 THEN LEFT(@OldNodePath, @RootSlashPos - 1)
                                ELSE @OldNodePath
                             END;

        -- Validate new parent node exists and has a SortPath.
        IF @NewParentID <> -1
        BEGIN
            SELECT
                @ParentNodePath = p.NodePath,
                @ParentSortPath = p.SortPath
            FROM [portal].[NavTree] p WITH (UPDLOCK, HOLDLOCK)
            WHERE p.ID = @NewParentID;

            IF @ParentNodePath IS NULL
                THROW 50014, 'BusinessRule: Target parent does not exist or does not have a NodePath.', 1;

            -- Validate new parent has a SortPath.
            IF @ParentSortPath IS NULL OR LTRIM(RTRIM(@ParentSortPath)) = ''
                THROW 50015, 'BusinessRule: Target parent does not have a SortPath.', 1;

            -- Validate new parent is not the same as the old parent or a descendant of the old parent.
            IF @ParentNodePath = @OldNodePath OR @ParentNodePath LIKE @OldNodePath + '/%'
                THROW 50016, 'BusinessRule: Cannot move node under itself or its descendants.', 1;

            IF NOT (@ParentNodePath = @ScopeRootPath OR @ParentNodePath LIKE @ScopeRootPath + '/%')
                THROW 50020, 'BusinessRule: Cannot move node outside its scope tree.', 1;
        END

        IF @OldNodePath = @ScopeRootPath AND @NewParentID <> -1
            THROW 50021, 'BusinessRule: Scope root node cannot be moved under another parent.', 1;

        IF @NewParentID = -1 AND @OldNodePath <> @ScopeRootPath
            THROW 50022, 'BusinessRule: Cannot detach a scope node from its root tree.', 1;

        SET @EffectiveNewSeq = ISNULL(@NewSeq, @CurrentSeq);
        SET @SortSegment = RIGHT(REPLICATE('0', 5) + CONVERT(VARCHAR(5), @EffectiveNewSeq), 5);

        -- Early exit when parent and effective sequence are unchanged (no-op move request).
        IF @NewParentID = @CurrentParentID AND @EffectiveNewSeq = @CurrentSeq
        BEGIN
            COMMIT TRANSACTION;

            SELECT
                @NodeID AS NodeID,
                @CurrentParentID AS ParentID,
                @CurrentSeq AS Seq,
                @OldNodePath AS NodePath,
                @OldSortPath AS SortPath;
            RETURN 0;
        END

        
        IF @NewParentID = -1
        BEGIN
            SET @NewNodePath = '/' + CONVERT(VARCHAR(20), @NodeID);
            SET @NewSortPath = '/' + @SortSegment;
        END
        ELSE
        BEGIN
            SET @NewNodePath = @ParentNodePath + '/' + CONVERT(VARCHAR(20), @NodeID);
            SET @NewSortPath = @ParentSortPath + '/' + @SortSegment;
        END

        IF LEN(@NewNodePath) > 2048 OR LEN(@NewSortPath) > 2048
            THROW 50017, 'BusinessRule: New NodePath/SortPath length exceeds 2048.', 1;

        IF EXISTS
        (
            SELECT 1
            FROM [portal].[NavTree] d
            WHERE d.NodePath LIKE @OldNodePath + '/%'
              AND
              (
                  LEN(@NewNodePath + SUBSTRING(d.NodePath, LEN(@OldNodePath) + 1, 2048)) > 2048
                  OR LEN(@NewSortPath + SUBSTRING(d.SortPath, LEN(@OldSortPath) + 1, 2048)) > 2048
              )
        )
            THROW 50018, 'BusinessRule: Move would exceed NodePath/SortPath length for descendants.', 1;

        IF EXISTS
        (
            SELECT 1
            FROM [portal].[NavTree] d
            WHERE d.NodePath LIKE @OldNodePath + '/%'
              AND (d.SortPath IS NULL OR LTRIM(RTRIM(d.SortPath)) = '')
        )
            THROW 50019, 'BusinessRule: Cannot move subtree with descendants missing SortPath.', 1;

        UPDATE [portal].[NavTree]
        SET ParentID = @NewParentID,
            Seq = @EffectiveNewSeq,
            NodePath = @NewNodePath,
            SortPath = @NewSortPath
        WHERE ID = @NodeID;

        UPDATE d
        SET
            d.NodePath = @NewNodePath + SUBSTRING(d.NodePath, LEN(@OldNodePath) + 1, 2048),
            d.SortPath = @NewSortPath + SUBSTRING(d.SortPath, LEN(@OldSortPath) + 1, 2048)
        FROM [portal].[NavTree] d
        WHERE d.NodePath LIKE @OldNodePath + '/%';

        COMMIT TRANSACTION;

        SELECT
            @NodeID AS NodeID,
            @NewParentID AS ParentID,
            @EffectiveNewSeq AS Seq,
            @NewNodePath AS NodePath,
            @NewSortPath AS SortPath;
    END TRY
    BEGIN CATCH
        IF XACT_STATE() <> 0 ROLLBACK TRANSACTION;
        THROW;
    END CATCH
END
GO

--
-- Create or alter procedure [portal].[spNavTreeDeleteNode]
--
GO
PRINT (N'Create or alter procedure [portal].[spNavTreeDeleteNode]')
GO

/*
    Author  Inty Saez
    Date    05/07/2026
    Subject Delete NavTree Node (cascade subtree delete by NodePath)
*/
CREATE OR ALTER PROCEDURE portal.spNavTreeDeleteNode
    @NodeID INT
AS
BEGIN
    SET NOCOUNT ON;
    SET XACT_ABORT ON;

    IF @NodeID IS NULL OR @NodeID <= 0
        THROW 50030, 'ValidationError: NodeID is required.', 1;

    DECLARE @NodePath VARCHAR(2048);
    DECLARE @ParentID INT;
    DECLARE @DeletedRows INT;

    BEGIN TRY
        BEGIN TRANSACTION;

        SELECT
            @NodePath = n.NodePath,
            @ParentID = n.ParentID
        FROM [portal].[NavTree] n WITH (UPDLOCK, HOLDLOCK)
        WHERE n.ID = @NodeID;

        IF @NodePath IS NULL
            THROW 50031, 'BusinessRule: Node does not exist or does not have a NodePath.', 1;

        IF @ParentID = -1
            THROW 50032, 'BusinessRule: Scope root node cannot be deleted.', 1;

        DELETE n
        FROM [portal].[NavTree] n
        WHERE n.NodePath = @NodePath
           OR n.NodePath LIKE @NodePath + '/%';

        SET @DeletedRows = @@ROWCOUNT;

        COMMIT TRANSACTION;

        SELECT
            @NodeID AS DeletedNodeID,
            @NodePath AS DeletedNodePath,
            @DeletedRows AS DeletedRows;
    END TRY
    BEGIN CATCH
        IF XACT_STATE() <> 0 ROLLBACK TRANSACTION;
        THROW;
    END CATCH
END
GO

--
-- Create table [portal].[Labs]
--
PRINT (N'Create table [portal].[Labs]')
GO
CREATE TABLE portal.Labs (
  ID int IDENTITY,
  Name varchar(128) NOT NULL,
  CONSTRAINT PK_Labs_1 PRIMARY KEY CLUSTERED (ID)
)
GO

--
-- Create table [portal].[Batches]
--
PRINT (N'Create table [portal].[Batches]')
GO
CREATE TABLE portal.Batches (
  ID int IDENTITY,
  Name varchar(128) NULL,
  LabID int NULL,
  CONSTRAINT PK_Batches PRIMARY KEY CLUSTERED (ID)
)
GO

--
-- Create foreign key [FK_Batches_Labs_ID] on table [portal].[Batches]
--
PRINT (N'Create foreign key [FK_Batches_Labs_ID] on table [portal].[Batches]')
GO
ALTER TABLE portal.Batches
  ADD CONSTRAINT FK_Batches_Labs_ID FOREIGN KEY (LabID) REFERENCES portal.Labs (ID) ON DELETE CASCADE ON UPDATE CASCADE
GO

--
-- Create table [portal].[Institutions]
--
PRINT (N'Create table [portal].[Institutions]')
GO
CREATE TABLE portal.Institutions (
  ID int IDENTITY,
  Name varchar(128) NOT NULL,
  CONSTRAINT PK_Institutions_1 PRIMARY KEY CLUSTERED (ID)
)
GO

--
-- Create table [RBAC].[Scopes]
--
PRINT (N'Create table [RBAC].[Scopes]')
GO
CREATE TABLE RBAC.Scopes (
  ScopeID int IDENTITY,
  ScopeType varchar(32) NOT NULL,
  Name varchar(200) NOT NULL,
  InstitutionID int NULL,
  LabID int NULL,
  OwnerUserID int NULL,
  Active bit NOT NULL CONSTRAINT DF_RBAC_Scopes_Active DEFAULT (1),
  CreatedAtUtc datetime2(3) NOT NULL CONSTRAINT DF_RBAC_Scopes_CreatedAtUtc DEFAULT (sysutcdatetime()),
  CONSTRAINT PK_RBAC_Scopes PRIMARY KEY CLUSTERED (ScopeID),
  CONSTRAINT CK_RBAC_Scopes_EntityByType CHECK ([ScopeType]='INSTITUTION' AND [InstitutionID] IS NOT NULL AND [LabID] IS NULL AND [OwnerUserID] IS NULL OR [ScopeType]='LAB' AND [LabID] IS NOT NULL AND [InstitutionID] IS NULL AND [OwnerUserID] IS NULL OR [ScopeType]='INDEPENDENT_USER' AND [OwnerUserID] IS NOT NULL AND [InstitutionID] IS NULL AND [LabID] IS NULL),
  CONSTRAINT CK_RBAC_Scopes_ScopeType CHECK ([ScopeType]='INDEPENDENT_USER' OR [ScopeType]='LAB' OR [ScopeType]='INSTITUTION')
)
GO

--
-- Create index [IX_RBAC_Scopes_InstitutionID] on table [RBAC].[Scopes]
--
PRINT (N'Create index [IX_RBAC_Scopes_InstitutionID] on table [RBAC].[Scopes]')
GO
CREATE INDEX IX_RBAC_Scopes_InstitutionID
  ON RBAC.Scopes (InstitutionID)
GO

--
-- Create index [IX_RBAC_Scopes_LabID] on table [RBAC].[Scopes]
--
PRINT (N'Create index [IX_RBAC_Scopes_LabID] on table [RBAC].[Scopes]')
GO
CREATE INDEX IX_RBAC_Scopes_LabID
  ON RBAC.Scopes (LabID)
GO

--
-- Create index [IX_RBAC_Scopes_OwnerUserID] on table [RBAC].[Scopes]
--
PRINT (N'Create index [IX_RBAC_Scopes_OwnerUserID] on table [RBAC].[Scopes]')
GO
CREATE INDEX IX_RBAC_Scopes_OwnerUserID
  ON RBAC.Scopes (OwnerUserID)
GO

--
-- Create index [UX_RBAC_Scopes_IndependentUser] on table [RBAC].[Scopes]
--
PRINT (N'Create index [UX_RBAC_Scopes_IndependentUser] on table [RBAC].[Scopes]')
GO
CREATE UNIQUE INDEX UX_RBAC_Scopes_IndependentUser
  ON RBAC.Scopes (OwnerUserID)
  WHERE ([ScopeType]='INDEPENDENT_USER')
GO

--
-- Create index [UX_RBAC_Scopes_Institution] on table [RBAC].[Scopes]
--
PRINT (N'Create index [UX_RBAC_Scopes_Institution] on table [RBAC].[Scopes]')
GO
CREATE UNIQUE INDEX UX_RBAC_Scopes_Institution
  ON RBAC.Scopes (InstitutionID)
  WHERE ([ScopeType]='INSTITUTION')
GO

--
-- Create index [UX_RBAC_Scopes_Lab] on table [RBAC].[Scopes]
--
PRINT (N'Create index [UX_RBAC_Scopes_Lab] on table [RBAC].[Scopes]')
GO
CREATE UNIQUE INDEX UX_RBAC_Scopes_Lab
  ON RBAC.Scopes (LabID)
  WHERE ([ScopeType]='LAB')
GO

--
-- Create foreign key [FK_RBAC_Scopes_InstitutionID] on table [RBAC].[Scopes]
--
PRINT (N'Create foreign key [FK_RBAC_Scopes_InstitutionID] on table [RBAC].[Scopes]')
GO
ALTER TABLE RBAC.Scopes
  ADD CONSTRAINT FK_RBAC_Scopes_InstitutionID FOREIGN KEY (InstitutionID) REFERENCES portal.Institutions (ID)
GO

--
-- Create foreign key [FK_RBAC_Scopes_LabID] on table [RBAC].[Scopes]
--
PRINT (N'Create foreign key [FK_RBAC_Scopes_LabID] on table [RBAC].[Scopes]')
GO
ALTER TABLE RBAC.Scopes
  ADD CONSTRAINT FK_RBAC_Scopes_LabID FOREIGN KEY (LabID) REFERENCES portal.Labs (ID)
GO

--
-- Create foreign key [FK_RBAC_Scopes_OwnerUserID] on table [RBAC].[Scopes]
--
PRINT (N'Create foreign key [FK_RBAC_Scopes_OwnerUserID] on table [RBAC].[Scopes]')
GO
ALTER TABLE RBAC.Scopes
  ADD CONSTRAINT FK_RBAC_Scopes_OwnerUserID FOREIGN KEY (OwnerUserID) REFERENCES RBAC.Users (ID)
GO

--
-- Create table [RBAC].[UserRoleGrants]
--
PRINT (N'Create table [RBAC].[UserRoleGrants]')
GO
CREATE TABLE RBAC.UserRoleGrants (
  ID int IDENTITY,
  UserID int NOT NULL,
  RoleID int NOT NULL,
  GrantType varchar(16) NOT NULL,
  ScopeID int NULL,
  Active bit NOT NULL CONSTRAINT DF_UserRoleGrants_Active DEFAULT (1),
  GrantedAtUtc datetime2(3) NOT NULL CONSTRAINT DF_UserRoleGrants_GrantedAtUtc DEFAULT (sysutcdatetime()),
  RevokedAtUtc datetime2(3) NULL,
  ApprovalID bigint NULL,
  CONSTRAINT PK_RBAC_UserRoleGrants PRIMARY KEY CLUSTERED (ID),
  CONSTRAINT CK_UserRoleGrants_GrantType CHECK ([GrantType]='SCOPED' OR [GrantType]='GLOBAL'),
  CONSTRAINT CK_UserRoleGrants_ScopeByGrantType CHECK ([GrantType]='GLOBAL' AND [ScopeID] IS NULL OR [GrantType]='SCOPED' AND [ScopeID] IS NOT NULL)
)
GO

--
-- Create index [IX_UserRoleGrants_Scope_Active] on table [RBAC].[UserRoleGrants]
--
PRINT (N'Create index [IX_UserRoleGrants_Scope_Active] on table [RBAC].[UserRoleGrants]')
GO
CREATE INDEX IX_UserRoleGrants_Scope_Active
  ON RBAC.UserRoleGrants (ScopeID, Active)
GO

--
-- Create index [IX_UserRoleGrants_User_Active] on table [RBAC].[UserRoleGrants]
--
PRINT (N'Create index [IX_UserRoleGrants_User_Active] on table [RBAC].[UserRoleGrants]')
GO
CREATE INDEX IX_UserRoleGrants_User_Active
  ON RBAC.UserRoleGrants (UserID, Active)
GO

--
-- Create index [UX_UserRoleGrants_ActiveGrant] on table [RBAC].[UserRoleGrants]
--
PRINT (N'Create index [UX_UserRoleGrants_ActiveGrant] on table [RBAC].[UserRoleGrants]')
GO
CREATE UNIQUE INDEX UX_UserRoleGrants_ActiveGrant
  ON RBAC.UserRoleGrants (UserID, RoleID, GrantType, ScopeID)
  WHERE ([Active]=(1))
GO

--
-- Create or alter trigger [trg_UserRoleGrants_EnforceBypassScopeApproval] on table [RBAC].[UserRoleGrants]
--
GO
PRINT (N'Create or alter trigger [trg_UserRoleGrants_EnforceBypassScopeApproval] on table [RBAC].[UserRoleGrants]')
GO
CREATE OR ALTER TRIGGER RBAC.trg_UserRoleGrants_EnforceBypassScopeApproval
ON RBAC.UserRoleGrants
AFTER INSERT, UPDATE
AS
BEGIN
    SET NOCOUNT ON;

    IF EXISTS
    (
        SELECT 1
        FROM inserted i
        INNER JOIN [RBAC].[Roles] r ON r.[ID] = i.[RoleID]
        WHERE i.[Active] = 1
          AND r.[BypassScope] = 1
          AND i.[GrantType] <> 'GLOBAL'
    )
    BEGIN
        THROW 50150, 'BypassScope roles require GLOBAL grant type.', 1;
    END

    IF EXISTS
    (
        SELECT 1
        FROM inserted i
        INNER JOIN [RBAC].[Roles] r ON r.[ID] = i.[RoleID]
        LEFT JOIN [RBAC].[BypassScopeApprovals] a
            ON a.[ApprovalID] = i.[ApprovalID]
           AND a.[UserID] = i.[UserID]
           AND a.[RoleID] = i.[RoleID]
           AND a.[Status] = 'APPROVED'
           AND (a.[ExpiresAtUtc] IS NULL OR a.[ExpiresAtUtc] >= SYSUTCDATETIME())
        WHERE i.[Active] = 1
          AND r.[BypassScope] = 1
          AND a.[ApprovalID] IS NULL
    )
    BEGIN
        THROW 50151, 'BypassScope role grant requires a valid approved approval record.', 1;
    END
END
GO

--
-- Create foreign key [FK_UserRoleGrants_ApprovalID] on table [RBAC].[UserRoleGrants]
--
PRINT (N'Create foreign key [FK_UserRoleGrants_ApprovalID] on table [RBAC].[UserRoleGrants]')
GO
ALTER TABLE RBAC.UserRoleGrants
  ADD CONSTRAINT FK_UserRoleGrants_ApprovalID FOREIGN KEY (ApprovalID) REFERENCES RBAC.BypassScopeApprovals (ApprovalID)
GO

--
-- Create foreign key [FK_UserRoleGrants_RoleID] on table [RBAC].[UserRoleGrants]
--
PRINT (N'Create foreign key [FK_UserRoleGrants_RoleID] on table [RBAC].[UserRoleGrants]')
GO
ALTER TABLE RBAC.UserRoleGrants
  ADD CONSTRAINT FK_UserRoleGrants_RoleID FOREIGN KEY (RoleID) REFERENCES RBAC.Roles (ID)
GO

--
-- Create foreign key [FK_UserRoleGrants_ScopeID] on table [RBAC].[UserRoleGrants]
--
PRINT (N'Create foreign key [FK_UserRoleGrants_ScopeID] on table [RBAC].[UserRoleGrants]')
GO
ALTER TABLE RBAC.UserRoleGrants
  ADD CONSTRAINT FK_UserRoleGrants_ScopeID FOREIGN KEY (ScopeID) REFERENCES RBAC.Scopes (ScopeID)
GO

--
-- Create foreign key [FK_UserRoleGrants_UserID] on table [RBAC].[UserRoleGrants]
--
PRINT (N'Create foreign key [FK_UserRoleGrants_UserID] on table [RBAC].[UserRoleGrants]')
GO
ALTER TABLE RBAC.UserRoleGrants
  ADD CONSTRAINT FK_UserRoleGrants_UserID FOREIGN KEY (UserID) REFERENCES RBAC.Users (ID)
GO

--
-- Create table [RBAC].[Sessions]
--
PRINT (N'Create table [RBAC].[Sessions]')
GO
CREATE TABLE RBAC.Sessions (
  ID int IDENTITY,
  Name varchar(200) NULL,
  UserID int NOT NULL,
  Created datetime2(3) NOT NULL DEFAULT (sysutcdatetime()),
  ActiveScopeID int NULL,
  AuthProviderID int NULL,
  AuthSubjectSnapshot nvarchar(512) NULL,
  AuthenticatedAtUtc datetime2(3) NULL,
  PRIMARY KEY CLUSTERED (ID),
  CONSTRAINT CK_Sessions_AuthBinding CHECK ([AuthProviderID] IS NULL AND [AuthSubjectSnapshot] IS NULL OR [AuthProviderID] IS NOT NULL AND [AuthSubjectSnapshot] IS NOT NULL)
)
GO

--
-- Create index [IX_Sessions_ActiveScopeID] on table [RBAC].[Sessions]
--
PRINT (N'Create index [IX_Sessions_ActiveScopeID] on table [RBAC].[Sessions]')
GO
CREATE INDEX IX_Sessions_ActiveScopeID
  ON RBAC.Sessions (ActiveScopeID)
GO

--
-- Create index [IX_Sessions_AuthProviderID] on table [RBAC].[Sessions]
--
PRINT (N'Create index [IX_Sessions_AuthProviderID] on table [RBAC].[Sessions]')
GO
CREATE INDEX IX_Sessions_AuthProviderID
  ON RBAC.Sessions (AuthProviderID)
GO

--
-- Create foreign key [FK_Sessions_ActiveScopeID] on table [RBAC].[Sessions]
--
PRINT (N'Create foreign key [FK_Sessions_ActiveScopeID] on table [RBAC].[Sessions]')
GO
ALTER TABLE RBAC.Sessions
  ADD CONSTRAINT FK_Sessions_ActiveScopeID FOREIGN KEY (ActiveScopeID) REFERENCES RBAC.Scopes (ScopeID)
GO

--
-- Create foreign key [FK_Sessions_AuthProviderID] on table [RBAC].[Sessions]
--
PRINT (N'Create foreign key [FK_Sessions_AuthProviderID] on table [RBAC].[Sessions]')
GO
ALTER TABLE RBAC.Sessions
  ADD CONSTRAINT FK_Sessions_AuthProviderID FOREIGN KEY (AuthProviderID) REFERENCES RBAC.IdentityProviders (ProviderID)
GO

--
-- Create foreign key [FK_Sessions_Users] on table [RBAC].[Sessions]
--
PRINT (N'Create foreign key [FK_Sessions_Users] on table [RBAC].[Sessions]')
GO
ALTER TABLE RBAC.Sessions
  ADD CONSTRAINT FK_Sessions_Users FOREIGN KEY (UserID) REFERENCES RBAC.Users (ID) ON DELETE CASCADE ON UPDATE CASCADE
GO

--
-- Create table [RBAC].[Session_Roles]
--
PRINT (N'Create table [RBAC].[Session_Roles]')
GO
CREATE TABLE RBAC.Session_Roles (
  SessionID int NOT NULL,
  RoleID int NOT NULL,
  CONSTRAINT PK_Session_Roles PRIMARY KEY CLUSTERED (SessionID, RoleID)
)
GO

--
-- Create foreign key [FK_Session_Roles_Roles] on table [RBAC].[Session_Roles]
--
PRINT (N'Create foreign key [FK_Session_Roles_Roles] on table [RBAC].[Session_Roles]')
GO
ALTER TABLE RBAC.Session_Roles
  ADD CONSTRAINT FK_Session_Roles_Roles FOREIGN KEY (RoleID) REFERENCES RBAC.Roles (ID) ON DELETE CASCADE ON UPDATE CASCADE
GO

--
-- Create foreign key [FK_Session_Roles_Sessions] on table [RBAC].[Session_Roles]
--
PRINT (N'Create foreign key [FK_Session_Roles_Sessions] on table [RBAC].[Session_Roles]')
GO
ALTER TABLE RBAC.Session_Roles
  ADD CONSTRAINT FK_Session_Roles_Sessions FOREIGN KEY (SessionID) REFERENCES RBAC.Sessions (ID) ON DELETE CASCADE ON UPDATE CASCADE
GO

--
-- Create or alter procedure [RBAC].[usp_session_revoke_role]
--
GO
PRINT (N'Create or alter procedure [RBAC].[usp_session_revoke_role]')
GO
/*
Revokes a direct role assignment from a session.
Returns:
  - Result set 1: revocation summary
  - Result set 2: standardized status envelope
*/
CREATE OR ALTER PROCEDURE RBAC.usp_session_revoke_role
    @SessionID INT,
    @RoleID INT
AS
BEGIN
    SET NOCOUNT ON;
    SET XACT_ABORT ON;

    DECLARE @StartedTran BIT = 0;
    DECLARE @DeletedRows INT = 0;

    BEGIN TRY
        IF @@TRANCOUNT = 0
        BEGIN
            BEGIN TRANSACTION;
            SET @StartedTran = 1;
        END

        IF @SessionID IS NULL OR @SessionID <= 0
        BEGIN
            SELECT 50001 AS StatusCode, N'ValidationError: @SessionID must be > 0.' AS StatusMessage,
                   NULL AS SqlErrorNumber, NULL AS SqlErrorState, NULL AS SqlErrorSeverity,
                   OBJECT_SCHEMA_NAME(@@PROCID) + N'.' + OBJECT_NAME(@@PROCID) AS ProcedureName,
                   SYSUTCDATETIME() AS UtcTimestamp;
            RETURN 50001;
        END

        IF @RoleID IS NULL OR @RoleID <= 0
        BEGIN
            SELECT 50001 AS StatusCode, N'ValidationError: @RoleID must be > 0.' AS StatusMessage,
                   NULL AS SqlErrorNumber, NULL AS SqlErrorState, NULL AS SqlErrorSeverity,
                   OBJECT_SCHEMA_NAME(@@PROCID) + N'.' + OBJECT_NAME(@@PROCID) AS ProcedureName,
                   SYSUTCDATETIME() AS UtcTimestamp;
            RETURN 50001;
        END

        DELETE FROM [RBAC].[Session_Roles]
        WHERE [SessionID] = @SessionID
          AND [RoleID] = @RoleID;

        SET @DeletedRows = @@ROWCOUNT;

        SELECT
            @SessionID AS SessionID,
            @RoleID AS RoleID,
            @DeletedRows AS RowsAffected;

        IF @StartedTran = 1
        BEGIN
            COMMIT TRANSACTION;
        END

        SELECT
            0 AS StatusCode,
            N'OK' AS StatusMessage,
            NULL AS SqlErrorNumber,
            NULL AS SqlErrorState,
            NULL AS SqlErrorSeverity,
            OBJECT_SCHEMA_NAME(@@PROCID) + N'.' + OBJECT_NAME(@@PROCID) AS ProcedureName,
            SYSUTCDATETIME() AS UtcTimestamp;
        RETURN 0;
    END TRY
    BEGIN CATCH
        IF @StartedTran = 1 AND @@TRANCOUNT > 0
        BEGIN
            ROLLBACK TRANSACTION;
        END

        SELECT
            ERROR_NUMBER() AS StatusCode,
            ERROR_MESSAGE() AS StatusMessage,
            ERROR_NUMBER() AS SqlErrorNumber,
            ERROR_STATE() AS SqlErrorState,
            ERROR_SEVERITY() AS SqlErrorSeverity,
            COALESCE(ERROR_PROCEDURE(), OBJECT_SCHEMA_NAME(@@PROCID) + N'.' + OBJECT_NAME(@@PROCID)) AS ProcedureName,
            SYSUTCDATETIME() AS UtcTimestamp;
        RETURN ERROR_NUMBER();
    END CATCH
END;
GO

--
-- Create or alter procedure [RBAC].[usp_session_is_authorized]
--
GO
PRINT (N'Create or alter procedure [RBAC].[usp_session_is_authorized]')
GO
/*
Checks if a session is authorized for an object/operation pair.
Returns:
  - Result set 1: authorization decision
  - Result set 2: standardized status envelope
*/
CREATE OR ALTER PROCEDURE RBAC.usp_session_is_authorized
    @SessionID INT,
    @ObjID INT,
    @OperationID INT
AS
BEGIN
    SET NOCOUNT ON;
    SET XACT_ABORT ON;

    BEGIN TRY
        IF @SessionID IS NULL OR @SessionID <= 0
        BEGIN
            SELECT 50001 AS StatusCode, N'ValidationError: @SessionID must be > 0.' AS StatusMessage,
                   NULL AS SqlErrorNumber, NULL AS SqlErrorState, NULL AS SqlErrorSeverity,
                   OBJECT_SCHEMA_NAME(@@PROCID) + N'.' + OBJECT_NAME(@@PROCID) AS ProcedureName,
                   SYSUTCDATETIME() AS UtcTimestamp;
            RETURN 50001;
        END

        IF @ObjID IS NULL OR @ObjID <= 0
        BEGIN
            SELECT 50001 AS StatusCode, N'ValidationError: @ObjID must be > 0.' AS StatusMessage,
                   NULL AS SqlErrorNumber, NULL AS SqlErrorState, NULL AS SqlErrorSeverity,
                   OBJECT_SCHEMA_NAME(@@PROCID) + N'.' + OBJECT_NAME(@@PROCID) AS ProcedureName,
                   SYSUTCDATETIME() AS UtcTimestamp;
            RETURN 50001;
        END

        IF @OperationID IS NULL OR @OperationID <= 0
        BEGIN
            SELECT 50001 AS StatusCode, N'ValidationError: @OperationID must be > 0.' AS StatusMessage,
                   NULL AS SqlErrorNumber, NULL AS SqlErrorState, NULL AS SqlErrorSeverity,
                   OBJECT_SCHEMA_NAME(@@PROCID) + N'.' + OBJECT_NAME(@@PROCID) AS ProcedureName,
                   SYSUTCDATETIME() AS UtcTimestamp;
            RETURN 50001;
        END

        IF NOT EXISTS (SELECT 1 FROM [RBAC].[Sessions] WHERE [ID] = @SessionID)
        BEGIN
            SELECT 50010 AS StatusCode, N'BusinessRule: Session not found.' AS StatusMessage,
                   NULL AS SqlErrorNumber, NULL AS SqlErrorState, NULL AS SqlErrorSeverity,
                   OBJECT_SCHEMA_NAME(@@PROCID) + N'.' + OBJECT_NAME(@@PROCID) AS ProcedureName,
                   SYSUTCDATETIME() AS UtcTimestamp;
            RETURN 50010;
        END

        IF NOT EXISTS (SELECT 1 FROM [dbo].[Objs] WHERE [ID] = @ObjID)
        BEGIN
            SELECT 50010 AS StatusCode, N'BusinessRule: Object not found.' AS StatusMessage,
                   NULL AS SqlErrorNumber, NULL AS SqlErrorState, NULL AS SqlErrorSeverity,
                   OBJECT_SCHEMA_NAME(@@PROCID) + N'.' + OBJECT_NAME(@@PROCID) AS ProcedureName,
                   SYSUTCDATETIME() AS UtcTimestamp;
            RETURN 50010;
        END

        IF NOT EXISTS (SELECT 1 FROM [Meta].[Operations] WHERE [ID] = @OperationID)
        BEGIN
            SELECT 50010 AS StatusCode, N'BusinessRule: Operation not found.' AS StatusMessage,
                   NULL AS SqlErrorNumber, NULL AS SqlErrorState, NULL AS SqlErrorSeverity,
                   OBJECT_SCHEMA_NAME(@@PROCID) + N'.' + OBJECT_NAME(@@PROCID) AS ProcedureName,
                   SYSUTCDATETIME() AS UtcTimestamp;
            RETURN 50010;
        END

        ;WITH SessionUser AS
        (
            SELECT s.[ID] AS SessionID, s.[UserID]
            FROM [RBAC].[Sessions] s
            WHERE s.[ID] = @SessionID
        ),
        EffectiveRoles AS
        (
            SELECT sr.[RoleID]
            FROM [RBAC].[Session_Roles] sr
            WHERE sr.[SessionID] = @SessionID

            UNION

            SELECT gr.[RoleID]
            FROM SessionUser su
            INNER JOIN [RBAC].[Group_Users] gu ON gu.[UserID] = su.[UserID]
            INNER JOIN [RBAC].[Group_Roles] gr ON gr.[GroupID] = gu.[GroupID]
        ),
        PermissionMatch AS
        (
            SELECT 1 AS MatchFound
            FROM EffectiveRoles er
            INNER JOIN [RBAC].[Role_Permissions] rp ON rp.[RoleID] = er.[RoleID]
            INNER JOIN [RBAC].[Permissions] p ON p.[ID] = rp.[PermissionID]
            WHERE p.[ObjID] = @ObjID
              AND p.[OperationID] = @OperationID
        )
        SELECT
            @SessionID AS SessionID,
            @ObjID AS ObjID,
            @OperationID AS OperationID,
            CAST(CASE WHEN EXISTS (SELECT 1 FROM PermissionMatch) THEN 1 ELSE 0 END AS BIT) AS IsAuthorized;

        SELECT
            0 AS StatusCode,
            N'OK' AS StatusMessage,
            NULL AS SqlErrorNumber,
            NULL AS SqlErrorState,
            NULL AS SqlErrorSeverity,
            OBJECT_SCHEMA_NAME(@@PROCID) + N'.' + OBJECT_NAME(@@PROCID) AS ProcedureName,
            SYSUTCDATETIME() AS UtcTimestamp;
        RETURN 0;
    END TRY
    BEGIN CATCH
        SELECT
            ERROR_NUMBER() AS StatusCode,
            ERROR_MESSAGE() AS StatusMessage,
            ERROR_NUMBER() AS SqlErrorNumber,
            ERROR_STATE() AS SqlErrorState,
            ERROR_SEVERITY() AS SqlErrorSeverity,
            COALESCE(ERROR_PROCEDURE(), OBJECT_SCHEMA_NAME(@@PROCID) + N'.' + OBJECT_NAME(@@PROCID)) AS ProcedureName,
            SYSUTCDATETIME() AS UtcTimestamp;
        RETURN ERROR_NUMBER();
    END CATCH
END;
GO

--
-- Create or alter procedure [RBAC].[usp_session_get_effective_roles_v2]
--
GO
PRINT (N'Create or alter procedure [RBAC].[usp_session_get_effective_roles_v2]')
GO

CREATE OR ALTER PROCEDURE RBAC.usp_session_get_effective_roles_v2
    @SessionID INT
AS
BEGIN
    SET NOCOUNT ON;
    SET XACT_ABORT ON;

    IF @SessionID IS NULL OR @SessionID <= 0
        THROW 50001, 'ValidationError: @SessionID must be > 0.', 1;

    IF NOT EXISTS (SELECT 1 FROM [RBAC].[Sessions] WHERE [ID] = @SessionID)
        THROW 50010, 'BusinessRule: Session not found.', 1;

    ;WITH SessionUser AS
    (
        SELECT s.[ID] AS SessionID, s.[UserID], s.[ActiveScopeID]
        FROM [RBAC].[Sessions] s
        WHERE s.[ID] = @SessionID
    ),
    CandidateRoles AS
    (
        SELECT
            urg.[RoleID],
            CAST(CASE WHEN urg.[GrantType] = 'GLOBAL' THEN N'GlobalGrant' ELSE N'ScopedGrant' END AS NVARCHAR(32)) AS RoleSource,
            CAST(CASE WHEN urg.[GrantType] = 'GLOBAL' THEN 400 ELSE 350 END AS INT) AS SourcePriority
        FROM SessionUser su
        INNER JOIN [RBAC].[UserRoleGrants] urg
            ON urg.[UserID] = su.[UserID]
           AND urg.[Active] = 1
           AND
           (
               urg.[GrantType] = 'GLOBAL'
               OR (urg.[GrantType] = 'SCOPED' AND urg.[ScopeID] = su.[ActiveScopeID])
           )

        UNION ALL

        SELECT
            sr.[RoleID],
            CAST(N'DirectSessionRole' AS NVARCHAR(32)) AS RoleSource,
            CAST(200 AS INT) AS SourcePriority
        FROM [RBAC].[Session_Roles] sr
        WHERE sr.[SessionID] = @SessionID

        UNION ALL

        SELECT
            gr.[RoleID],
            CAST(N'GroupInheritedRole' AS NVARCHAR(32)) AS RoleSource,
            CAST(100 AS INT) AS SourcePriority
        FROM SessionUser su
        INNER JOIN [RBAC].[Group_Users] gu ON gu.[UserID] = su.[UserID]
        INNER JOIN [RBAC].[Group_Roles] gr ON gr.[GroupID] = gu.[GroupID]
    ),
    RankedRoles AS
    (
        SELECT
            cr.[RoleID],
            cr.[RoleSource],
            cr.[SourcePriority],
            ROW_NUMBER() OVER (PARTITION BY cr.[RoleID] ORDER BY cr.[SourcePriority] DESC) AS rn
        FROM CandidateRoles cr
    )
    SELECT
        rr.[RoleID],
        r.[Name] AS RoleName,
        r.[BypassScope],
        rr.[RoleSource] AS EffectiveRoleSource,
        rr.[SourcePriority] AS EffectiveRolePriority
    FROM RankedRoles rr
    INNER JOIN [RBAC].[Roles] r ON r.[ID] = rr.[RoleID]
    WHERE rr.rn = 1
    ORDER BY rr.[SourcePriority] DESC, r.[BypassScope] DESC, r.[Name];
END
GO

--
-- Create or alter procedure [RBAC].[usp_session_get_effective_permissions]
--
GO
PRINT (N'Create or alter procedure [RBAC].[usp_session_get_effective_permissions]')
GO
/*
Returns effective permissions for a session from:
  - direct session roles
  - inherited roles from user group memberships
Returns:
  - Result set 1: permissions
  - Result set 2: standardized status envelope
*/
CREATE OR ALTER PROCEDURE RBAC.usp_session_get_effective_permissions
    @SessionID INT
AS
BEGIN
    SET NOCOUNT ON;
    SET XACT_ABORT ON;

    BEGIN TRY
        IF @SessionID IS NULL OR @SessionID <= 0
        BEGIN
            SELECT 50001 AS StatusCode, N'ValidationError: @SessionID must be > 0.' AS StatusMessage,
                   NULL AS SqlErrorNumber, NULL AS SqlErrorState, NULL AS SqlErrorSeverity,
                   OBJECT_SCHEMA_NAME(@@PROCID) + N'.' + OBJECT_NAME(@@PROCID) AS ProcedureName,
                   SYSUTCDATETIME() AS UtcTimestamp;
            RETURN 50001;
        END

        IF NOT EXISTS (SELECT 1 FROM [RBAC].[Sessions] WHERE [ID] = @SessionID)
        BEGIN
            SELECT 50010 AS StatusCode, N'BusinessRule: Session not found.' AS StatusMessage,
                   NULL AS SqlErrorNumber, NULL AS SqlErrorState, NULL AS SqlErrorSeverity,
                   OBJECT_SCHEMA_NAME(@@PROCID) + N'.' + OBJECT_NAME(@@PROCID) AS ProcedureName,
                   SYSUTCDATETIME() AS UtcTimestamp;
            RETURN 50010;
        END

        ;WITH SessionUser AS
        (
            SELECT s.[ID] AS SessionID, s.[UserID]
            FROM [RBAC].[Sessions] s
            WHERE s.[ID] = @SessionID
        ),
        EffectiveRoles AS
        (
            SELECT sr.[RoleID], CAST(N'DirectSessionRole' AS NVARCHAR(50)) AS RoleSource
            FROM [RBAC].[Session_Roles] sr
            WHERE sr.[SessionID] = @SessionID

            UNION

            SELECT gr.[RoleID], CAST(N'GroupInheritedRole' AS NVARCHAR(50)) AS RoleSource
            FROM SessionUser su
            INNER JOIN [RBAC].[Group_Users] gu ON gu.[UserID] = su.[UserID]
            INNER JOIN [RBAC].[Group_Roles] gr ON gr.[GroupID] = gu.[GroupID]
        )
        SELECT DISTINCT
            er.[RoleID],
            r.[Name] AS RoleName,
            rp.[PermissionID],
            p.[Name] AS PermissionName,
            p.[ObjID],
            p.[OperationID],
            er.[RoleSource]
        FROM EffectiveRoles er
        INNER JOIN [RBAC].[Roles] r ON r.[ID] = er.[RoleID]
        INNER JOIN [RBAC].[Role_Permissions] rp ON rp.[RoleID] = er.[RoleID]
        INNER JOIN [RBAC].[Permissions] p ON p.[ID] = rp.[PermissionID]
        ORDER BY er.[RoleSource], er.[RoleID], rp.[PermissionID];

        SELECT
            0 AS StatusCode,
            N'OK' AS StatusMessage,
            NULL AS SqlErrorNumber,
            NULL AS SqlErrorState,
            NULL AS SqlErrorSeverity,
            OBJECT_SCHEMA_NAME(@@PROCID) + N'.' + OBJECT_NAME(@@PROCID) AS ProcedureName,
            SYSUTCDATETIME() AS UtcTimestamp;
        RETURN 0;
    END TRY
    BEGIN CATCH
        SELECT
            ERROR_NUMBER() AS StatusCode,
            ERROR_MESSAGE() AS StatusMessage,
            ERROR_NUMBER() AS SqlErrorNumber,
            ERROR_STATE() AS SqlErrorState,
            ERROR_SEVERITY() AS SqlErrorSeverity,
            COALESCE(ERROR_PROCEDURE(), OBJECT_SCHEMA_NAME(@@PROCID) + N'.' + OBJECT_NAME(@@PROCID)) AS ProcedureName,
            SYSUTCDATETIME() AS UtcTimestamp;
        RETURN ERROR_NUMBER();
    END CATCH
END;
GO

--
-- Create or alter procedure [RBAC].[usp_session_assign_role]
--
GO
PRINT (N'Create or alter procedure [RBAC].[usp_session_assign_role]')
GO
/*
Assigns a role directly to a session.
Returns:
  - Result set 1: assignment details
  - Result set 2: standardized status envelope
*/
CREATE OR ALTER PROCEDURE RBAC.usp_session_assign_role
    @SessionID INT,
    @RoleID INT
AS
BEGIN
    SET NOCOUNT ON;
    SET XACT_ABORT ON;

    DECLARE @StartedTran BIT = 0;

    BEGIN TRY
        IF @@TRANCOUNT = 0
        BEGIN
            BEGIN TRANSACTION;
            SET @StartedTran = 1;
        END

        IF @SessionID IS NULL OR @SessionID <= 0
        BEGIN
            SELECT 50001 AS StatusCode, N'ValidationError: @SessionID must be > 0.' AS StatusMessage,
                   NULL AS SqlErrorNumber, NULL AS SqlErrorState, NULL AS SqlErrorSeverity,
                   OBJECT_SCHEMA_NAME(@@PROCID) + N'.' + OBJECT_NAME(@@PROCID) AS ProcedureName,
                   SYSUTCDATETIME() AS UtcTimestamp;
            RETURN 50001;
        END

        IF @RoleID IS NULL OR @RoleID <= 0
        BEGIN
            SELECT 50001 AS StatusCode, N'ValidationError: @RoleID must be > 0.' AS StatusMessage,
                   NULL AS SqlErrorNumber, NULL AS SqlErrorState, NULL AS SqlErrorSeverity,
                   OBJECT_SCHEMA_NAME(@@PROCID) + N'.' + OBJECT_NAME(@@PROCID) AS ProcedureName,
                   SYSUTCDATETIME() AS UtcTimestamp;
            RETURN 50001;
        END

        IF NOT EXISTS (SELECT 1 FROM [RBAC].[Sessions] WHERE [ID] = @SessionID)
        BEGIN
            SELECT 50010 AS StatusCode, N'BusinessRule: Session not found.' AS StatusMessage,
                   NULL AS SqlErrorNumber, NULL AS SqlErrorState, NULL AS SqlErrorSeverity,
                   OBJECT_SCHEMA_NAME(@@PROCID) + N'.' + OBJECT_NAME(@@PROCID) AS ProcedureName,
                   SYSUTCDATETIME() AS UtcTimestamp;
            RETURN 50010;
        END

        IF NOT EXISTS (SELECT 1 FROM [RBAC].[Roles] WHERE [ID] = @RoleID)
        BEGIN
            SELECT 50010 AS StatusCode, N'BusinessRule: Role not found.' AS StatusMessage,
                   NULL AS SqlErrorNumber, NULL AS SqlErrorState, NULL AS SqlErrorSeverity,
                   OBJECT_SCHEMA_NAME(@@PROCID) + N'.' + OBJECT_NAME(@@PROCID) AS ProcedureName,
                   SYSUTCDATETIME() AS UtcTimestamp;
            RETURN 50010;
        END

        IF NOT EXISTS
        (
            SELECT 1
            FROM [RBAC].[Session_Roles]
            WHERE [SessionID] = @SessionID
              AND [RoleID] = @RoleID
        )
        BEGIN
            INSERT INTO [RBAC].[Session_Roles]
            (
                [SessionID],
                [RoleID]
            )
            VALUES
            (
                @SessionID,
                @RoleID
            );
        END

        SELECT
            sr.[SessionID],
            sr.[RoleID]
        FROM [RBAC].[Session_Roles] sr
        WHERE sr.[SessionID] = @SessionID
          AND sr.[RoleID] = @RoleID;

        IF @StartedTran = 1
        BEGIN
            COMMIT TRANSACTION;
        END

        SELECT
            0 AS StatusCode,
            N'OK' AS StatusMessage,
            NULL AS SqlErrorNumber,
            NULL AS SqlErrorState,
            NULL AS SqlErrorSeverity,
            OBJECT_SCHEMA_NAME(@@PROCID) + N'.' + OBJECT_NAME(@@PROCID) AS ProcedureName,
            SYSUTCDATETIME() AS UtcTimestamp;
        RETURN 0;
    END TRY
    BEGIN CATCH
        IF @StartedTran = 1 AND @@TRANCOUNT > 0
        BEGIN
            ROLLBACK TRANSACTION;
        END

        SELECT
            ERROR_NUMBER() AS StatusCode,
            ERROR_MESSAGE() AS StatusMessage,
            ERROR_NUMBER() AS SqlErrorNumber,
            ERROR_STATE() AS SqlErrorState,
            ERROR_SEVERITY() AS SqlErrorSeverity,
            COALESCE(ERROR_PROCEDURE(), OBJECT_SCHEMA_NAME(@@PROCID) + N'.' + OBJECT_NAME(@@PROCID)) AS ProcedureName,
            SYSUTCDATETIME() AS UtcTimestamp;
        RETURN ERROR_NUMBER();
    END CATCH
END;
GO

--
-- Create table [RBAC].[ScopeIdentityProviders]
--
PRINT (N'Create table [RBAC].[ScopeIdentityProviders]')
GO
CREATE TABLE RBAC.ScopeIdentityProviders (
  ScopeID int NOT NULL,
  ProviderID int NOT NULL,
  Active bit NOT NULL CONSTRAINT DF_RBAC_ScopeIdentityProviders_Active DEFAULT (1),
  CreatedAtUtc datetime2(3) NOT NULL CONSTRAINT DF_RBAC_ScopeIdentityProviders_CreatedAtUtc DEFAULT (sysutcdatetime()),
  CONSTRAINT PK_RBAC_ScopeIdentityProviders PRIMARY KEY CLUSTERED (ScopeID, ProviderID)
)
GO

--
-- Create index [IX_RBAC_ScopeIdentityProviders_ProviderID] on table [RBAC].[ScopeIdentityProviders]
--
PRINT (N'Create index [IX_RBAC_ScopeIdentityProviders_ProviderID] on table [RBAC].[ScopeIdentityProviders]')
GO
CREATE INDEX IX_RBAC_ScopeIdentityProviders_ProviderID
  ON RBAC.ScopeIdentityProviders (ProviderID, Active)
GO

--
-- Create index [UX_RBAC_ScopeIdentityProviders_ActiveScope] on table [RBAC].[ScopeIdentityProviders]
--
PRINT (N'Create index [UX_RBAC_ScopeIdentityProviders_ActiveScope] on table [RBAC].[ScopeIdentityProviders]')
GO
CREATE UNIQUE INDEX UX_RBAC_ScopeIdentityProviders_ActiveScope
  ON RBAC.ScopeIdentityProviders (ScopeID)
  WHERE ([Active]=(1))
GO

--
-- Create foreign key [FK_RBAC_ScopeIdentityProviders_ProviderID] on table [RBAC].[ScopeIdentityProviders]
--
PRINT (N'Create foreign key [FK_RBAC_ScopeIdentityProviders_ProviderID] on table [RBAC].[ScopeIdentityProviders]')
GO
ALTER TABLE RBAC.ScopeIdentityProviders
  ADD CONSTRAINT FK_RBAC_ScopeIdentityProviders_ProviderID FOREIGN KEY (ProviderID) REFERENCES RBAC.IdentityProviders (ProviderID)
GO

--
-- Create foreign key [FK_RBAC_ScopeIdentityProviders_ScopeID] on table [RBAC].[ScopeIdentityProviders]
--
PRINT (N'Create foreign key [FK_RBAC_ScopeIdentityProviders_ScopeID] on table [RBAC].[ScopeIdentityProviders]')
GO
ALTER TABLE RBAC.ScopeIdentityProviders
  ADD CONSTRAINT FK_RBAC_ScopeIdentityProviders_ScopeID FOREIGN KEY (ScopeID) REFERENCES RBAC.Scopes (ScopeID)
GO

--
-- Create or alter procedure [RBAC].[spResolveUserByScopeIdentity]
--
GO
PRINT (N'Create or alter procedure [RBAC].[spResolveUserByScopeIdentity]')
GO

CREATE OR ALTER PROCEDURE RBAC.spResolveUserByScopeIdentity
    @ScopeID INT,
    @Issuer NVARCHAR(512),
    @Subject NVARCHAR(512),
    @UserID INT OUTPUT,
    @ProviderID INT OUTPUT
AS
BEGIN
    SET NOCOUNT ON;
    SET XACT_ABORT ON;

    SET @UserID = NULL;
    SET @ProviderID = NULL;

    IF @ScopeID IS NULL OR @ScopeID <= 0
       OR @Issuer IS NULL OR LTRIM(RTRIM(@Issuer)) = N'' 
       OR @Subject IS NULL OR LTRIM(RTRIM(@Subject)) = N''
    BEGIN
        THROW 50001, 'ValidationError: ScopeID, Issuer and Subject are required.', 1;
    END

    SELECT TOP (1)
        @ProviderID = ip.[ProviderID]
    FROM [RBAC].[ScopeIdentityProviders] sip
    INNER JOIN [RBAC].[IdentityProviders] ip
        ON ip.[ProviderID] = sip.[ProviderID]
    WHERE sip.[ScopeID] = @ScopeID
      AND sip.[Active] = 1
      AND ip.[Active] = 1
      AND ip.[Issuer] = @Issuer;

    IF @ProviderID IS NULL
    BEGIN
        THROW 50020, 'BusinessRule: Scope is not configured for the provided issuer.', 1;
    END

    SELECT TOP (1)
        @UserID = ei.[UserID]
    FROM [RBAC].[ExternalIdentities] ei
    INNER JOIN [RBAC].[Users] u
        ON u.[ID] = ei.[UserID]
    WHERE ei.[ProviderID] = @ProviderID
      AND ei.[Subject] = @Subject
      AND ei.[Active] = 1
      AND u.[Active] = 1;

    IF @UserID IS NOT NULL
    BEGIN
        UPDATE [RBAC].[ExternalIdentities]
        SET [LastLoginAtUtc] = SYSUTCDATETIME()
        WHERE [ProviderID] = @ProviderID
          AND [Subject] = @Subject
          AND [UserID] = @UserID;
    END

    SELECT @ProviderID AS [ProviderID], @UserID AS [UserID];
END
GO

--
-- Create table [portal].[Role2Node]
--
PRINT (N'Create table [portal].[Role2Node]')
GO
CREATE TABLE portal.Role2Node (
  RoleId int NOT NULL,
  NodeId int NOT NULL,
  ScopeId int NOT NULL,
  CONSTRAINT PK_Role2Node PRIMARY KEY CLUSTERED (ScopeId, RoleId, NodeId)
)
GO

--
-- Create index [IX_Role2Node_RoleNode] on table [portal].[Role2Node]
--
PRINT (N'Create index [IX_Role2Node_RoleNode] on table [portal].[Role2Node]')
GO
CREATE INDEX IX_Role2Node_RoleNode
  ON portal.Role2Node (RoleId, NodeId, ScopeId)
GO

--
-- Create foreign key [FK_Role2Node_NavTree_ID] on table [portal].[Role2Node]
--
PRINT (N'Create foreign key [FK_Role2Node_NavTree_ID] on table [portal].[Role2Node]')
GO
ALTER TABLE portal.Role2Node WITH NOCHECK
  ADD CONSTRAINT FK_Role2Node_NavTree_ID FOREIGN KEY (NodeId) REFERENCES portal.NavTree (ID) ON DELETE CASCADE ON UPDATE CASCADE
GO

--
-- Create foreign key [FK_Role2Node_Roles_ID] on table [portal].[Role2Node]
--
PRINT (N'Create foreign key [FK_Role2Node_Roles_ID] on table [portal].[Role2Node]')
GO
ALTER TABLE portal.Role2Node
  ADD CONSTRAINT FK_Role2Node_Roles_ID FOREIGN KEY (RoleId) REFERENCES RBAC.Roles (ID) ON DELETE CASCADE ON UPDATE CASCADE
GO

--
-- Create foreign key [FK_Role2Node_Scopes_ScopeID] on table [portal].[Role2Node]
--
PRINT (N'Create foreign key [FK_Role2Node_Scopes_ScopeID] on table [portal].[Role2Node]')
GO
ALTER TABLE portal.Role2Node
  ADD CONSTRAINT FK_Role2Node_Scopes_ScopeID FOREIGN KEY (ScopeId) REFERENCES RBAC.Scopes (ScopeID) ON DELETE CASCADE ON UPDATE CASCADE
GO

--
-- Create or alter procedure [RBAC].[spGetUserNavTree]
--
GO
PRINT (N'Create or alter procedure [RBAC].[spGetUserNavTree]')
GO

/*
	Author	Inty Saez
	Date 	05/06/2026
	Subject	Get User Nav Tree (RBAC)
*/

CREATE OR ALTER PROCEDURE RBAC.spGetUserNavTree
    @UserId INT,
    @RoleId INT,
    @ScopeID INT = NULL
AS
BEGIN
    SET NOCOUNT ON;

    ;WITH UserAuthorizedNodes AS
    (
        SELECT
            rn.NodeId,
            n.NodePath
        FROM portal.Role2Node rn
        JOIN portal.NavTree n ON n.ID = rn.NodeId
        JOIN portal.viewUserAllRoles u ON u.RoleId = rn.RoleId AND u.UserId = @UserId
        WHERE 
            rn.RoleId = @RoleId
            AND (@ScopeID IS NULL OR rn.ScopeId = @ScopeID)
    ),
    VisibleTree AS
    (
        SELECT
            n.ID,
            n.ParentID,
            n.Caption,
            n.Seq,
            n.InfoID,
            n.SortPath
        FROM portal.NavTree n
        WHERE EXISTS
        (
            SELECT 1
            FROM UserAuthorizedNodes uan
            WHERE uan.NodeId = n.ID OR uan.NodePath LIKE n.NodePath + '/%'
        )
    )
    SELECT
        CAST(v.ID AS SMALLINT)       AS ID,
        CAST(v.ParentID AS SMALLINT) AS ParentID,
        v.Caption,
        v.Seq,
        v.InfoID
    FROM VisibleTree v
    ORDER BY v.SortPath, v.ID;
END;
GO

--
-- Create table [portal].[Patients]
--
PRINT (N'Create table [portal].[Patients]')
GO
CREATE TABLE portal.Patients (
  ID int IDENTITY,
  InstitutionID int NULL,
  Name varchar(50) NOT NULL,
  Sex char(1) NOT NULL,
  YearBorn int NULL,
  Race varchar(50) NULL,
  Ethnicity varchar(50) NULL,
  CONSTRAINT PK_Patients_1 PRIMARY KEY CLUSTERED (ID)
)
GO

--
-- Create foreign key [FK_Patients_Institutions] on table [portal].[Patients]
--
PRINT (N'Create foreign key [FK_Patients_Institutions] on table [portal].[Patients]')
GO
ALTER TABLE portal.Patients
  ADD CONSTRAINT FK_Patients_Institutions FOREIGN KEY (InstitutionID) REFERENCES portal.Institutions (ID) ON DELETE SET NULL ON UPDATE CASCADE
GO

--
-- Create table [portal].[InstitutionLabs]
--
PRINT (N'Create table [portal].[InstitutionLabs]')
GO
CREATE TABLE portal.InstitutionLabs (
  InstitutionID int IDENTITY,
  LabID int NOT NULL,
  CONSTRAINT PK_InstitutionLabs PRIMARY KEY CLUSTERED (InstitutionID, LabID)
)
GO

--
-- Create foreign key [FK_InstitutionLabs] on table [portal].[InstitutionLabs]
--
PRINT (N'Create foreign key [FK_InstitutionLabs] on table [portal].[InstitutionLabs]')
GO
ALTER TABLE portal.InstitutionLabs
  ADD CONSTRAINT FK_InstitutionLabs FOREIGN KEY (InstitutionID) REFERENCES portal.Institutions (ID)
GO

--
-- Create foreign key [FK_InstitutionLabs_Labs_ID] on table [portal].[InstitutionLabs]
--
PRINT (N'Create foreign key [FK_InstitutionLabs_Labs_ID] on table [portal].[InstitutionLabs]')
GO
ALTER TABLE portal.InstitutionLabs
  ADD CONSTRAINT FK_InstitutionLabs_Labs_ID FOREIGN KEY (LabID) REFERENCES portal.Labs (ID)
GO

--
-- Create table [portal].[Diseases]
--
PRINT (N'Create table [portal].[Diseases]')
GO
CREATE TABLE portal.Diseases (
  ID int IDENTITY,
  Name varchar(128) NOT NULL,
  ParentID int NULL,
  CONSTRAINT PK_Diseases_1 PRIMARY KEY CLUSTERED (ID)
)
GO

--
-- Create table [portal].[Customers]
--
PRINT (N'Create table [portal].[Customers]')
GO
CREATE TABLE portal.Customers (
  ID int IDENTITY,
  Name varchar(100) NOT NULL,
  CONSTRAINT PK_Customers_ID PRIMARY KEY CLUSTERED (ID)
)
GO

--
-- Create table [portal].[Samples]
--
PRINT (N'Create table [portal].[Samples]')
GO
CREATE TABLE portal.Samples (
  ID int IDENTITY,
  PatientID int NOT NULL,
  CustomerID int NULL,
  ParticipantID varchar(128) NULL,
  Age int NULL,
  Height int NULL,
  Weight int NULL,
  BMI float NULL,
  DiseaseID int NULL,
  Stage varchar(50) NULL,
  CONSTRAINT PK_Samples_1 PRIMARY KEY CLUSTERED (ID)
)
GO

--
-- Create foreign key [FK_Samples_Customers_ID] on table [portal].[Samples]
--
PRINT (N'Create foreign key [FK_Samples_Customers_ID] on table [portal].[Samples]')
GO
ALTER TABLE portal.Samples
  ADD CONSTRAINT FK_Samples_Customers_ID FOREIGN KEY (CustomerID) REFERENCES portal.Customers (ID) ON DELETE CASCADE ON UPDATE CASCADE
GO

--
-- Create foreign key [FK_Samples_Diseases_ID] on table [portal].[Samples]
--
PRINT (N'Create foreign key [FK_Samples_Diseases_ID] on table [portal].[Samples]')
GO
ALTER TABLE portal.Samples
  ADD CONSTRAINT FK_Samples_Diseases_ID FOREIGN KEY (DiseaseID) REFERENCES portal.Diseases (ID)
GO

--
-- Create foreign key [FK_Samples_Patients] on table [portal].[Samples]
--
PRINT (N'Create foreign key [FK_Samples_Patients] on table [portal].[Samples]')
GO
ALTER TABLE portal.Samples
  ADD CONSTRAINT FK_Samples_Patients FOREIGN KEY (PatientID) REFERENCES portal.Patients (ID) ON DELETE CASCADE ON UPDATE CASCADE
GO

--
-- Create or alter procedure [portal].[spGetSamplesByCustomer]
--
GO
PRINT (N'Create or alter procedure [portal].[spGetSamplesByCustomer]')
GO
/*
	Author	Inty Saez 
	Date 	5/13/2026
	Subject	Get all samples vy customer 
*/

CREATE OR ALTER PROCEDURE portal.spGetSamplesByCustomer(
	@CustomerId INT
) AS
BEGIN
	SET NOCOUNT ON;

	SELECT
			s.ID 
		,	Age
		, 	Height
		, 	Weight
		, 	BMI
		, 	d.Name AS Disease
		, 	Stage
	FROM 
		portal.Samples s 
		JOIN portal.Diseases d ON s.DiseaseID = d.ID
	WHERE 
		S.CustomerID = @CustomerId;

END
GO

--
-- Create table [portal].[SamplesProstateCancer]
--
PRINT (N'Create table [portal].[SamplesProstateCancer]')
GO
CREATE TABLE portal.SamplesProstateCancer (
  ID int NOT NULL,
  PSA float NULL,
  DRE bit NULL,
  GleasonScore varchar(10) NULL,
  CONSTRAINT PK_SamplesProstateCancer PRIMARY KEY CLUSTERED (ID)
)
GO

--
-- Create foreign key [FK_SamplesProstateCancer_Samples] on table [portal].[SamplesProstateCancer]
--
PRINT (N'Create foreign key [FK_SamplesProstateCancer_Samples] on table [portal].[SamplesProstateCancer]')
GO
ALTER TABLE portal.SamplesProstateCancer
  ADD CONSTRAINT FK_SamplesProstateCancer_Samples FOREIGN KEY (ID) REFERENCES portal.Samples (ID) ON DELETE CASCADE ON UPDATE CASCADE
GO

--
-- Create or alter procedure [portal].[spGetProstateSamplesByCustomer]
--
GO
PRINT (N'Create or alter procedure [portal].[spGetProstateSamplesByCustomer]')
GO

/*
	Author 	Inty Saez 
	Date 	6/6/2026
	Subject	Get Prostate samples by customer 
*/

CREATE OR ALTER PROCEDURE portal.spGetProstateSamplesByCustomer(
	@CustomerId INT 
) AS 
BEGIN
	SELECT 
	  s.PatientID,
	  s.Age,
	  s.Height,
	  s.Weight,
	  s.BMI,
	  s.DiseaseID,
	  s.Stage,
	  spc.PSA,
	  spc.DRE,
	  spc.GleasonScore,
	  spc.GleasonScore                 AS ReportedGleason,
	  gm.EquivalentPattern,
	  gm.RealGleasonScore,
	  gm.GradeGroup,
	  gm.RiskLevel
	FROM 
		[portal].[vSamples] s
		JOIN [portal].[SamplesProstateCancer] spc ON spc.ID = s.ID
		LEFT JOIN [portal].[vGleasonMapping] gm ON gm.ReportedValue = spc.GleasonScore
	WHERE 
		s.CustomerID = @CustomerId 
END
GO

--
-- Create table [portal].[SamplesBreastCancer]
--
PRINT (N'Create table [portal].[SamplesBreastCancer]')
GO
CREATE TABLE portal.SamplesBreastCancer (
  ID int NOT NULL,
  ER_Status bit NULL,
  PR_Status bit NULL,
  HER2_Status bit NULL,
  CONSTRAINT PK_SamplesBreastCancer PRIMARY KEY CLUSTERED (ID)
)
GO

--
-- Create foreign key [FK_SamplesBreastCancer_Samples] on table [portal].[SamplesBreastCancer]
--
PRINT (N'Create foreign key [FK_SamplesBreastCancer_Samples] on table [portal].[SamplesBreastCancer]')
GO
ALTER TABLE portal.SamplesBreastCancer
  ADD CONSTRAINT FK_SamplesBreastCancer_Samples FOREIGN KEY (ID) REFERENCES portal.Samples (ID) ON DELETE CASCADE ON UPDATE CASCADE
GO

--
-- Create table [portal].[LabSamples]
--
PRINT (N'Create table [portal].[LabSamples]')
GO
CREATE TABLE portal.LabSamples (
  ID int IDENTITY,
  SampleID int NOT NULL,
  BatchID int NULL,
  Sample varchar(128) NULL,
  AlignmentQC json NULL,
  CONSTRAINT PK_LabSamples_ID PRIMARY KEY CLUSTERED (ID)
)
GO

--
-- Create foreign key [FK_LabSamples_Batches_ID] on table [portal].[LabSamples]
--
PRINT (N'Create foreign key [FK_LabSamples_Batches_ID] on table [portal].[LabSamples]')
GO
ALTER TABLE portal.LabSamples
  ADD CONSTRAINT FK_LabSamples_Batches_ID FOREIGN KEY (BatchID) REFERENCES portal.Batches (ID) ON DELETE CASCADE ON UPDATE CASCADE
GO

--
-- Create foreign key [FK_LabSamples_Samples] on table [portal].[LabSamples]
--
PRINT (N'Create foreign key [FK_LabSamples_Samples] on table [portal].[LabSamples]')
GO
ALTER TABLE portal.LabSamples
  ADD CONSTRAINT FK_LabSamples_Samples FOREIGN KEY (SampleID) REFERENCES portal.Samples (ID) ON DELETE CASCADE ON UPDATE CASCADE
GO

--
-- Create table [portal].[Groups]
--
PRINT (N'Create table [portal].[Groups]')
GO
CREATE TABLE portal.Groups (
  ID int IDENTITY,
  Name varchar(128) NOT NULL,
  Description varchar(256) NULL,
  CustomerID int NULL,
  CONSTRAINT PK_Groups PRIMARY KEY CLUSTERED (ID),
  CONSTRAINT UQ_Groups_CustomerID_Name UNIQUE (CustomerID, Name)
)
GO

--
-- Create foreign key [FK_Groups_Customers_ID] on table [portal].[Groups]
--
PRINT (N'Create foreign key [FK_Groups_Customers_ID] on table [portal].[Groups]')
GO
ALTER TABLE portal.Groups
  ADD CONSTRAINT FK_Groups_Customers_ID FOREIGN KEY (CustomerID) REFERENCES portal.Customers (ID) ON DELETE CASCADE ON UPDATE CASCADE
GO

--
-- Create or alter procedure [portal].[spGetGroupSamplesByCustomer]
--
GO
PRINT (N'Create or alter procedure [portal].[spGetGroupSamplesByCustomer]')
GO

/* 
	Author 	Inty Saez 
	Date 	5/13/2026
	Subject Get All Samples Group by customer 
*/

CREATE OR ALTER PROCEDURE portal.spGetGroupSamplesByCustomer(
	@CustomerId INT 
) AS 
BEGIN
	SET NOCOUNT ON;

	SELECT 
		g.id, g.Name, g.Description
	FROM 
		portal.Groups g 
	WHERE 
		g.CustomerID = @CustomerId;
END
GO

--
-- Create table [portal].[GroupSamples]
--
PRINT (N'Create table [portal].[GroupSamples]')
GO
CREATE TABLE portal.GroupSamples (
  GroupID int NOT NULL,
  SampleID int NOT NULL,
  CONSTRAINT PK_GroupSamples PRIMARY KEY CLUSTERED (GroupID, SampleID)
)
GO

--
-- Create foreign key [FK_GroupSamples_Groups] on table [portal].[GroupSamples]
--
PRINT (N'Create foreign key [FK_GroupSamples_Groups] on table [portal].[GroupSamples]')
GO
ALTER TABLE portal.GroupSamples
  ADD CONSTRAINT FK_GroupSamples_Groups FOREIGN KEY (GroupID) REFERENCES portal.Groups (ID) ON DELETE CASCADE ON UPDATE CASCADE
GO

--
-- Create foreign key [FK_GroupSamples_Samples] on table [portal].[GroupSamples]
--
PRINT (N'Create foreign key [FK_GroupSamples_Samples] on table [portal].[GroupSamples]')
GO
ALTER TABLE portal.GroupSamples
  ADD CONSTRAINT FK_GroupSamples_Samples FOREIGN KEY (SampleID) REFERENCES portal.Samples (ID)
GO

--
-- Create or alter procedure [portal].[spGetSamplesByGroup]
--
GO
PRINT (N'Create or alter procedure [portal].[spGetSamplesByGroup]')
GO
/* 
	Author	Inty Saez 
	Date 	5/13/2026
	Subject	Get Samples by Group 
*/

CREATE OR ALTER PROCEDURE portal.spGetSamplesByGroup(
	@GroupId INT 
) AS 
BEGIN
	set NOCOUNT ON;

	SELECT 
			S.PatientID
		,	s.Age
		, 	s.Height
		, 	s.Weight
		, 	s.BMI
		, 	d.Name AS Disease 
		, 	s.Stage
	FROM 
		portal.Samples s
		JOIN portal.GroupSamples gs ON s.ID = gs.SampleID
		JOIN portal.Diseases d ON s.DiseaseID = d.ID
	WHERE 
		gs.GroupID = @GroupId
END
GO

--
-- Create table [portal].[CustomerInstitutions]
--
PRINT (N'Create table [portal].[CustomerInstitutions]')
GO
CREATE TABLE portal.CustomerInstitutions (
  CustomerID int NOT NULL,
  InstitutionID int NOT NULL,
  CONSTRAINT PK_CustomerInstitutions PRIMARY KEY CLUSTERED (CustomerID, InstitutionID)
)
GO

--
-- Create foreign key [FK_CustomerInstitutions_Customers_ID] on table [portal].[CustomerInstitutions]
--
PRINT (N'Create foreign key [FK_CustomerInstitutions_Customers_ID] on table [portal].[CustomerInstitutions]')
GO
ALTER TABLE portal.CustomerInstitutions
  ADD CONSTRAINT FK_CustomerInstitutions_Customers_ID FOREIGN KEY (CustomerID) REFERENCES portal.Customers (ID) ON DELETE CASCADE ON UPDATE CASCADE
GO

--
-- Create foreign key [FK_CustomerInstitutions_Institutions_ID] on table [portal].[CustomerInstitutions]
--
PRINT (N'Create foreign key [FK_CustomerInstitutions_Institutions_ID] on table [portal].[CustomerInstitutions]')
GO
ALTER TABLE portal.CustomerInstitutions
  ADD CONSTRAINT FK_CustomerInstitutions_Institutions_ID FOREIGN KEY (InstitutionID) REFERENCES portal.Institutions (ID) ON DELETE CASCADE ON UPDATE CASCADE
GO

--
-- Create or alter procedure [portal].[spGetAllInstitutionByCustomer]
--
GO
PRINT (N'Create or alter procedure [portal].[spGetAllInstitutionByCustomer]')
GO

/* 
	Auther 	Inty Saez 
	Date 	5/13/2026
	Subject List all institution by customer 
*/

CREATE OR ALTER PROCEDURE portal.spGetAllInstitutionByCustomer (
	@CustomerId INT 
) AS 
BEGIN
	SET NOCOUNT ON;

	SET @CustomerId = 1;

	SELECT 
			i.Id
		,	i.Name
	FROM 
		portal.Institutions i 
		JOIN portal.CustomerInstitutions ci ON i.ID = ci.InstitutionID
		JOIN portal.Customers c ON ci.CustomerID = c.ID
	WHERE 
		c.id = @CustomerId;

END
GO

--
-- Create schema [Contract]
--
PRINT (N'Create schema [Contract]')
GO
CREATE SCHEMA Contract AUTHORIZATION dbo
GO

--
-- Create table [Contract].[Contracts]
--
PRINT (N'Create table [Contract].[Contracts]')
GO
CREATE TABLE Contract.Contracts (
  ContractID int IDENTITY,
  CustomerID int NOT NULL,
  Name varchar(200) NOT NULL,
  Status varchar(24) NOT NULL CONSTRAINT DF_Contracts_Status DEFAULT ('DRAFT'),
  StartDateUtc datetime2(3) NOT NULL,
  EndDateUtc datetime2(3) NULL,
  PlanCode varchar(64) NULL,
  BillingCycle varchar(16) NULL,
  AutoRenew bit NOT NULL CONSTRAINT DF_Contracts_AutoRenew DEFAULT (0),
  TermsVersion varchar(32) NULL,
  CreatedAtUtc datetime2(3) NOT NULL CONSTRAINT DF_Contracts_CreatedAtUtc DEFAULT (sysutcdatetime()),
  UpdatedAtUtc datetime2(3) NOT NULL CONSTRAINT DF_Contracts_UpdatedAtUtc DEFAULT (sysutcdatetime()),
  CONSTRAINT PK_Contract_Contracts PRIMARY KEY CLUSTERED (ContractID),
  CONSTRAINT UQ_Contract_Contracts_CustomerRef UNIQUE (CustomerID),
  CONSTRAINT CK_Contracts_DateRange CHECK ([EndDateUtc] IS NULL OR [EndDateUtc]>=[StartDateUtc]),
  CONSTRAINT CK_Contracts_Status CHECK ([Status]='TERMINATED' OR [Status]='EXPIRED' OR [Status]='SUSPENDED' OR [Status]='ACTIVE' OR [Status]='DRAFT')
)
GO

--
-- Create foreign key [FK_Contracts_Customers_ID] on table [Contract].[Contracts]
--
PRINT (N'Create foreign key [FK_Contracts_Customers_ID] on table [Contract].[Contracts]')
GO
ALTER TABLE Contract.Contracts
  ADD CONSTRAINT FK_Contracts_Customers_ID FOREIGN KEY (CustomerID) REFERENCES portal.Customers (ID) ON DELETE CASCADE ON UPDATE CASCADE
GO

--
-- Create table [Contract].[WorkflowUsageCounters]
--
PRINT (N'Create table [Contract].[WorkflowUsageCounters]')
GO
CREATE TABLE Contract.WorkflowUsageCounters (
  ContractID int NOT NULL,
  ScopeID int NOT NULL,
  WorkflowDefID bigint NOT NULL,
  PeriodType varchar(8) NOT NULL,
  PeriodStartUtc datetime2(3) NOT NULL,
  RunsExecuted int NOT NULL CONSTRAINT DF_WUC_RunsExecuted DEFAULT (0),
  UpdatedAtUtc datetime2(3) NOT NULL CONSTRAINT DF_WUC_UpdatedAtUtc DEFAULT (sysutcdatetime()),
  CONSTRAINT PK_Contract_WorkflowUsageCounters PRIMARY KEY CLUSTERED (ContractID, ScopeID, WorkflowDefID, PeriodType, PeriodStartUtc),
  CONSTRAINT CK_WUC_PeriodType CHECK ([PeriodType]='MONTH' OR [PeriodType]='WEEK' OR [PeriodType]='DAY')
)
GO

--
-- Create foreign key [FK_WUC_ContractID] on table [Contract].[WorkflowUsageCounters]
--
PRINT (N'Create foreign key [FK_WUC_ContractID] on table [Contract].[WorkflowUsageCounters]')
GO
ALTER TABLE Contract.WorkflowUsageCounters
  ADD CONSTRAINT FK_WUC_ContractID FOREIGN KEY (ContractID) REFERENCES Contract.Contracts (ContractID)
GO

--
-- Create foreign key [FK_WUC_ScopeID] on table [Contract].[WorkflowUsageCounters]
--
PRINT (N'Create foreign key [FK_WUC_ScopeID] on table [Contract].[WorkflowUsageCounters]')
GO
ALTER TABLE Contract.WorkflowUsageCounters
  ADD CONSTRAINT FK_WUC_ScopeID FOREIGN KEY (ScopeID) REFERENCES RBAC.Scopes (ScopeID)
GO

--
-- Create foreign key [FK_WUC_WorkflowDefID] on table [Contract].[WorkflowUsageCounters]
--
PRINT (N'Create foreign key [FK_WUC_WorkflowDefID] on table [Contract].[WorkflowUsageCounters]')
GO
ALTER TABLE Contract.WorkflowUsageCounters
  ADD CONSTRAINT FK_WUC_WorkflowDefID FOREIGN KEY (WorkflowDefID) REFERENCES wf.workflow_def (id)
GO

--
-- Create table [Contract].[ContractWorkflowEntitlements]
--
PRINT (N'Create table [Contract].[ContractWorkflowEntitlements]')
GO
CREATE TABLE Contract.ContractWorkflowEntitlements (
  ContractID int NOT NULL,
  WorkflowDefID bigint NOT NULL,
  Enabled bit NOT NULL CONSTRAINT DF_CWE_Enabled DEFAULT (1),
  MaxRunsPerPeriod int NULL,
  PeriodType varchar(8) NULL,
  MaxConcurrentRuns int NULL,
  DataRetentionDays int NULL,
  SlaTier varchar(32) NULL,
  SupportTier varchar(32) NULL,
  CreatedAtUtc datetime2(3) NOT NULL CONSTRAINT DF_CWE_CreatedAtUtc DEFAULT (sysutcdatetime()),
  CONSTRAINT PK_Contract_WorkflowEntitlements PRIMARY KEY CLUSTERED (ContractID, WorkflowDefID),
  CONSTRAINT CK_CWE_PeriodType CHECK ([PeriodType] IS NULL OR ([PeriodType]='MONTH' OR [PeriodType]='WEEK' OR [PeriodType]='DAY'))
)
GO

--
-- Create index [IX_CWE_Enabled] on table [Contract].[ContractWorkflowEntitlements]
--
PRINT (N'Create index [IX_CWE_Enabled] on table [Contract].[ContractWorkflowEntitlements]')
GO
CREATE INDEX IX_CWE_Enabled
  ON Contract.ContractWorkflowEntitlements (Enabled, WorkflowDefID)
GO

--
-- Create foreign key [FK_CWE_ContractID] on table [Contract].[ContractWorkflowEntitlements]
--
PRINT (N'Create foreign key [FK_CWE_ContractID] on table [Contract].[ContractWorkflowEntitlements]')
GO
ALTER TABLE Contract.ContractWorkflowEntitlements
  ADD CONSTRAINT FK_CWE_ContractID FOREIGN KEY (ContractID) REFERENCES Contract.Contracts (ContractID)
GO

--
-- Create foreign key [FK_CWE_WorkflowDefID] on table [Contract].[ContractWorkflowEntitlements]
--
PRINT (N'Create foreign key [FK_CWE_WorkflowDefID] on table [Contract].[ContractWorkflowEntitlements]')
GO
ALTER TABLE Contract.ContractWorkflowEntitlements
  ADD CONSTRAINT FK_CWE_WorkflowDefID FOREIGN KEY (WorkflowDefID) REFERENCES wf.workflow_def (id)
GO

--
-- Create table [Contract].[ContractScopes]
--
PRINT (N'Create table [Contract].[ContractScopes]')
GO
CREATE TABLE Contract.ContractScopes (
  ContractID int NOT NULL,
  ScopeID int NOT NULL,
  Status varchar(24) NOT NULL CONSTRAINT DF_ContractScopes_Status DEFAULT ('ACTIVE'),
  ActivatedAtUtc datetime2(3) NOT NULL CONSTRAINT DF_ContractScopes_ActivatedAtUtc DEFAULT (sysutcdatetime()),
  EffectiveFromUtc datetime2(3) NOT NULL CONSTRAINT DF_ContractScopes_EffectiveFromUtc DEFAULT (sysutcdatetime()),
  EffectiveToUtc datetime2(3) NULL,
  CONSTRAINT PK_Contract_ContractScopes PRIMARY KEY CLUSTERED (ContractID, ScopeID),
  CONSTRAINT CK_ContractScopes_EffectiveRange CHECK ([EffectiveToUtc] IS NULL OR [EffectiveToUtc]>=[EffectiveFromUtc]),
  CONSTRAINT CK_ContractScopes_Status CHECK ([Status]='TERMINATED' OR [Status]='EXPIRED' OR [Status]='SUSPENDED' OR [Status]='ACTIVE')
)
GO

--
-- Create index [IX_ContractScopes_Status] on table [Contract].[ContractScopes]
--
PRINT (N'Create index [IX_ContractScopes_Status] on table [Contract].[ContractScopes]')
GO
CREATE INDEX IX_ContractScopes_Status
  ON Contract.ContractScopes (Status, ScopeID, EffectiveFromUtc, EffectiveToUtc)
GO

--
-- Create index [UX_ContractScopes_ActiveScope] on table [Contract].[ContractScopes]
--
PRINT (N'Create index [UX_ContractScopes_ActiveScope] on table [Contract].[ContractScopes]')
GO
CREATE UNIQUE INDEX UX_ContractScopes_ActiveScope
  ON Contract.ContractScopes (ScopeID)
  WHERE ([Status]='ACTIVE')
GO

--
-- Create foreign key [FK_ContractScopes_ContractID] on table [Contract].[ContractScopes]
--
PRINT (N'Create foreign key [FK_ContractScopes_ContractID] on table [Contract].[ContractScopes]')
GO
ALTER TABLE Contract.ContractScopes
  ADD CONSTRAINT FK_ContractScopes_ContractID FOREIGN KEY (ContractID) REFERENCES Contract.Contracts (ContractID)
GO

--
-- Create foreign key [FK_ContractScopes_ScopeID] on table [Contract].[ContractScopes]
--
PRINT (N'Create foreign key [FK_ContractScopes_ScopeID] on table [Contract].[ContractScopes]')
GO
ALTER TABLE Contract.ContractScopes
  ADD CONSTRAINT FK_ContractScopes_ScopeID FOREIGN KEY (ScopeID) REFERENCES RBAC.Scopes (ScopeID)
GO

--
-- Create or alter procedure [Contract].[spContractValidateScopeAccess]
--
GO
PRINT (N'Create or alter procedure [Contract].[spContractValidateScopeAccess]')
GO

CREATE OR ALTER PROCEDURE Contract.spContractValidateScopeAccess
    @ScopeID INT,
    @UtcNow DATETIME2(3) = NULL
AS
BEGIN
    SET NOCOUNT ON;
    IF @UtcNow IS NULL SET @UtcNow = SYSUTCDATETIME();

    IF @ScopeID IS NULL OR @ScopeID <= 0
    BEGIN
        SELECT CAST(0 AS BIT) AS IsAllowed, NULL AS ContractID, N'INVALID_SCOPE' AS ReasonCode;
        RETURN 50001;
    END

    ;WITH ScopeContract AS
    (
        SELECT TOP (1)
            c.ContractID
        FROM Contract.ContractScopes cs
        INNER JOIN Contract.Contracts c ON c.ContractID = cs.ContractID
        WHERE cs.ScopeID = @ScopeID
          AND cs.Status = 'ACTIVE'
          AND cs.EffectiveFromUtc <= @UtcNow
          AND (cs.EffectiveToUtc IS NULL OR cs.EffectiveToUtc >= @UtcNow)
          AND c.Status = 'ACTIVE'
          AND c.StartDateUtc <= @UtcNow
          AND (c.EndDateUtc IS NULL OR c.EndDateUtc >= @UtcNow)
        ORDER BY cs.EffectiveFromUtc DESC, c.ContractID DESC
    )
    SELECT
        CAST(CASE WHEN EXISTS (SELECT 1 FROM ScopeContract) THEN 1 ELSE 0 END AS BIT) AS IsAllowed,
        (SELECT TOP (1) ContractID FROM ScopeContract) AS ContractID,
        CASE WHEN EXISTS (SELECT 1 FROM ScopeContract) THEN N'OK' ELSE N'NO_ACTIVE_CONTRACT_FOR_SCOPE' END AS ReasonCode;

    RETURN 0;
END
GO

--
-- Create or alter procedure [Contract].[spContractValidateWorkflowExecution]
--
GO
PRINT (N'Create or alter procedure [Contract].[spContractValidateWorkflowExecution]')
GO

CREATE OR ALTER PROCEDURE Contract.spContractValidateWorkflowExecution
    @ScopeID INT,
    @WorkflowDefID BIGINT,
    @UtcNow DATETIME2(3) = NULL
AS
BEGIN
    SET NOCOUNT ON;
    IF @UtcNow IS NULL SET @UtcNow = SYSUTCDATETIME();

    DECLARE @Access TABLE (IsAllowed BIT, ContractID INT, ReasonCode NVARCHAR(64));
    INSERT INTO @Access
    EXEC Contract.spContractValidateScopeAccess @ScopeID = @ScopeID, @UtcNow = @UtcNow;

    DECLARE @ScopeAllowed BIT = (SELECT TOP (1) IsAllowed FROM @Access);
    DECLARE @ContractID INT = (SELECT TOP (1) ContractID FROM @Access);

    IF ISNULL(@ScopeAllowed, 0) = 0
    BEGIN
        SELECT CAST(0 AS BIT) AS IsAllowed, @ContractID AS ContractID, N'NO_ACTIVE_CONTRACT_FOR_SCOPE' AS ReasonCode;
        RETURN 50020;
    END

    DECLARE @Enabled BIT = 0;
    DECLARE @MaxRuns INT = NULL;
    DECLARE @PeriodType VARCHAR(8) = NULL;
    DECLARE @RunsExecuted INT = 0;
    DECLARE @PeriodStartUtc DATETIME2(3) = NULL;

    SELECT
        @Enabled = e.Enabled,
        @MaxRuns = e.MaxRunsPerPeriod,
        @PeriodType = e.PeriodType
    FROM Contract.ContractWorkflowEntitlements e
    WHERE e.ContractID = @ContractID
      AND e.WorkflowDefID = @WorkflowDefID;

    IF @Enabled IS NULL OR @Enabled = 0
    BEGIN
        SELECT CAST(0 AS BIT) AS IsAllowed, @ContractID AS ContractID, N'WORKFLOW_NOT_ENTITLED' AS ReasonCode;
        RETURN 50021;
    END

    IF @MaxRuns IS NOT NULL AND @PeriodType IS NOT NULL
    BEGIN
        IF @PeriodType = 'DAY'
            SET @PeriodStartUtc = DATEFROMPARTS(YEAR(@UtcNow), MONTH(@UtcNow), DAY(@UtcNow));
        ELSE IF @PeriodType = 'WEEK'
            SET @PeriodStartUtc = DATEADD(DAY, 1 - DATEPART(WEEKDAY, CAST(@UtcNow AS DATE)), CAST(CAST(@UtcNow AS DATE) AS DATETIME2(3)));
        ELSE IF @PeriodType = 'MONTH'
            SET @PeriodStartUtc = DATEFROMPARTS(YEAR(@UtcNow), MONTH(@UtcNow), 1);

        SELECT @RunsExecuted = ISNULL(w.RunsExecuted, 0)
        FROM Contract.WorkflowUsageCounters w
        WHERE w.ContractID = @ContractID
          AND w.ScopeID = @ScopeID
          AND w.WorkflowDefID = @WorkflowDefID
          AND w.PeriodType = @PeriodType
          AND w.PeriodStartUtc = @PeriodStartUtc;

        IF @RunsExecuted >= @MaxRuns
        BEGIN
            SELECT
                CAST(0 AS BIT) AS IsAllowed,
                @ContractID AS ContractID,
                N'WORKFLOW_QUOTA_EXCEEDED' AS ReasonCode,
                @RunsExecuted AS RunsExecuted,
                @MaxRuns AS MaxRunsPerPeriod,
                @PeriodType AS PeriodType;
            RETURN 50022;
        END
    END

    SELECT
        CAST(1 AS BIT) AS IsAllowed,
        @ContractID AS ContractID,
        N'OK' AS ReasonCode,
        ISNULL(@RunsExecuted, 0) AS RunsExecuted,
        @MaxRuns AS MaxRunsPerPeriod,
        @PeriodType AS PeriodType;

    RETURN 0;
END
GO

--
-- Create or alter procedure [Contract].[spContractConsumeWorkflowQuota]
--
GO
PRINT (N'Create or alter procedure [Contract].[spContractConsumeWorkflowQuota]')
GO
CREATE OR ALTER PROCEDURE Contract.spContractConsumeWorkflowQuota
    @ScopeID INT,
    @WorkflowDefID BIGINT,
    @ConsumeRuns INT = 1,
    @UtcNow DATETIME2(3) = NULL
AS
BEGIN
    SET NOCOUNT ON;
    SET XACT_ABORT ON;

    IF @UtcNow IS NULL SET @UtcNow = SYSUTCDATETIME();

    IF @ScopeID IS NULL OR @ScopeID <= 0
       OR @WorkflowDefID IS NULL OR @WorkflowDefID <= 0
       OR @ConsumeRuns IS NULL OR @ConsumeRuns <= 0
    BEGIN
        RAISERROR('ValidationError: ScopeID, WorkflowDefID and ConsumeRuns (>0) are required.', 16, 1);
        RETURN;
    END

    DECLARE @ContractID INT = NULL;
    DECLARE @Enabled BIT = 0;
    DECLARE @MaxRuns INT = NULL;
    DECLARE @PeriodType VARCHAR(8) = NULL;
    DECLARE @PeriodStartUtc DATETIME2(3) = NULL;
    DECLARE @RunsExecuted INT = 0;
    DECLARE @NewRunsExecuted INT = 0;

    BEGIN TRY
        BEGIN TRANSACTION;

        SELECT TOP (1)
            @ContractID = c.ContractID
        FROM Contract.ContractScopes cs
        INNER JOIN Contract.Contracts c
            ON c.ContractID = cs.ContractID
        WHERE cs.ScopeID = @ScopeID
          AND cs.Status = 'ACTIVE'
          AND cs.EffectiveFromUtc <= @UtcNow
          AND (cs.EffectiveToUtc IS NULL OR cs.EffectiveToUtc >= @UtcNow)
          AND c.Status = 'ACTIVE'
          AND c.StartDateUtc <= @UtcNow
          AND (c.EndDateUtc IS NULL OR c.EndDateUtc >= @UtcNow)
        ORDER BY cs.EffectiveFromUtc DESC, c.ContractID DESC;

        IF @ContractID IS NULL
        BEGIN
            RAISERROR('BusinessRule: Scope has no active contract.', 16, 1);
            RETURN;
        END

        SELECT
            @Enabled = e.Enabled,
            @MaxRuns = e.MaxRunsPerPeriod,
            @PeriodType = e.PeriodType
        FROM Contract.ContractWorkflowEntitlements e
        WHERE e.ContractID = @ContractID
          AND e.WorkflowDefID = @WorkflowDefID;

        IF @Enabled IS NULL OR @Enabled = 0
        BEGIN
            RAISERROR('BusinessRule: Workflow is not entitled for the active contract.', 16, 1);
            RETURN;
        END

        -- No periodic limit configured: allow execution without counter mutation.
        IF @MaxRuns IS NULL OR @PeriodType IS NULL
        BEGIN
            SELECT
                CAST(1 AS BIT) AS IsAllowed,
                @ContractID AS ContractID,
                N'OK_UNLIMITED' AS ReasonCode,
                CAST(NULL AS INT) AS RunsExecuted,
                CAST(NULL AS INT) AS MaxRunsPerPeriod,
                CAST(NULL AS VARCHAR(8)) AS PeriodType,
                CAST(NULL AS DATETIME2(3)) AS PeriodStartUtc;
            COMMIT TRANSACTION;
            RETURN 0;
        END

        IF @PeriodType = 'DAY'
            SET @PeriodStartUtc = DATEFROMPARTS(YEAR(@UtcNow), MONTH(@UtcNow), DAY(@UtcNow));
        ELSE IF @PeriodType = 'WEEK'
            SET @PeriodStartUtc = DATEADD(DAY, 1 - DATEPART(WEEKDAY, CAST(@UtcNow AS DATE)), CAST(CAST(@UtcNow AS DATE) AS DATETIME2(3)));
        ELSE IF @PeriodType = 'MONTH'
            SET @PeriodStartUtc = DATEFROMPARTS(YEAR(@UtcNow), MONTH(@UtcNow), 1);
        ELSE
        BEGIN
            RAISERROR('ValidationError: Unsupported PeriodType.', 16, 1);
            RETURN;
        END

        -- Lock counter row (or key-range) before evaluating and mutating to prevent race conditions.
        SELECT
            @RunsExecuted = w.RunsExecuted
        FROM Contract.WorkflowUsageCounters w WITH (UPDLOCK, HOLDLOCK)
        WHERE w.ContractID = @ContractID
          AND w.ScopeID = @ScopeID
          AND w.WorkflowDefID = @WorkflowDefID
          AND w.PeriodType = @PeriodType
          AND w.PeriodStartUtc = @PeriodStartUtc;

        IF @RunsExecuted IS NULL
        BEGIN
            INSERT INTO Contract.WorkflowUsageCounters
            (
                ContractID,
                ScopeID,
                WorkflowDefID,
                PeriodType,
                PeriodStartUtc,
                RunsExecuted,
                UpdatedAtUtc
            )
            VALUES
            (
                @ContractID,
                @ScopeID,
                @WorkflowDefID,
                @PeriodType,
                @PeriodStartUtc,
                0,
                @UtcNow
            );
            SET @RunsExecuted = 0;
        END

        SET @NewRunsExecuted = @RunsExecuted + @ConsumeRuns;

        IF @NewRunsExecuted > @MaxRuns
        BEGIN
            SELECT
                CAST(0 AS BIT) AS IsAllowed,
                @ContractID AS ContractID,
                N'WORKFLOW_QUOTA_EXCEEDED' AS ReasonCode,
                @RunsExecuted AS RunsExecuted,
                @MaxRuns AS MaxRunsPerPeriod,
                @PeriodType AS PeriodType,
                @PeriodStartUtc AS PeriodStartUtc;
            ROLLBACK TRANSACTION;
            RETURN 50022;
        END

        UPDATE Contract.WorkflowUsageCounters
        SET RunsExecuted = @NewRunsExecuted,
            UpdatedAtUtc = @UtcNow
        WHERE ContractID = @ContractID
          AND ScopeID = @ScopeID
          AND WorkflowDefID = @WorkflowDefID
          AND PeriodType = @PeriodType
          AND PeriodStartUtc = @PeriodStartUtc;

        SELECT
            CAST(1 AS BIT) AS IsAllowed,
            @ContractID AS ContractID,
            N'OK' AS ReasonCode,
            @NewRunsExecuted AS RunsExecuted,
            @MaxRuns AS MaxRunsPerPeriod,
            @PeriodType AS PeriodType,
            @PeriodStartUtc AS PeriodStartUtc;

        COMMIT TRANSACTION;
        RETURN 0;
    END TRY
    BEGIN CATCH
        IF XACT_STATE() <> 0 ROLLBACK TRANSACTION;
        -- Re-raise the original error
        DECLARE @ErrorMessage NVARCHAR(4000), @ErrorSeverity INT, @ErrorState INT;
        SELECT 
            @ErrorMessage = ERROR_MESSAGE(),
            @ErrorSeverity = ERROR_SEVERITY(),
            @ErrorState = ERROR_STATE();
        RAISERROR (@ErrorMessage, @ErrorSeverity, @ErrorState);
    END CATCH
END
GO

--
-- Create table [Contract].[ContractRolePolicies]
--
PRINT (N'Create table [Contract].[ContractRolePolicies]')
GO
CREATE TABLE Contract.ContractRolePolicies (
  ContractID int NOT NULL,
  RoleID int NOT NULL,
  GrantTypeAllowed varchar(16) NOT NULL CONSTRAINT DF_CRP_GrantTypeAllowed DEFAULT ('SCOPED'),
  MaxUsersForRole int NULL,
  RequiresApproval bit NOT NULL CONSTRAINT DF_CRP_RequiresApproval DEFAULT (0),
  CreatedAtUtc datetime2(3) NOT NULL CONSTRAINT DF_CRP_CreatedAtUtc DEFAULT (sysutcdatetime()),
  CONSTRAINT PK_Contract_RolePolicies PRIMARY KEY CLUSTERED (ContractID, RoleID),
  CONSTRAINT CK_CRP_GrantTypeAllowed CHECK ([GrantTypeAllowed]='BOTH' OR [GrantTypeAllowed]='SCOPED' OR [GrantTypeAllowed]='GLOBAL')
)
GO

--
-- Create foreign key [FK_CRP_ContractID] on table [Contract].[ContractRolePolicies]
--
PRINT (N'Create foreign key [FK_CRP_ContractID] on table [Contract].[ContractRolePolicies]')
GO
ALTER TABLE Contract.ContractRolePolicies
  ADD CONSTRAINT FK_CRP_ContractID FOREIGN KEY (ContractID) REFERENCES Contract.Contracts (ContractID)
GO

--
-- Create foreign key [FK_CRP_RoleID] on table [Contract].[ContractRolePolicies]
--
PRINT (N'Create foreign key [FK_CRP_RoleID] on table [Contract].[ContractRolePolicies]')
GO
ALTER TABLE Contract.ContractRolePolicies
  ADD CONSTRAINT FK_CRP_RoleID FOREIGN KEY (RoleID) REFERENCES RBAC.Roles (ID)
GO

--
-- Create or alter procedure [Contract].[spContractValidateRoleGrant]
--
GO
PRINT (N'Create or alter procedure [Contract].[spContractValidateRoleGrant]')
GO

CREATE OR ALTER PROCEDURE Contract.spContractValidateRoleGrant
    @ScopeID INT,
    @RoleID INT,
    @GrantType VARCHAR(16),
    @UtcNow DATETIME2(3) = NULL
AS
BEGIN
    SET NOCOUNT ON;
    IF @UtcNow IS NULL SET @UtcNow = SYSUTCDATETIME();

    IF @GrantType NOT IN ('GLOBAL','SCOPED')
    BEGIN
        SELECT CAST(0 AS BIT) AS IsAllowed, NULL AS ContractID, N'INVALID_GRANT_TYPE' AS ReasonCode, CAST(0 AS BIT) AS RequiresApproval;
        RETURN 50030;
    END

    DECLARE @Access TABLE (IsAllowed BIT, ContractID INT, ReasonCode NVARCHAR(64));
    INSERT INTO @Access
    EXEC Contract.spContractValidateScopeAccess @ScopeID = @ScopeID, @UtcNow = @UtcNow;

    DECLARE @ScopeAllowed BIT = (SELECT TOP (1) IsAllowed FROM @Access);
    DECLARE @ContractID INT = (SELECT TOP (1) ContractID FROM @Access);

    IF ISNULL(@ScopeAllowed, 0) = 0
    BEGIN
        SELECT CAST(0 AS BIT) AS IsAllowed, @ContractID AS ContractID, N'NO_ACTIVE_CONTRACT_FOR_SCOPE' AS ReasonCode, CAST(0 AS BIT) AS RequiresApproval;
        RETURN 50031;
    END

    DECLARE @Allowed VARCHAR(16) = NULL;
    DECLARE @RequiresApproval BIT = 0;

    SELECT
        @Allowed = p.GrantTypeAllowed,
        @RequiresApproval = p.RequiresApproval
    FROM Contract.ContractRolePolicies p
    WHERE p.ContractID = @ContractID
      AND p.RoleID = @RoleID;

    IF @Allowed IS NULL
    BEGIN
        SELECT CAST(0 AS BIT) AS IsAllowed, @ContractID AS ContractID, N'ROLE_NOT_ALLOWED_BY_CONTRACT' AS ReasonCode, CAST(0 AS BIT) AS RequiresApproval;
        RETURN 50032;
    END

    IF NOT
    (
        @Allowed = 'BOTH'
        OR (@Allowed = 'GLOBAL' AND @GrantType = 'GLOBAL')
        OR (@Allowed = 'SCOPED' AND @GrantType = 'SCOPED')
    )
    BEGIN
        SELECT CAST(0 AS BIT) AS IsAllowed, @ContractID AS ContractID, N'GRANT_TYPE_NOT_ALLOWED_BY_CONTRACT' AS ReasonCode, @RequiresApproval AS RequiresApproval;
        RETURN 50033;
    END

    SELECT CAST(1 AS BIT) AS IsAllowed, @ContractID AS ContractID, N'OK' AS ReasonCode, @RequiresApproval AS RequiresApproval;
    RETURN 0;
END
GO

--
-- Create table [Contract].[ContractLimits]
--
PRINT (N'Create table [Contract].[ContractLimits]')
GO
CREATE TABLE Contract.ContractLimits (
  ContractLimitID bigint IDENTITY,
  ContractID int NOT NULL,
  ScopeID int NULL,
  MaxActiveUsers int NULL,
  MaxStorageGB decimal(18, 2) NULL,
  MaxRunsPerMonth int NULL,
  CreatedAtUtc datetime2(3) NOT NULL CONSTRAINT DF_ContractLimits_CreatedAtUtc DEFAULT (sysutcdatetime()),
  CONSTRAINT PK_Contract_ContractLimits PRIMARY KEY CLUSTERED (ContractLimitID)
)
GO

--
-- Create index [UX_ContractLimits_GlobalPerContract] on table [Contract].[ContractLimits]
--
PRINT (N'Create index [UX_ContractLimits_GlobalPerContract] on table [Contract].[ContractLimits]')
GO
CREATE UNIQUE INDEX UX_ContractLimits_GlobalPerContract
  ON Contract.ContractLimits (ContractID)
  WHERE ([ScopeID] IS NULL)
GO

--
-- Create index [UX_ContractLimits_PerScope] on table [Contract].[ContractLimits]
--
PRINT (N'Create index [UX_ContractLimits_PerScope] on table [Contract].[ContractLimits]')
GO
CREATE UNIQUE INDEX UX_ContractLimits_PerScope
  ON Contract.ContractLimits (ContractID, ScopeID)
  WHERE ([ScopeID] IS NOT NULL)
GO

--
-- Create foreign key [FK_ContractLimits_ContractID] on table [Contract].[ContractLimits]
--
PRINT (N'Create foreign key [FK_ContractLimits_ContractID] on table [Contract].[ContractLimits]')
GO
ALTER TABLE Contract.ContractLimits
  ADD CONSTRAINT FK_ContractLimits_ContractID FOREIGN KEY (ContractID) REFERENCES Contract.Contracts (ContractID)
GO

--
-- Create schema [Onboarding]
--
PRINT (N'Create schema [Onboarding]')
GO
CREATE SCHEMA Onboarding AUTHORIZATION dbo
GO

--
-- Create table [Onboarding].[InvitationBatches]
--
PRINT (N'Create table [Onboarding].[InvitationBatches]')
GO
CREATE TABLE Onboarding.InvitationBatches (
  BatchID bigint IDENTITY,
  ScopeID int NOT NULL,
  CreatedByUserID int NOT NULL,
  Status varchar(16) NOT NULL CONSTRAINT DF_InvitationBatches_Status DEFAULT ('OPEN'),
  CreatedAtUtc datetime2(3) NOT NULL CONSTRAINT DF_InvitationBatches_CreatedAtUtc DEFAULT (sysutcdatetime()),
  CONSTRAINT PK_Onboarding_InvitationBatches PRIMARY KEY CLUSTERED (BatchID),
  CONSTRAINT CK_InvitationBatches_Status CHECK ([Status]='CANCELLED' OR [Status]='CLOSED' OR [Status]='OPEN')
)
GO

--
-- Create foreign key [FK_InvitationBatches_CreatedByUserID] on table [Onboarding].[InvitationBatches]
--
PRINT (N'Create foreign key [FK_InvitationBatches_CreatedByUserID] on table [Onboarding].[InvitationBatches]')
GO
ALTER TABLE Onboarding.InvitationBatches
  ADD CONSTRAINT FK_InvitationBatches_CreatedByUserID FOREIGN KEY (CreatedByUserID) REFERENCES RBAC.Users (ID)
GO

--
-- Create foreign key [FK_InvitationBatches_ScopeID] on table [Onboarding].[InvitationBatches]
--
PRINT (N'Create foreign key [FK_InvitationBatches_ScopeID] on table [Onboarding].[InvitationBatches]')
GO
ALTER TABLE Onboarding.InvitationBatches
  ADD CONSTRAINT FK_InvitationBatches_ScopeID FOREIGN KEY (ScopeID) REFERENCES RBAC.Scopes (ScopeID)
GO

--
-- Create table [Onboarding].[Invitations]
--
PRINT (N'Create table [Onboarding].[Invitations]')
GO
CREATE TABLE Onboarding.Invitations (
  InvitationID bigint IDENTITY,
  BatchID bigint NULL,
  ScopeID int NOT NULL,
  Email varchar(255) NOT NULL,
  ExpectedProviderID int NOT NULL,
  ExpectedDomain varchar(255) NULL,
  DefaultRoleID int NULL,
  GrantType varchar(16) NOT NULL CONSTRAINT DF_Invitations_GrantType DEFAULT ('SCOPED'),
  ExpiresAtUtc datetime2(3) NOT NULL,
  Status varchar(16) NOT NULL CONSTRAINT DF_Invitations_Status DEFAULT ('PENDING'),
  AcceptedByUserID int NULL,
  AcceptedAtUtc datetime2(3) NULL,
  CreatedAtUtc datetime2(3) NOT NULL CONSTRAINT DF_Invitations_CreatedAtUtc DEFAULT (sysutcdatetime()),
  CONSTRAINT PK_Onboarding_Invitations PRIMARY KEY CLUSTERED (InvitationID),
  CONSTRAINT CK_Invitations_GrantType CHECK ([GrantType]='SCOPED' OR [GrantType]='GLOBAL'),
  CONSTRAINT CK_Invitations_Status CHECK ([Status]='REVOKED' OR [Status]='CANCELLED' OR [Status]='EXPIRED' OR [Status]='ACCEPTED' OR [Status]='PENDING')
)
GO

--
-- Create index [IX_Invitations_Email] on table [Onboarding].[Invitations]
--
PRINT (N'Create index [IX_Invitations_Email] on table [Onboarding].[Invitations]')
GO
CREATE INDEX IX_Invitations_Email
  ON Onboarding.Invitations (Email, Status)
GO

--
-- Create index [IX_Invitations_Scope_Status] on table [Onboarding].[Invitations]
--
PRINT (N'Create index [IX_Invitations_Scope_Status] on table [Onboarding].[Invitations]')
GO
CREATE INDEX IX_Invitations_Scope_Status
  ON Onboarding.Invitations (ScopeID, Status, ExpiresAtUtc)
GO

--
-- Create foreign key [FK_Invitations_AcceptedByUserID] on table [Onboarding].[Invitations]
--
PRINT (N'Create foreign key [FK_Invitations_AcceptedByUserID] on table [Onboarding].[Invitations]')
GO
ALTER TABLE Onboarding.Invitations
  ADD CONSTRAINT FK_Invitations_AcceptedByUserID FOREIGN KEY (AcceptedByUserID) REFERENCES RBAC.Users (ID)
GO

--
-- Create foreign key [FK_Invitations_BatchID] on table [Onboarding].[Invitations]
--
PRINT (N'Create foreign key [FK_Invitations_BatchID] on table [Onboarding].[Invitations]')
GO
ALTER TABLE Onboarding.Invitations
  ADD CONSTRAINT FK_Invitations_BatchID FOREIGN KEY (BatchID) REFERENCES Onboarding.InvitationBatches (BatchID)
GO

--
-- Create foreign key [FK_Invitations_DefaultRoleID] on table [Onboarding].[Invitations]
--
PRINT (N'Create foreign key [FK_Invitations_DefaultRoleID] on table [Onboarding].[Invitations]')
GO
ALTER TABLE Onboarding.Invitations
  ADD CONSTRAINT FK_Invitations_DefaultRoleID FOREIGN KEY (DefaultRoleID) REFERENCES RBAC.Roles (ID)
GO

--
-- Create foreign key [FK_Invitations_ExpectedProviderID] on table [Onboarding].[Invitations]
--
PRINT (N'Create foreign key [FK_Invitations_ExpectedProviderID] on table [Onboarding].[Invitations]')
GO
ALTER TABLE Onboarding.Invitations
  ADD CONSTRAINT FK_Invitations_ExpectedProviderID FOREIGN KEY (ExpectedProviderID) REFERENCES RBAC.IdentityProviders (ProviderID)
GO

--
-- Create foreign key [FK_Invitations_ScopeID] on table [Onboarding].[Invitations]
--
PRINT (N'Create foreign key [FK_Invitations_ScopeID] on table [Onboarding].[Invitations]')
GO
ALTER TABLE Onboarding.Invitations
  ADD CONSTRAINT FK_Invitations_ScopeID FOREIGN KEY (ScopeID) REFERENCES RBAC.Scopes (ScopeID)
GO

--
-- Create table [Onboarding].[AuditLog]
--
PRINT (N'Create table [Onboarding].[AuditLog]')
GO
CREATE TABLE Onboarding.AuditLog (
  AuditID bigint IDENTITY,
  ScopeID int NULL,
  InvitationID bigint NULL,
  ActorUserID int NULL,
  EventType varchar(64) NOT NULL,
  EventDataJson json NULL,
  CreatedAtUtc datetime2(3) NOT NULL CONSTRAINT DF_OnboardingAudit_CreatedAtUtc DEFAULT (sysutcdatetime()),
  CONSTRAINT PK_Onboarding_AuditLog PRIMARY KEY CLUSTERED (AuditID)
)
GO

--
-- Create index [IX_OnboardingAudit_Scope_Time] on table [Onboarding].[AuditLog]
--
PRINT (N'Create index [IX_OnboardingAudit_Scope_Time] on table [Onboarding].[AuditLog]')
GO
CREATE INDEX IX_OnboardingAudit_Scope_Time
  ON Onboarding.AuditLog (ScopeID, CreatedAtUtc)
GO

--
-- Create foreign key [FK_OnboardingAudit_ActorUserID] on table [Onboarding].[AuditLog]
--
PRINT (N'Create foreign key [FK_OnboardingAudit_ActorUserID] on table [Onboarding].[AuditLog]')
GO
ALTER TABLE Onboarding.AuditLog
  ADD CONSTRAINT FK_OnboardingAudit_ActorUserID FOREIGN KEY (ActorUserID) REFERENCES RBAC.Users (ID)
GO

--
-- Create foreign key [FK_OnboardingAudit_InvitationID] on table [Onboarding].[AuditLog]
--
PRINT (N'Create foreign key [FK_OnboardingAudit_InvitationID] on table [Onboarding].[AuditLog]')
GO
ALTER TABLE Onboarding.AuditLog
  ADD CONSTRAINT FK_OnboardingAudit_InvitationID FOREIGN KEY (InvitationID) REFERENCES Onboarding.Invitations (InvitationID)
GO

--
-- Create foreign key [FK_OnboardingAudit_ScopeID] on table [Onboarding].[AuditLog]
--
PRINT (N'Create foreign key [FK_OnboardingAudit_ScopeID] on table [Onboarding].[AuditLog]')
GO
ALTER TABLE Onboarding.AuditLog
  ADD CONSTRAINT FK_OnboardingAudit_ScopeID FOREIGN KEY (ScopeID) REFERENCES RBAC.Scopes (ScopeID)
GO

--
-- Create or alter procedure [Onboarding].[spAcceptInvitation]
--
GO
PRINT (N'Create or alter procedure [Onboarding].[spAcceptInvitation]')
GO

CREATE OR ALTER PROCEDURE Onboarding.spAcceptInvitation
    @InvitationID BIGINT,
    @Issuer NVARCHAR(512),
    @Subject NVARCHAR(512),
    @Email VARCHAR(255),
    @FullName VARCHAR(200),
    @UserID INT OUTPUT,
    @UtcNow DATETIME2(3) = NULL
AS
BEGIN
    SET NOCOUNT ON;
    SET XACT_ABORT ON;

    IF @UtcNow IS NULL SET @UtcNow = SYSUTCDATETIME();

    -- Validate required inputs before opening the transaction.
    IF @InvitationID IS NULL OR @InvitationID <= 0
       OR @Issuer IS NULL OR LTRIM(RTRIM(@Issuer)) = N''
       OR @Subject IS NULL OR LTRIM(RTRIM(@Subject)) = N''
       OR @Email IS NULL OR LTRIM(RTRIM(@Email)) = ''
       OR @FullName IS NULL OR LTRIM(RTRIM(@FullName)) = ''
    BEGIN
        THROW 50001, 'ValidationError: InvitationID, Issuer, Subject, Email and FullName are required.', 1;
    END

    DECLARE @NormalizedEmail VARCHAR(255) = LOWER(LTRIM(RTRIM(@Email)));
    DECLARE @NormalizedInvitationEmail VARCHAR(255);
    DECLARE @ScopeID INT;
    DECLARE @ExpectedProviderID INT;
    DECLARE @ExpectedDomain VARCHAR(255);
    DECLARE @DefaultRoleID INT;
    DECLARE @GrantType VARCHAR(16);
    DECLARE @InvitationStatus VARCHAR(16);
    DECLARE @ExpiresAtUtc DATETIME2(3);
    DECLARE @ContractID INT = NULL;
    DECLARE @RoleAllowed BIT = 1;
    DECLARE @RoleRequiresApproval BIT = 0;
    DECLARE @RoleReason NVARCHAR(64) = N'OK';
    DECLARE @GrantedRoleID INT = NULL;
    DECLARE @GrantedScopeID INT = NULL;

    SET @UserID = NULL;

    BEGIN TRY
        BEGIN TRANSACTION;

        SELECT
            @ScopeID = i.[ScopeID],
            @NormalizedInvitationEmail = LOWER(LTRIM(RTRIM(i.[Email]))),
            @ExpectedProviderID = i.[ExpectedProviderID],
            @ExpectedDomain = i.[ExpectedDomain],
            @DefaultRoleID = i.[DefaultRoleID],
            @GrantType = i.[GrantType],
            @InvitationStatus = i.[Status],
            @ExpiresAtUtc = i.[ExpiresAtUtc]
        FROM [Onboarding].[Invitations] i WITH (UPDLOCK, HOLDLOCK)
        WHERE i.[InvitationID] = @InvitationID;

        -- Validate invitation existence.
        IF @ScopeID IS NULL
            THROW 50010, 'BusinessRule: Invitation not found.', 1;

        -- Validate invitation lifecycle state.
        IF @InvitationStatus <> 'PENDING'
            THROW 50011, 'BusinessRule: Invitation is not pending.', 1;

        -- Validate invitation expiration and mark stale pending rows as expired.
        IF @ExpiresAtUtc < @UtcNow
        BEGIN
            UPDATE [Onboarding].[Invitations]
            SET [Status] = 'EXPIRED'
            WHERE [InvitationID] = @InvitationID
              AND [Status] = 'PENDING';

            THROW 50012, 'BusinessRule: Invitation has expired.', 1;
        END

        -- Validate that authenticated email matches invited email.
        IF @NormalizedInvitationEmail <> @NormalizedEmail
            THROW 50013, 'BusinessRule: Invitation email does not match authenticated email.', 1;

        -- Validate issuer/provider consistency for the invitation.
        IF NOT EXISTS
        (
            SELECT 1
            FROM [RBAC].[IdentityProviders] ip
            WHERE ip.[ProviderID] = @ExpectedProviderID
              AND ip.[Active] = 1
              AND ip.[Issuer] = @Issuer
        )
            THROW 50014, 'BusinessRule: Invitation provider does not match authenticated issuer.', 1;

        -- Validate that the scope is currently linked to the expected provider.
        IF NOT EXISTS
        (
            SELECT 1
            FROM [RBAC].[ScopeIdentityProviders] sip
            WHERE sip.[ScopeID] = @ScopeID
              AND sip.[ProviderID] = @ExpectedProviderID
              AND sip.[Active] = 1
        )
            THROW 50015, 'BusinessRule: Scope is not active for invitation provider.', 1;

        -- Validate optional email domain policy from the invitation.
        IF @ExpectedDomain IS NOT NULL
           AND RIGHT(@NormalizedEmail, LEN(@ExpectedDomain) + 1) <> '@' + LOWER(@ExpectedDomain)
            THROW 50016, 'BusinessRule: Authenticated email is outside expected domain.', 1;

        DECLARE @ScopeAccess TABLE (IsAllowed BIT, ContractID INT, ReasonCode NVARCHAR(64));
        INSERT INTO @ScopeAccess
        EXEC [Contract].[spContractValidateScopeAccess]
            @ScopeID = @ScopeID,
            @UtcNow = @UtcNow;

        SELECT TOP (1)
            @ContractID = sa.[ContractID]
        FROM @ScopeAccess sa
        WHERE sa.[IsAllowed] = 1;

        -- Validate active contract coverage for the invited scope.
        IF @ContractID IS NULL
            THROW 50017, 'BusinessRule: Scope has no active contract for invitation acceptance.', 1;

        SELECT
            @UserID = u.[ID]
        FROM [RBAC].[Users] u WITH (UPDLOCK, HOLDLOCK)
        WHERE u.[email] = @NormalizedEmail;

        IF @UserID IS NULL
        BEGIN
            INSERT INTO [RBAC].[Users] ([FullName], [email], [Active])
            VALUES (@FullName, @NormalizedEmail, 1);
            SET @UserID = SCOPE_IDENTITY();
        END
        ELSE
        BEGIN
            UPDATE [RBAC].[Users]
            SET [FullName] = @FullName,
                [Active] = 1
            WHERE [ID] = @UserID
              AND ([FullName] <> @FullName OR [Active] <> 1);
        END

        DECLARE @ExistingIdentityUserID INT = NULL;
        SELECT
            @ExistingIdentityUserID = ei.[UserID]
        FROM [RBAC].[ExternalIdentities] ei WITH (UPDLOCK, HOLDLOCK)
        WHERE ei.[ProviderID] = @ExpectedProviderID
          AND ei.[Subject] = @Subject;

        -- Validate that external identity is not already bound to another user.
        IF @ExistingIdentityUserID IS NOT NULL AND @ExistingIdentityUserID <> @UserID
            THROW 50018, 'BusinessRule: External identity is already bound to a different user.', 1;

        IF @ExistingIdentityUserID IS NULL
        BEGIN
            INSERT INTO [RBAC].[ExternalIdentities]
            (
                [UserID],
                [ProviderID],
                [Subject],
                [EmailSnapshot],
                [Active],
                [LastLoginAtUtc]
            )
            VALUES
            (
                @UserID,
                @ExpectedProviderID,
                @Subject,
                @NormalizedEmail,
                1,
                @UtcNow
            );
        END
        ELSE
        BEGIN
            UPDATE [RBAC].[ExternalIdentities]
            SET [UserID] = @UserID,
                [EmailSnapshot] = @NormalizedEmail,
                [Active] = 1,
                [LastLoginAtUtc] = @UtcNow
            WHERE [ProviderID] = @ExpectedProviderID
              AND [Subject] = @Subject;
        END

        IF @DefaultRoleID IS NOT NULL
        BEGIN
            DECLARE @RoleValidation TABLE (IsAllowed BIT, ContractID INT, ReasonCode NVARCHAR(64), RequiresApproval BIT);
            INSERT INTO @RoleValidation
            EXEC [Contract].[spContractValidateRoleGrant]
                @ScopeID = @ScopeID,
                @RoleID = @DefaultRoleID,
                @GrantType = @GrantType,
                @UtcNow = @UtcNow;

            SELECT TOP (1)
                @RoleAllowed = rv.[IsAllowed],
                @RoleRequiresApproval = rv.[RequiresApproval],
                @RoleReason = rv.[ReasonCode]
            FROM @RoleValidation rv;

            -- Validate role/grant entitlement against contract policies.
            IF ISNULL(@RoleAllowed, 0) = 0
                THROW 50019, 'BusinessRule: Invitation role/grant is not allowed by contract.', 1;

            -- Validate whether policy requires an additional approval workflow.
            IF ISNULL(@RoleRequiresApproval, 0) = 1
                THROW 50020, 'BusinessRule: Invitation role grant requires manual approval.', 1;

            IF @GrantType = 'SCOPED'
                SET @GrantedScopeID = @ScopeID;
            ELSE
                SET @GrantedScopeID = NULL;

            IF NOT EXISTS
            (
                SELECT 1
                FROM [RBAC].[UserRoleGrants] urg
                WHERE urg.[UserID] = @UserID
                  AND urg.[RoleID] = @DefaultRoleID
                  AND urg.[GrantType] = @GrantType
                  AND
                  (
                      (urg.[ScopeID] = @GrantedScopeID)
                      OR (urg.[ScopeID] IS NULL AND @GrantedScopeID IS NULL)
                  )
                  AND urg.[Active] = 1
            )
            BEGIN
                INSERT INTO [RBAC].[UserRoleGrants]
                (
                    [UserID],
                    [RoleID],
                    [GrantType],
                    [ScopeID],
                    [Active]
                )
                VALUES
                (
                    @UserID,
                    @DefaultRoleID,
                    @GrantType,
                    @GrantedScopeID,
                    1
                );
            END

            SET @GrantedRoleID = @DefaultRoleID;
        END

        UPDATE [Onboarding].[Invitations]
        SET [Status] = 'ACCEPTED',
            [AcceptedByUserID] = @UserID,
            [AcceptedAtUtc] = @UtcNow
        WHERE [InvitationID] = @InvitationID;

        INSERT INTO [Onboarding].[AuditLog]
        (
            [ScopeID],
            [InvitationID],
            [ActorUserID],
            [EventType],
            [EventDataJson]
        )
        VALUES
        (
            @ScopeID,
            @InvitationID,
            @UserID,
            'INVITATION_ACCEPTED',
            (
                SELECT
                    @ContractID AS ContractID,
                    @ExpectedProviderID AS ProviderID,
                    @GrantedRoleID AS GrantedRoleID,
                    @GrantType AS GrantType,
                    @RoleReason AS RolePolicyReasonCode
                FOR JSON PATH, WITHOUT_ARRAY_WRAPPER
            )
        );

        COMMIT TRANSACTION;

        SELECT
            @InvitationID AS InvitationID,
            @UserID AS UserID,
            @ScopeID AS ScopeID,
            @ExpectedProviderID AS ProviderID,
            @ContractID AS ContractID,
            @GrantedRoleID AS GrantedRoleID,
            @GrantType AS GrantType,
            CAST(CASE WHEN @GrantedRoleID IS NULL THEN 0 ELSE 1 END AS BIT) AS RoleGrantApplied;

        RETURN 0;
    END TRY
    BEGIN CATCH
        IF XACT_STATE() <> 0 ROLLBACK TRANSACTION;
        THROW;
    END CATCH
END
GO

--
-- Create table [Onboarding].[ApprovalQueue]
--
PRINT (N'Create table [Onboarding].[ApprovalQueue]')
GO
CREATE TABLE Onboarding.ApprovalQueue (
  ApprovalID bigint IDENTITY,
  InvitationID bigint NOT NULL,
  RequestedRoleID int NOT NULL,
  RequestedGrantType varchar(16) NOT NULL,
  Status varchar(16) NOT NULL CONSTRAINT DF_ApprovalQueue_Status DEFAULT ('PENDING'),
  ReviewedByUserID int NULL,
  ReviewedAtUtc datetime2(3) NULL,
  ReviewComment varchar(500) NULL,
  CreatedAtUtc datetime2(3) NOT NULL CONSTRAINT DF_ApprovalQueue_CreatedAtUtc DEFAULT (sysutcdatetime()),
  CONSTRAINT PK_Onboarding_ApprovalQueue PRIMARY KEY CLUSTERED (ApprovalID),
  CONSTRAINT CK_ApprovalQueue_GrantType CHECK ([RequestedGrantType]='SCOPED' OR [RequestedGrantType]='GLOBAL'),
  CONSTRAINT CK_ApprovalQueue_Status CHECK ([Status]='CANCELLED' OR [Status]='REJECTED' OR [Status]='APPROVED' OR [Status]='PENDING')
)
GO

--
-- Create foreign key [FK_ApprovalQueue_InvitationID] on table [Onboarding].[ApprovalQueue]
--
PRINT (N'Create foreign key [FK_ApprovalQueue_InvitationID] on table [Onboarding].[ApprovalQueue]')
GO
ALTER TABLE Onboarding.ApprovalQueue
  ADD CONSTRAINT FK_ApprovalQueue_InvitationID FOREIGN KEY (InvitationID) REFERENCES Onboarding.Invitations (InvitationID)
GO

--
-- Create foreign key [FK_ApprovalQueue_RequestedRoleID] on table [Onboarding].[ApprovalQueue]
--
PRINT (N'Create foreign key [FK_ApprovalQueue_RequestedRoleID] on table [Onboarding].[ApprovalQueue]')
GO
ALTER TABLE Onboarding.ApprovalQueue
  ADD CONSTRAINT FK_ApprovalQueue_RequestedRoleID FOREIGN KEY (RequestedRoleID) REFERENCES RBAC.Roles (ID)
GO

--
-- Create foreign key [FK_ApprovalQueue_ReviewedByUserID] on table [Onboarding].[ApprovalQueue]
--
PRINT (N'Create foreign key [FK_ApprovalQueue_ReviewedByUserID] on table [Onboarding].[ApprovalQueue]')
GO
ALTER TABLE Onboarding.ApprovalQueue
  ADD CONSTRAINT FK_ApprovalQueue_ReviewedByUserID FOREIGN KEY (ReviewedByUserID) REFERENCES RBAC.Users (ID)
GO

--
-- Create schema [e_portal]
--
PRINT (N'Create schema [e_portal]')
GO
CREATE SCHEMA e_portal AUTHORIZATION dbo
GO

--
-- Create or alter procedure [e_portal].[spSessionSetUserId]
--
GO
PRINT (N'Create or alter procedure [e_portal].[spSessionSetUserId]')
GO
CREATE OR ALTER PROCEDURE e_portal.spSessionSetUserId(@uniSessionId VARCHAR(32), @UserId BIGINT)
AS
BEGIN
  DECLARE @LastUpdate DATETIME = (SELECT MAX(StartDate) FROM EPSessions WHERE sqlSessionId = @@SPID);

  UPDATE EPSessions
  SET
    UserId = @UserId
  WHERE (uniSessionId = @uniSessionId) AND (StartDate = @LastUpdate);
END
GO

--
-- Create or alter procedure [e_portal].[spRoleGrant]
--
GO
PRINT (N'Create or alter procedure [e_portal].[spRoleGrant]')
GO
CREATE OR ALTER PROCEDURE e_portal.spRoleGrant(@NodeId INT, @AppRoleId TINYINT)
AS
BEGIN
  DECLARE @Id INT = @NodeId;

  WHILE @NodeId IS NOT NULL AND (@Id >= 0)
  BEGIN
    IF NOT EXISTS (SELECT * FROM e_portal.AppRole2Node WHERE AppRoleId = @AppRoleId AND NodeId = @Id)
      INSERT INTO e_portal.AppRole2Node(AppRoleId, NodeId)
      VALUES(@AppRoleId, @Id);

    SET @Id = (SELECT ParentId FROM e_portal.NavTree WHERE Id = @Id);
  END;

  DECLARE @ChildrenQty INT = (SELECT COUNT(*) FROM e_portal.NavTree WHERE ParentId = @NodeId);
  SET @Id = -1;
  WHILE @ChildrenQty > 0
  BEGIN
    SET @Id = (SELECT MIN(Id) FROM e_portal.NavTree WHERE ParentId = @NodeId AND Id > @Id);

    EXEC spRoleGrant @Id, @AppRoleId;

    SET @ChildrenQty = @ChildrenQty - 1;
  END;

  RETURN 0;
END;
GO

--
-- Create or alter procedure [e_portal].[spRoleDeny]
--
GO
PRINT (N'Create or alter procedure [e_portal].[spRoleDeny]')
GO
CREATE OR ALTER PROCEDURE e_portal.spRoleDeny(@NodeId INT, @AppRoleId TINYINT)
AS
BEGIN
  DELETE FROM e_portal.AppRole2Node
  WHERE AppRoleId = @AppRoleId AND NodeId = @NodeId;

  DECLARE @ChildrenQty INT = (SELECT COUNT(*) FROM e_portal.NavTree WHERE ParentId = @NodeId);
  DECLARE @Id INT = -1;
  WHILE @ChildrenQty > 0
  BEGIN
    SET @Id = (SELECT MIN(Id) FROM e_portal.NavTree WHERE ParentId = @NodeId AND Id > @Id);

    EXEC e_portal.spRoleDeny @Id, @AppRoleId;

    SET @ChildrenQty = @ChildrenQty - 1;
  END;

  RETURN 0;
END;
GO

--
-- Create or alter procedure [e_portal].[spRemoveUserRole]
--
GO
PRINT (N'Create or alter procedure [e_portal].[spRemoveUserRole]')
GO
CREATE OR ALTER PROCEDURE e_portal.spRemoveUserRole (
  @UserId BIGINT,
  @RoleId BIGINT
) AS
BEGIN
  SET NOCOUNT ON;

  DELETE FROM e_portal.UserRoles
  WHERE UserId = @UserId AND RoleId = @RoleId;

  RETURN 0;
END
GO

--
-- Create or alter procedure [e_portal].[spNodeSetName]
--
GO
PRINT (N'Create or alter procedure [e_portal].[spNodeSetName]')
GO
CREATE OR ALTER PROCEDURE e_portal.spNodeSetName(@ID INTEGER, @Name VARCHAR(128))
AS
BEGIN
  UPDATE e_portal.NavTree
  SET
    Caption = @Name
  WHERE ID = @ID;
END;
GO

--
-- Create or alter procedure [e_portal].[spNodeNewChild]
--
GO
PRINT (N'Create or alter procedure [e_portal].[spNodeNewChild]')
GO
CREATE OR ALTER PROCEDURE e_portal.spNodeNewChild(@ParentID INTEGER, @ID INTEGER OUTPUT)
AS
BEGIN
  BEGIN TRANSACTION;

  DECLARE @Children INTEGER = (SELECT COUNT(*) FROM e_portal.NavTree WHERE ParentID = @ParentID);

  INSERT INTO e_portal.NavTree(ParentID, Caption, Seq)
  VALUES(@ParentID, 'New Node', @Children + 1);

  SET @ID = SCOPE_IDENTITY();

  COMMIT TRANSACTION;
END;
GO

--
-- Create or alter procedure [e_portal].[spNodeMove]
--
GO
PRINT (N'Create or alter procedure [e_portal].[spNodeMove]')
GO
CREATE OR ALTER PROCEDURE e_portal.spNodeMove(@SrcID INTEGER, @DstID INTEGER)
AS
BEGIN
  DECLARE @SrcSeq INTEGER = (SELECT Seq FROM e_portal.NavTree WHERE ID = @SrcID);
  DECLARE @DstSeq INTEGER = (SELECT Seq FROM e_portal.NavTree WHERE ID = @DstID);

  BEGIN TRANSACTION;

  UPDATE e_portal.NavTree
  SET
    Seq = @DstSeq
  WHERE ID = @SrcID;

  UPDATE e_portal.NavTree
  SET
    Seq = @SrcSeq
  WHERE ID = @DstID;

  COMMIT TRANSACTION;
END;
GO

--
-- Create or alter procedure [e_portal].[spNodeDelete]
--
GO
PRINT (N'Create or alter procedure [e_portal].[spNodeDelete]')
GO
CREATE OR ALTER PROCEDURE e_portal.spNodeDelete(@ID INTEGER)
AS
BEGIN
  DECLARE @ParentID INTEGER = (SELECT ParentID FROM e_portal.NavTree WHERE ID = @ID);
  DECLARE @Seq      INTEGER = (SELECT Seq FROM e_portal.NavTree WHERE ID = @ID);

  BEGIN TRANSACTION;

  DELETE FROM e_portal.NavTree
  WHERE ID = @ID;

  UPDATE e_portal.NavTree
  SET
    Seq = Seq - 1
  WHERE ParentID = @ParentID AND Seq > @Seq;

  COMMIT TRANSACTION;
END;
GO

--
-- Create or alter procedure [e_portal].[spNodeCutPaste]
--
GO
PRINT (N'Create or alter procedure [e_portal].[spNodeCutPaste]')
GO
CREATE OR ALTER PROCEDURE e_portal.spNodeCutPaste(@SrcID INTEGER, @DstID INTEGER)
AS
BEGIN
  DECLARE @SrcParentID INTEGER = (SELECT ParentID FROM e_portal.NavTree WHERE ID = @SrcID);
  DECLARE @OldSrcSeq   INTEGER = (SELECT Seq FROM e_portal.NavTree WHERE ID = @SrcID);
  DECLARE @NewDstSeq   INTEGER = COALESCE((SELECT MAX(Seq) FROM e_portal.NavTree WHERE ParentID = @DstID), 0) + 1;

  BEGIN TRANSACTION;

  UPDATE e_portal.NavTree
  SET
    ParentID = @DstID,
    Seq = @NewDstSeq
  WHERE ID = @SrcID;

  UPDATE e_portal.NavTree
  SET
    Seq = Seq - 1
  WHERE ParentID = @SrcParentID AND Seq > @OldSrcSeq;

  COMMIT TRANSACTION;
END;
GO

--
-- Create or alter procedure [e_portal].[spInsertFromNavTree]
--
GO
PRINT (N'Create or alter procedure [e_portal].[spInsertFromNavTree]')
GO
CREATE OR ALTER PROCEDURE e_portal.spInsertFromNavTree(@ID SMALLINT)
AS
BEGIN
  INSERT INTO #Tree(ID, ParentID, Caption, Seq, InfoID)
    SELECT * FROM e_portal.NavTree WHERE ID = @ID;

  DECLARE @ChildrenQty SMALLINT = 0;
  DECLARE @ChildID     SMALLINT = NULL;
  DECLARE @Seq         SMALLINT = -1;

  SET @ChildrenQty = (SELECT COUNT(*) FROM e_portal.NavTree WHERE ParentID = @ID);
  WHILE @ChildrenQty > 0
  BEGIN
    SET @Seq = (SELECT MIN(Seq) FROM e_portal.NavTree WHERE ParentID = @ID AND Seq > @Seq);
    SET @ChildID = (SELECT TOP 1 ID FROM e_portal.NavTree WHERE ParentID = @ID AND Seq = @Seq AND ID NOT IN (SELECT ID FROM #Tree));

    EXEC e_portal.spInsertFromNavTree @ChildID;

    SET @ChildrenQty = @ChildrenQty - 1;
  END;
END;
GO

--
-- Create or alter procedure [e_portal].[spGetUserNavTree]
--
GO
PRINT (N'Create or alter procedure [e_portal].[spGetUserNavTree]')
GO
CREATE OR ALTER PROCEDURE e_portal.spGetUserNavTree (@UserId BIGINT, @AppRoleId TINYINT)
AS
BEGIN
  CREATE TABLE #Tree
  (
    Idx       INT IDENTITY,
    ID        smallint,
    ParentID  smallint NULL,
    Caption   varchar(50) NOT NULL,
    Seq       smallint NOT NULL,
    InfoID    int NULL
  );

  EXEC e_portal.spInsertFromNavTree 1;

  WITH UserAuthorizedNodes(NodeId) AS
  (
    SELECT
      NodeId
    FROM
      e_portal.AppRole2Node R
      JOIN e_portal.viewUserAllRoles U ON R.AppRoleId = U.RoleId AND U.UserId = @UserId
    WHERE R.AppRoleId = @AppRoleId
  )
  SELECT
    T.ID,
    T.ParentID,
    T.Caption,
    T.Seq,
    T.InfoID
  FROM #Tree T
  WHERE T.ID IN (SELECT NodeID FROM UserAuthorizedNodes)
  ORDER BY T.Idx;
END;
GO

--
-- Create or alter procedure [e_portal].[spGetNavTree]
--
GO
PRINT (N'Create or alter procedure [e_portal].[spGetNavTree]')
GO
CREATE OR ALTER PROCEDURE e_portal.spGetNavTree(@RootID SMALLINT)
AS
BEGIN
  CREATE TABLE #Tree
  (
    ID        smallint,
    ParentID  smallint NULL,
    Caption   varchar(50) NOT NULL,
    Seq       smallint NOT NULL,
    InfoID    int NULL
  );

  EXEC e_portal.spInsertFromNavTree @RootID;

  SELECT ID, ParentID, Caption, Seq, InfoID AS FormID FROM #Tree;
END;
GO

--
-- Create or alter procedure [e_portal].[spGetUserRoles]
--
GO
PRINT (N'Create or alter procedure [e_portal].[spGetUserRoles]')
GO
CREATE OR ALTER PROCEDURE e_portal.spGetUserRoles (
  @UserId BIGINT
) AS
BEGIN
  SET NOCOUNT ON;

  ;WITH Permissions (UserId, RoleId, Role, Included) AS
  (
    SELECT
      @UserId,
      r.RoleId,
      r.Name AS Role,
      0 AS Included
    FROM
      e_portal.Roles r
    UNION
    SELECT
      ur.UserId,
      arr.RoleId,
      r.Name AS Role,
      1 AS Included
    FROM
      e_portal.UserRoles ur
      JOIN e_portal.Roles r ON ur.RoleId = r.RoleId
      LEFT JOIN e_portal.AppRole2Roles arr ON r.RoleId = arr.RoleId
    WHERE
      ur.UserId = @UserId
  )
  SELECT
    UserId,
    RoleId,
    Role,
    CAST(MAX(Included) AS BIT) AS Included
  FROM
    Permissions
  GROUP BY
    UserId,
    RoleId,
    Role;
END
GO

--
-- Create or alter procedure [e_portal].[spGetUserIdByEmail]
--
GO
PRINT (N'Create or alter procedure [e_portal].[spGetUserIdByEmail]')
GO
CREATE OR ALTER PROCEDURE e_portal.spGetUserIdByEmail(@Email varchar(128), @UserId bigint output)
AS
BEGIN
  SET @UserId = (SELECT UserId FROM [e_portal].SyUsers WHERE Email = @Email);
END
GO

--
-- Create or alter procedure [e_portal].[spGetUserAppRoles]
--
GO
PRINT (N'Create or alter procedure [e_portal].[spGetUserAppRoles]')
GO
CREATE OR ALTER PROCEDURE e_portal.spGetUserAppRoles (@UserId BIGINT)
AS
BEGIN
  SELECT
    U.UserId,
    R.RoleId,
    R.Name AS Role
  FROM
    e_portal.SyUsers U
    JOIN e_portal.UserRoles UR ON UR.UserId = U.UserId
    JOIN e_portal.Roles R ON R.RoleId = UR.RoleId
  WHERE
    U.UserId = @UserId;
END;
GO

--
-- Create or alter procedure [e_portal].[spGetSamplesByPatient]
--
GO
PRINT (N'Create or alter procedure [e_portal].[spGetSamplesByPatient]')
GO
CREATE OR ALTER PROCEDURE e_portal.spGetSamplesByPatient
  @ID INT
AS
BEGIN
  SET NOCOUNT ON;

  SELECT
    s.ID,
    s.Age,
    s.Height,
    s.Weight
  FROM portal.Samples s
  WHERE s.ID = @ID;
END;
GO

--
-- Create or alter procedure [e_portal].[spGetAllPatients]
--
GO
PRINT (N'Create or alter procedure [e_portal].[spGetAllPatients]')
GO
CREATE OR ALTER PROCEDURE e_portal.spGetAllPatients
AS
BEGIN
  SET NOCOUNT ON;

  SELECT
    p.ID,
    p.Name,
    p.Sex
  FROM portal.Patients p
  ORDER BY p.ID;
END;
GO

--
-- Create or alter procedure [e_portal].[spAddUserRole]
--
GO
PRINT (N'Create or alter procedure [e_portal].[spAddUserRole]')
GO
CREATE OR ALTER PROCEDURE e_portal.spAddUserRole (
  @UserId BIGINT,
  @RoleId BIGINT
) AS
BEGIN
  SET NOCOUNT ON;

  INSERT INTO e_portal.UserRoles (UserId, RoleId)
  VALUES (@UserId, @RoleId);

  RETURN 0;
END
GO

--
-- Create user [portal_reader]
--
PRINT (N'Create user [portal_reader]')
GO
CREATE USER portal_reader
  WITH PASSWORD = ''
GO

--
-- Create schema [app]
--
PRINT (N'Create schema [app]')
GO
CREATE SCHEMA app AUTHORIZATION dbo
GO

SET NOEXEC OFF
GO