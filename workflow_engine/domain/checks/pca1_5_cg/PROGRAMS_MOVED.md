# Deprecated program paths

DomainPrograms for this check moved to algorithm-generic fixtures under
`workflow_engine/domain/fixtures/` (e.g. `mc_stability.program.json`, `data_driven.program.json`).
Study identity belongs in `project_*.json` / instance `projectPath`, not in the program filename.
