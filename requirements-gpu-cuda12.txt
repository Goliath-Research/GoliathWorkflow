# GPU/accelerated requirements (CUDA 12.x).
# Requires Python 3.10+. Install with:
#   pip install -r requirements-gpu-cuda12.txt --extra-index-url https://pypi.nvidia.com
# Used by: centroid comparison (cupy, cudf), GPU cleanup (torch), RMM via cudf.
cupy-cuda12x
cudf-cu12
torch
